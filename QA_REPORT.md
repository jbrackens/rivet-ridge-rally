# RIVET RIDGE RALLY — QA Report

**Report date:** 2026-07-15

**Milestone:** `1.0.0-rc.2` working-candidate hardening

**Current source:** `1.0.0-rc.2` source snapshot; no annotated `v1.0.0-rc.2` tag or release-bound rebuild

**Overall code-owned QA status:** UNVERIFIED

**Release decision:** NOT READY

## 1. Executive result

Player feedback reopened keyboard defaults, first-play training, and concept-art fidelity after `v1.0.0-rc.1`. Subsequent source review also found and corrected an unsafe initial persistence-retry path, the absent loading/countdown race gate, a Turbo-only bypass of the required Ride lesson, and an unbounded compressed-rider request that could hold the loading gate forever. The fixes are being qualified as a new `1.0.0-rc.2` candidate. The `v1.0.0-rc.1` tag, artifact, reports, and test evidence remain immutable historical records; none of that evidence is represented as qualification of the changed candidate.

An earlier `rc.2` snapshot produced the static, unit, Chromium, campaign, selected cross-engine, performance, soak, manifest, and Chrome-smoke results retained below. Those results remain useful historical evidence for their exact recorded bytes and scopes. They do not qualify the current changed tutorial/visual work. The automated suite is intentionally paused at the owner's request; no current-source lint, unit, Playwright test-runner, performance, soak, audit, manifest, or offline-smoke pass is claimed. A current normal build and preceding scripted headless installed-Chrome render captures were executed separately and are recorded below without broadening their scope.

The controlled visual gate remains open. The retained controlled run and repeat passed the editor baseline but differed from the obsolete desktop, portrait, and high-contrast baselines by 61%, 36%, and 39%, respectively, against a 2% threshold. Those frozen compositions are now superseded and the baselines remain untouched. The apparent black wedge seen while inspecting one high-contrast capture was a decoder/display artifact; the source PNG is clean and no product defect is recorded for it. The obsolete-baseline failure is still unresolved and must be replaced by fresh accepted compositions and a rerun, not waived.

The retained 21-file normal-build format-1 manifest at `artifacts/history/release-manifest-1.0.0-rc.2-earlier-snapshot-format-1.json`, with aggregate SHA-256 `6a55c00ea36debe543ab946dee06dc5fa73cf8a13d45b047aec399469c9931a3`, and the schema-1 installed-Chrome smoke were recorded from the same earlier working session. Neither format records a cryptographic link between those two evidence files, and neither matches or qualifies current `dist/`. The current artifact/production-smoke gate is **UNVERIFIED** until the paused checks resume and service-worker `shell-v30`, an official format-2 manifest, and manifest-bound installed-browser smoke are qualified together.

The retained earlier soak pass is scoped honestly. It completed 58 races with 0 timeouts over 1,800,607 ms of active workload and reported no failed requests, HTTP errors, harness errors, or unexpected console/page messages. Its FPS is diagnostic because the browser was headless and offscreen scheduling throttled animation. The artifact explicitly requires manual review of memory, input-latency, and fixed-step trends: heap use rose from 14,761,964 to 28,469,480 bytes; input-window means improved rather than accumulated; and the cross-restart cumulative dropped-simulation counter reached 706,442 ms even though the per-attempt maximum trend fell by 1,441.92 ms between the first and last windows. This cumulative headless counter is retained as evidence, not treated as a headed-FPS result or silently generalized into a physical-device conclusion.

The canonical workspace now uses the neutral `Rivet Ridge Rally` directory name. Physical device/browser coverage and owner-controlled legal, licensing, hosting, deployment, support, and release actions remain unresolved. This report does not claim launch readiness.

## 2. Status definitions

| Status | Meaning in this report |
|---|---|
| `PASS` | The specifically named scope was executed against the recorded candidate state and produced evidence; any later source difference is stated explicitly. |
| `FAIL` | A mandatory behavior, artifact, or release gate is absent or did not pass. |
| `UNVERIFIED` | Required evidence has not been executed for this changed candidate or the required environment was unavailable. |
| `EXTERNAL BLOCKER` | Completion requires owner credentials, legal approval, publishing access, or another owner action outside the repository. |

Statuses are deliberately scoped. A focused Chromium pass does not qualify other browsers, a direct Vite compile does not replace the repository asset gate, and an emulated viewport does not prove physical-device ergonomics or performance.

## 3. Candidate identity

| Item | Status | Recorded evidence |
|---|---:|---|
| Working candidate | UNVERIFIED | Changes are assigned to `1.0.0-rc.2` and tracked by the commit containing this report, but the automated suite is paused and no annotated release tag or release-bound rebuild exists. |
| Historical predecessor source tag | PASS | Annotated `v1.0.0-rc.1` remains an unchanged historical source identity only. |
| Historical predecessor rollback artifact | UNVERIFIED | Historical reports name predecessor checks, but no tested immutable artifact archive locator plus retrieval/hash evidence is recorded. The source tag and reports alone do not prove a deployable rollback package. |
| Immutable `rc.2` source identity | UNVERIFIED | Format-2 guard source now requires a clean final revision and annotated `v1.0.0-rc.2` at `HEAD`, but the retained build embeds `dirty=true`, the tag is absent, and the deferred fixture was not run. |
| Current `rc.2` artifact identity | UNVERIFIED | The historical format-1 manifest at `artifacts/history/release-manifest-1.0.0-rc.2-earlier-snapshot-format-1.json`, with 21 files and aggregate SHA-256 `6a55c00ea36debe543ab946dee06dc5fa73cf8a13d45b047aec399469c9931a3`, and installed-Chrome smoke belong to an earlier normal build and do not match current `dist/`. Regenerate a format-2 sidecar after the candidate is frozen and testing resumes. |
| Release-input tracking | UNVERIFIED | The source snapshot tracks the intended source, fixture, Node-pin, provenance, art/font, and retained-history files in the commit containing this report. Post-commit clean-tree verification, pre-tag qualification, an annotated candidate tag, and a clean release-bound rebuild remain required. |

### Current disposition and explicitly historical scoped evidence while automated testing is paused

| Check | Status | Recorded evidence and scope |
|---|---:|---|
| Latest PB, AI scheduling, responsive, and release-binding corrective slice | UNVERIFIED | The permitted 05:53 normal build compiles bounded optional standing-Solo lap/checkpoint arrays, strictly-faster replacement, prior-PB signed Results comparisons, symmetric rival-pair forgiveness, per-fixed-step live and post-finish classification contact, 320/621/780 px HUD/control clamps, a 320×568 touch-scrollable tutorial card, child-Node executable provenance/final revalidation, credential-free root normalization, root/index byte equality, same-origin browser navigation, and one-response service-worker root/index caching. Deferred unit, rendered-component, E2E, release-fixture, smoke-support, and worker-install regressions exist but were **NOT RUN**. Static second-pass reviews found and source-corrected narrow lesson-marker, pause-overlay stacking, vertical-containment, and race-gate-timeout test risks; no remaining concrete concern was reported; that review plus compilation is not behavioral, browser, persistence, accessibility, offline, artifact, or release qualification. |
| Gameplay, presentation, accessibility, and release safeguard source | UNVERIFIED | The permitted 05:53 build compiles: normal Ride heat that approaches the safe 62 ceiling while Turbo may exceed it; one grounded same-lane rear-impact policy applied after every live and classification fixed step to every unfinished AI pair; course-anchored presentation-only lane numerals `1–4` plus a transverse campaign/Rider School start treatment with compact diagnostics and authored-Test-Ride exclusion; a pure portrait predicate (`height > width || width < 680`) shared by resize and render; viewport/px-bounded normal and mirrored controls at 320–780 px / 140% UI scale with 44 px minimum targets and separated Heat; native semantic hold/pulse touch behavior and a bounded Recovery progressbar; format-2 manifest/runtime-commit validation plus byte/SHA/aggregate binding of every served production file before and after Chrome smoke, including cached Practice offline; and a `shell-v30` install that fresh-reloads same-origin assets and deletes its partial current cache before rethrowing on failure. Focused unit, E2E, smoke-support, and service-worker-install source exists, but every such check was **NOT RUN**. This is compilation/source inventory only, not runtime, browser, accessibility, visual, offline, performance, artifact, or release qualification. |
| Post-audit P1 corrective slice | UNVERIFIED | The permitted 05:53 build compiles per-tick environment/player/track/AI/contact coupling for render-frame-independent catch-up; a conflict-safe native-v3 migration that swaps occupied arrow actions onto vacated `KeyA`/`KeyD`; explicit Canyon-panorama exclusion for authored Test Rides; a built-in focal-bike fallback with complete spinning knobby assemblies, hubs/discs, layered panels, exhaust, and rear `22`; Barlow Condensed 700/900 WOFF2 preload/CanvasTexture use/OFL notices; and candidate-specific staged production-smoke bundles that wait for both race gates and atomically retain only captured evidence. Focused regression source exists, but no unit, migration-browser, fallback capture, font-rendering, smoke, offline, or concept-fidelity check ran during the pause. |
| Earlier static gameplay/editor/persistence/accessibility audit | UNVERIFIED | Three independent source-only reviews found no P0 and 16 unique P1 defects. Source corrected those 16: selecting/editing reopened placements, 0–20,000 m editor navigation, pointer-derived non-color placement validity, bounded blocked-IndexedDB open, strict future-version-safe profile reads, atomic corrupt-track quarantine plus portable tagged recovery export, bounded strict JSON preflight, safe-area/scrolling short-phone pause, visible toggle focus, 44 px tutorial Skip, in-card narrow tutorial captions, typed one-shot wheelie crash feedback, accurate consecutive crash totals, UI-scale-aware narrow telemetry, competitive-only podium copy, and an authoritative HUD snapshot before countdown. Focused unit/component/Playwright regression source was added, and the permitted 05:53 normal build compiles that earlier corrective slice. No behavioral `PASS` is claimed. |
| 2026-07-15 follow-up static audit and corrective slice | UNVERIFIED | Three source-only follow-ups found no P0 and corrected held-touch precedence, cross-tab recovery, replay atomicity, the shared 500-module ceiling, selective profile recovery, exact pending Test Ride retry, and budget-before-materialization recovery encoding. Later audits identified and source-addressed campaign grade, multi-part runtime modules, venue density/signatures, the winding/graded authored-route model, and full rotated parent-footprint Start/Finish containment. None of the focused regressions was executed. |
| Current rider/rival concept-fidelity source slice | UNVERIFIED | Static source review found that every rival deliberately skipped the fallback player's tread/brake/panel/exhaust branches, used alternating generic accents, and displayed two plain bars instead of a number; the rider's motion was whole-group-only. Working source now gives the fixed five-entry field authored palettes and distinct `17`/`31`/`46`/`58`/`73` jersey/rear-plate CanvasTextures, shared complete procedural bike assemblies, and a six-pivot rider rig driven only by existing render snapshots. Reduced Motion resets nonessential joint motion and bob while retaining the static crash silhouette. Canvas/object diagnostics expose the pose style, rival detail style, number set, pivot count, and per-rider number. The latest permitted normal build typechecked and compiled this source. No unit test, browser session, screenshot, accessibility review, draw-call/performance run, soak, audit, manifest, or smoke command was executed. Runtime geometry attachment, number readability, palette separation, motion, disposal, and performance are **UNVERIFIED**; compilation is not concept acceptance. |
| Latest normal production build for the current app source | PASS | On macOS 26.5.2 arm64 with Node v26.4.0 and npm 11.17.0, the permitted `npm run build` ran from 2026-07-15 10:26:16 to 10:26:21 CEST and exited 0. It passed the exact 3D/title/icon/panorama/font/license asset contracts, whole-project strict typecheck, a 142-module Vite production compilation, and 43,825-byte notices generation (`5fa8f7b4aaf95b4cc28b9b36a10c7f451e0ef83e6ac959d530ed3fdceacb60d0`). `dist/` contains 24 files / 6,679,904 raw bytes; `dist/index.html` is 1,418 bytes with SHA-256 `4252cb572300d9e2dd838b7e39f8440b8b40b6762556e17cd9f3d7b44690f433`; `dist/sw.js` remains 5,722 bytes with SHA-256 `f5cc9b23c2d507b9cede7a11cd72f62d05549c4ae72888853b5191d4cb6b78ca` and `shell-v30`. The 282,888-byte game-view chunk is `GameView-Cy_z8oq_.js`, SHA-256 `3517031f108f19e570938c10d31fb6e58af56ecb8859ea25a09355b6ef575bd4`; the 612,073-byte shared route chunk is `CoursePresentationRoute-CMRHtjX1.js`, SHA-256 `87e856adf652bb8a49b45ed3d96256221a360ca7edff9a94341a17fa63af71b8`; and the 56,935-byte editor chunk is `TrackEditorScreen-niBUL8yv.js`, SHA-256 `6c800124e378c8e16dc420210b1e696e3418d4843c537ebab3218e3797b1f5f0`. It embeds commit `62664b1f8ba814f3ec685d228fa4e600e6e7dc84` with `dirty=true`. The build compiles the bounded full-range editor continuation/pick surface, module-inclusive orbit-safe overview fit, adaptive hidden picking mesh, pause/input fallback fixes, comprehensive tutorial source, five numbered rival palettes and articulated rider rigs, in addition to the earlier authored-route work. Working source also contains the release-evidence hardening, including pre-write symlink-ancestor containment and deferred regression source, but the normal build does not exercise that Node smoke path. A final read-only review found no remaining concrete issue in the helper or call sites; the regression itself remains unrun. No lint, standalone unit, migration-browser, browser, accessibility, audit, manifest, smoke, performance, or soak command ran, so behavior and visual acceptance remain **UNVERIFIED** and this is not an immutable release artifact. |
| Superseded 08:56 build attempt | FAIL | The permitted `npm run build` begun at 2026-07-15 08:56:44 CEST passed asset/art/license verification, then strict typecheck stopped with TS6133 for one unused local declaration in `CoursePresentationRoute.test.ts`; Vite compilation and notices generation did not run in that attempt. The dead declaration was removed; the 08:57 build then succeeded and was later superseded by the 10:09 and current 10:26 builds. |
| Superseded 05:53 normal build | PASS | On macOS 26.5.2 arm64 with Node v26.4.0 and npm 11.17.0, `npm run build` exited 0 during the permitted 2026-07-15 05:53:20–05:53:23 CEST window. The build wrapper passed strengthened `assets:verify`: the 29,132-byte GLB (`8668d05f42f3b8303c0825b8d4aa4b9c66a8ef64acba18f2df70b69f9a8f595b`) and 2,236-byte KTX2 (`a86588924b53fd51915d505d0ba23e343ac3e84a5139c687845a49f8ad8a1ba2`) were verified, with 22 Meshopt views, 34 nodes, 15 meshes, 7 materials, one texture, 30 mesh-bearing nodes, one merged 14-block tread mesh per wheel, one merged 10-segment rear number, and validator result 0 errors / 2 expected warnings / 3 informational notices. It also passed the exact 3D manifest/directory, shipped/source-title, and app-icon identity contracts and matched the 1,801,764-byte 1774×887 Canyon panorama (`43b39c9075428e9bc081b3d8f216fae0027e889f89caa59586aa6097080b26a5`), opaque RGB PNG structure, one C2PA chunk, and embedded generation/provenance markers. It also verified the 14,888-byte 700 WOFF2 (`e3e520cb7468f2efd60bc2ce96694567c3312f4ef414fffbe605e3ad146b24e6`), 13,940-byte 900 WOFF2 (`2036f7f4aacc7759dcf8c6be2bd5bbde873c25a860f24dca0530ad14d4937723`), their `wOF2` signatures, and the complete 4,377-byte OFL (`186d750eb496a4c17a76385f82be6aea2ac1cf2de074a811d63786cf374ea73f`). Whole-project strict typecheck and Vite production compilation completed, and notices verification completed. Its `dist/` had 24 files / 6,632,207 raw bytes; `THIRD_PARTY_NOTICES.txt` is 43,372 bytes with SHA-256 `cacb32a0ba4d78a481a2f29963220b1e13ac403b6a0337cc711814d00bce50a6`. The build contains service-worker cache generation `shell-v30`; `dist/sw.js` is 5,722 bytes with SHA-256 `f5cc9b23c2d507b9cede7a11cd72f62d05549c4ae72888853b5191d4cb6b78ca`. It includes the generated focal-bike detail/draw-budget pass, bounded focal shadow casting and rider presentation motion, Canyon panorama loading/fallback/crop/disposal, reviewed editor-session continuity, immediate attempt-scoped result commit, guarded finish presentation, persistence-recovery engine-lifecycle changes, and deterministic bilateral Canyon cooling-gate venue pockets with authored custom-track exclusion. The build embeds commit `62664b1f8ba814f3ec685d228fa4e600e6e7dc84` with `dirty=true`, so it is development/build evidence rather than immutable release evidence. No release manifest, production/offline smoke, dependency audit, automated test, browser capture, or performance command was run. |
| Current build entrypoint identity | PASS | The latest permitted build produced a 1,418-byte `dist/index.html` with SHA-256 `4252cb572300d9e2dd838b7e39f8440b8b40b6762556e17cd9f3d7b44690f433`. This byte record is build evidence only; it is not a format-2 manifest, served-byte proof, or production smoke. |
| Superseded intermediate normal build | PASS | Historical build-only evidence: `npm run build` exited 0 at 2026-07-15 04:16 CEST before the final static-audit P1 corrections. That intermediate `dist/` had 24 files / 6,605,996 raw bytes; its 1,418-byte `index.html` SHA-256 was `205d4a39731ad08818e2c5afa9e2ef429a7f0cedd3819e1b4b1f2ac379b7cbbb`. Its contemporaneous notices and service-worker records are historical rather than moving references to current bytes. It does not qualify the current source or artifact. |
| Release provenance guard source | UNVERIFIED | Format-2 source now captures clean `HEAD`, reads and revalidates package/lock/Node bytes from its detached checkout, captures and finally revalidates the exact annotated tag object/type, and rechecks the absolute npm CLI's file identity/SHA-256 after its version, install, and build invocations. It isolates empty user/global npm configs with install scripts forced enabled and enforces ignored-entry allowlists before install, after `ci`, and after build. Only regular isolated output replaces root `dist/`; recursive guarded cleanup removes stale `dist/` and file-or-directory sidecars on failure. Hostile-config/fake-CLI fixture source covers commit-derived output, phase injection, same-commit tag replacement, npm-CLI mutation, cleanup, provenance, and artifact guards. Nothing was executed, so this remains source inventory rather than a pass. |
| Release evidence binding and shell-v30 source | UNVERIFIED | Source now derives a public runtime build commit/dirty marker directly from Git inside Vite configuration. Measurement and soak inventory/hash local `dist/`, verify every served file plus `/` before and after, force and record non-`auto` quality, record host/browser/device/GPU fields, bind runtime commit to clean captured source, and fail request/HTTP/page/console gates with a nonzero status. Measurement release `PASS` additionally requires a headed run, desktop normal/stress mean ≥58 FPS with p95 work ≤16.67 ms, emulated-mobile normal/stress mean ≥30 FPS with p95 work ≤33.33 ms, and gzip bytes <12,000,000; emulation is not physical-mobile proof. Practice soak is diagnostic; release `PASS` requires Rival and 30 active minutes. Production smoke binds the runtime commit to the format-2 manifest, rechecks all served bytes after the journey, opens cached Practice to `racing` offline, and promotes via fixture-testable atomic helpers. Service-worker source/fixtures now select `shell-v30`. The permitted 05:53 build embeds runtime commit `62664b1f8ba814f3ec685d228fa4e600e6e7dc84` with `dirty=true` and typechecks/compiles this source; no focused fixture, performance, soak, manifest, browser, or smoke command ran, so every behavior and release-artifact claim remains **UNVERIFIED**. |
| Tutorial/input/pause/offline corrective slice | UNVERIFIED | The latest permitted normal build compiles the active-lesson `TutorialLessonGate`, ordered fresh evidence, measured mud slowdown, the atomic 550 ms input-preserving lesson handoff, the active critical-heat latch, focused **Retry this lesson** reconstruction, interactive-target keyboard guards, repeated-key pause guard, exact mapped-command gamepad arbitration, held-touch precedence, held-key restoration after touch release, page-side service-worker-controller invalidation, and `shell-v30` cache generation. Focused retry preserves the active lesson, earlier markers, and cumulative recap data while resetting the simulation, route/time/checkpoints, bike/heat, handled obstacles, replay/presentation transients, latched keyboard/touch commands, and active evidence so the named one-shot obstacle can be attempted again. Deferred unit/browser regression source covers early-evidence rejection, ordered multi-signal lessons, held Ride through the handoff, heat latching, all 12 riding lessons, remapped-Space native retry activation, deliberate bump failure/retry/success, finish integrity, and touch precedence over an actively commanded gamepad. No regression, browser, real worker-transition, or physical-input check was run while automation remains paused, so compilation/source review is not a behavior pass. |
| Original mobile touch pictograms | UNVERIFIED | The permitted 05:53 build contains six project-authored inline SVG pictograms for lane left/right, pitch up/down, Ride, and Turbo. They use `currentColor`, remain decorative and non-focusable inside the existing named buttons, retain visible Ride/Turbo text, preserve hit areas/safe-area layout, and mirror position without changing meaning. Asset inventory, concept-ledger, and deferred accessibility/browser assertions were updated; independent static review found no concrete P0/P1. No current portrait frame, high-contrast inspection, physical touch/reach check, or accessibility run was executed, so concept fidelity remains **NOT CAPTURED / NOT ACCEPTED / UNVERIFIED**. |
| Shared course presentation renderer | UNVERIFIED | The latest permitted build contains the renderer-only `CoursePresentationRoute` with C2 lateral bends and positive rolling grades between protected campaign corridors plus schema-v2 checkpoint-authored smootherstep centerlines shared by Builder and Test Ride. Start/Finish are zero anchors; omitted anchors and migrated v1 routes retain exact identity mapping. The editor surface, modules, camera, and pointer ribbon consume the same route used by Test Ride presentation while fixed-step progress, contacts, AI, timing, and replay remain scalar. Focused route/editor/migration source exists but was not executed; runtime/browser inspection, visual capture, regression tests, performance, and soak were **NOT RUN**. Concept fidelity remains **NOT ACCEPTED / UNVERIFIED**. |
| Dirt material and lane silhouette | UNVERIFIED | The permitted 05:53 normal build contains separate deterministic sRGB dirt/paint color and non-color height textures, with one shared periodic rut path and transform so visible ruts align with lighting relief. Painted lane guides are flattened in the height data. Campaign/tutorial presentation uses shallow trapezoidal grass berms; authored custom courses omit them so straight generic rails do not cross saved curves or banks. Campaign internal high-contrast guides clear the maximum berm top. Each authored high-contrast stripe now receives its own exact world-mapped piece gap only where a height/bank-following overlay of the same category visibly reaches that base channel; 180-degree reversal, quarter-turn geometry spans, and valid negative-height fallbacks are handled explicitly. Geometry-signature batches instance overlays across pieces/laps instead of creating one mesh per stripe. Read-only reviews found and corrected P1 risks involving campaign guide occlusion, authored-course berm clipping, color/height rut misalignment, authored overdraw, unbounded per-stripe mesh construction, and rotated/negative-height fallback loss; two final independent reviews reported no remaining concrete P0/P1 in this source path. No standalone surface image or external asset was added for this material pass. Runtime/browser capture, accessibility review, custom-track inspection, performance measurement, and automated regression were **NOT RUN** while automation remains paused; this work is **NOT ACCEPTED / UNVERIFIED**. |
| Canyon festival panorama | UNVERIFIED | Source inspection reviewed the original 1774×887 matte and found layered red-rock mesas, sky/clouds, teal festival props, flags, and sparse vegetation with no road, rider, UI, logo, visible text, watermark, or obvious third-party branding. The permitted 05:53 build verifies its exact bytes, PNG structure, single C2PA chunk, and embedded provenance markers. Canyon Kickoff/Rider School load it through a five-second `ImageBitmapLoader` deadline as a centered-cover 2D background, decoding 1024×512 Low, 1536×768 Medium, or full source High, with the generated sky as fallback and engine-owned bitmap/texture disposal. Editor-authored Test Rides explicitly report the asset as not applicable despite inheriting Canyon visual defaults. It is presentation-only and quality changes apply on the next race. An independent source-only audit found the authored-course leak, which source now excludes; a Playwright failure-path regression exists but was **NOT RUN**. No browser frame, fallback execution, performance measurement, similarity/legal review, or owner acceptance has qualified the integration. |
| Canyon cooling-gate venue framing, grandstands, and watchtowers | UNVERIFIED | The permitted 05:53 normal build contains four deterministic bilateral cooling-gate watchtowers across Canyon's first two unique gate definitions and two in the one-gate Rider School route. Handcrafted Canyon reuses the existing platform batch for outward-rising timber tiers—two/three/four rows on Low/Medium/High—and raises only each cooling showcase's existing deck, canopy, and four posts into an elevated watchtower with a mirrored teal/coral roof. Existing deck spectators total 8/12/16 across the four Canyon towers and 4/6/8 across Rider School's two towers by quality. QA-fast source math is exactly 8 pockets and 16/24/32 tiers. Production High retains all 22 route pockets because each already clears the 5.6 m showcase guard, producing 26 pockets and 104 tiers after adding four towers. Non-Canyon and editor-authored courses receive zero Canyon-specific watchtowers or tier pockets; separate per-theme density rules remain active. The five existing platform/canopy/post/body/head batches, shadow policy, and route transform are preserved; no material, geometry type, asset, or visible draw batch is added. Exact deferred assertions cover tier and tower style/count/staffing across quality presets plus authored isolation. Independent source reviews found and corrected the deferred production-High count mismatch, then found no remaining concrete P0/P1 in allocation, transforms, vertical clearance, diagnostics, lifecycle, or isolation. No browser frame, accessibility check, draw-call measurement, performance run, or regression test was executed while automation remains paused; concept fidelity is **NOT ACCEPTED / UNVERIFIED**. |
| Multi-part obstacle rules and module silhouettes | UNVERIFIED | The latest permitted build compiles one frozen contact list shared by player and AI, exact section spans/keys, parent-derived policies, distinct Tabletop/Double Jump/Jump Chain/Trail Bump/Bump Row/Offset Barrier/Sky Kicker meshes, one sideways footprint for quarter-turned rhythm modules, union-lane planning for Offset Barriers, and normalized four-hit Bump Row retention. Focused resolver regression source exists, but it and every editor/runtime player/AI/rotation/browser path were **NOT RUN** while qualification is paused. |
| Venue density and signature kits | UNVERIFIED | The latest permitted build compiles four/five/six-row start stands with 56/90/132 spectators, 2/4/6 merged service clusters, Pine bump roots/red markers, Foundry colored gantries/visible furnace panels, Summit finale equipment, and capped course-length clutter. A second static audit found and source-corrected buried/floating terrace placements before the build. No browser frame, draw-call reading, performance run, or owner comparison was executed. |
| Canyon course-edge safety wall | UNVERIFIED | The permitted 05:53 normal build contains one quality-tier `InstancedMesh` batch covering both course edges for Canyon Kickoff and Rider School at x ±7.70. Low/Medium/High use overlapping 8/6/4 m spans with 0.32/0.28/0.25 m overlap; expected instance counts are 660/880/1,320 for the full Canyon route, 330/440/660 for Rider School, and 72/96/144 for the QA-fast route. The wall receives shadows on every tier and casts them only on High. Authored custom Test Rides return zero wall batches and blocks. Canvas diagnostics and deferred browser assertions record the style, batch count, block count, quality tiers, and authored exclusion, but those assertions were **NOT RUN**. Independent source review found no concrete P0/P1 in coverage, clearance, count math, or custom isolation. This is presentation-only build/source evidence: browser appearance, non-collision readability, actual renderer draw calls, performance, accessibility, and owner acceptance remain **UNVERIFIED**. |
| Focused workspace-name/path scan | UNVERIFIED | The 19:55 scoped source/`dist/` scan passed and the canonical workspace rename remains complete, but that scan predates the latest permitted build and later route/module/theme changes. It is retained as earlier scoped evidence only. A current full source/license/secret/history audit remains paused and no current-candidate scan pass is claimed. |
| Scoped production render capture | PASS | Before the final audio-unlock, tutorial grass-exit/Ride-evidence/recap/isolation, persistence/editor-session, race-gate/asset-deadline, cache-generation, shared-course renderer, dirt-surface, authored-guide, focal-bike, Canyon-panorama, tiered-grandstand, and authored-route corrections, a script launched installed Google Chrome 150.0.7871.115 headlessly, loaded `http://127.0.0.1:4173/` at fixed 1672×941 and 390×844 viewports, and captured start/midcourse frames that were then manually reviewed. The preceding compressed bike reached `ready` in both profiles, and that capture session recorded no console error, page error, failed request, or HTTP error. Local-only evidence: ignored `artifacts/visual-review/rc2-manual-latest/manual-browser-inspection.json` plus the four PNGs beside it. The in-app Browser connector could not initialize (`Cannot redefine property: process`). This scripted capture is not the paused Playwright test suite, headed/native-device execution, current-byte production smoke, accessibility or touch qualification, or performance evidence; it is not bound to the latest `dist/` and does not show the latest graded route, multi-part module, venue-density/signature, or Builder/Test Ride centerline changes. |
| Five-track visual capture | PASS | The same preceding headless installed-Chrome session captured start and midcourse views for Canyon Kickoff, Pine Run, Coastline Clash, Foundry Flight, and Summit Showdown at a fixed 1672×941 viewport. The preceding compressed bike reached `ready` for all five tracks, and that capture session recorded no console error, page error, failed request, or HTTP error. The ten PNGs were manually reviewed. Local-only evidence: ignored `artifacts/visual-review/rc2-manual-five-track-latest/manual-five-track-inspection.json` plus the PNGs beside it. This is earlier scoped comparison evidence only, not qualification of the current visual assets/renderer, a gameplay matrix, headed/native-device evidence, performance evidence, or owner acceptance. |
| Tutorial course isolation | UNVERIFIED | Source review found that an editor Test Ride left its custom payload in session state and Rider School previously consumed it despite selecting tutorial mode. `GameView` now masks custom-track state for tutorial sessions and derives both the accessible course identity and engine options from that normalized value, so the canonical Canyon training route is selected at the sole production engine-construction boundary. A focused editor → Test Ride → Festival menu → Rider School Playwright regression now asserts canonical Canyon positively and the custom course negatively. The regression was **NOT RUN** because automated testing remains paused. |
| Tutorial surface inspection | PASS | A production visual build preceding the final corrections showed the six-control intro with `← Left / → Right` lane defaults and `↑ Up / ↓ Down` pitch, 12 visible lesson markers with a non-color `▶` current marker, Lesson 1 content, and zero premature “Enter the festival” actions. The current desktop/mobile render inspection did not exercise the tutorial, paused touch/gamepad adaptation, explicitly labeled pause-menu skip, or landscape layout. This scoped earlier result does not prove the current full under-three-minute sequence or the new negative paths. |
| Concept-art acceptance | UNVERIFIED | **NOT ACCEPTED.** The manually reviewed fixed-viewport captures show improved full-bike framing, articulated rider/readable `22`, atmosphere, and venue separation, but they predate the graded shared `CoursePresentationRoute`, distinct multi-part module silhouettes, expanded festival/per-theme density and signature kits, rebuilt focal bike, Canyon festival panorama, elevated staffed cooling-gate watchtowers, tiered Canyon grandstands, continuous Canyon/Rider School safety wall, and schema-v2 authored Builder/Test Ride centerline. The latest source/build addresses the earlier flat-grade, collapsed-jump, underscaled-density, incomplete-signature, and identity-only authored-route findings. No current runtime/browser capture, desktop/portrait crop review, motion/performance evidence, human similarity/legal review, or owner comparison has inspected those changes. Near-field environment/material quality and the complete composition remain unaccepted; no baseline was updated, and compilation/build evidence alone cannot prove visual fidelity. |

## 4. Retained evidence from an earlier `1.0.0-rc.2` snapshot

The `PASS` and `FAIL` labels in this table are historical results for their recorded scope and working state, not cryptographically bound current-working-tree qualification. Current disposition is recorded in Sections 5–7. The current automated suite remains paused.

| Check | Historical result | Executed result and scope |
|---|---:|---|
| Final strict TypeScript check | PASS | Whole-project typecheck completed without errors after the final scenery-density adjustment. |
| Final full lint | PASS | The complete lint suite passed after the final scenery-density adjustment. |
| Final Vitest suite | PASS | 9 test files / 78 tests passed after the final scenery-density adjustment. |
| Final Vitest coverage run | PASS | `npm run test:coverage` passed 9 files / 78 tests. V8 reported 17.06% statements, 24.00% branches, 16.61% functions, and 17.33% lines; no repository coverage threshold is configured, so this is execution evidence rather than a claim of comprehensive unit coverage. Browser suites carry the rendered runtime/editor/UI coverage. |
| Direct QA-mode Vite build | PASS | `VITE_QA_MODE=1 npx vite build` completed. This proves compilation only because it bypasses the repository asset-verification wrapper. |
| Quality presets + tutorial — Chromium | PASS | 2/2 passed for the earlier snapshot, but the assertion scope was insufficient: it did not prove that finish could not bypass the sequence, that the training bump/grass return/final barrier were explicitly completed, that storage failure could not bypass training, that cumulative actions could not pre-clear later lessons, or that the paused intro sampled touch/gamepad devices. This historical result does not close the current tutorial gate. |
| Sequential/default-binding migrations — Chromium | PASS | 4/4 migration cases passed, including conversion of the exact legacy default lane pair while preserving custom bindings. |
| Keyboard remapping — Chromium | PASS | 1/1 passed; a custom lane binding remains accepted and usable. |
| Lifecycle — Chromium | PASS | 5/5 passed for the focused lifecycle group. |
| Reliability — Chromium | PASS | 6/6 passed after the paused fallback HUD began emitting and the tutorial reliability case explicitly started Lesson 1. |
| Offline service worker — Chromium | PASS | 1/1 passed. |
| Fresh keyboard race/save/retry — Chromium | PASS | 1/1 passed with the arrow-cluster defaults. |
| Gamepad emulation + persistence + release quality — Chromium | PASS | 3/3 passed. This is emulated-controller evidence, not a physical-gamepad pass. |
| Accessibility controls — Chromium | PASS | 7 checks passed; 1 touch-project case was intentionally skipped by project scope. |
| Selected cross-engine browser journeys | PASS | The selected Firefox/WebKit/mobile-chrome/mobile-safari/tablet-chrome run produced 19 passes and 74 project-scope skips. Firefox and WebKit passed fresh keyboard race, editor, settings persistence, and release-quality surfaces; WebKit also passed five lifecycle stress cases; the phone/tablet projects passed touch-race and mirrored-control journeys. The only two initial failures were one stale keyboard focus-order assertion in Firefox and WebKit; after adding Rider School to the expected tab sequence, the targeted rerun passed 2/2. This is engine/emulation evidence, not installed Safari/Firefox or physical-device qualification. |
| Core fresh race/editor — Chromium | PASS | The fresh-race and editor checks passed; the touch-project case was intentionally skipped by project scope. |
| Original campaign modes suite — isolated Chromium | PASS | The original isolated `e2e/campaign-modes.spec.ts` run passed 3/3 in 6.4 minutes. |
| Hardened campaign modes suite — isolated Chromium | PASS | The hardened suite passed 18/18 in 7.3 minutes: 16 independently timed track/mode cases plus Solo-to-Rival and Summit Mastery progression. Per-case race-health capture now preserves failure context without turning one slow case into an opaque suite timeout. |
| Controlled visual regression + repeat — Chromium/mobile-chrome | FAIL | Each controlled run executed four applicable baselines: editor passed; desktop race differed by 61%, portrait race by 36%, and high-contrast race by 39%, each against a 2% threshold. The controlled repeat produced the same failures. Baselines were intentionally not updated pending owner acceptance. |
| Headed local performance measurement | PASS | `artifacts/performance/final-headed-measurement.json` records headed Chromium 149.0.7827.55 on macOS arm64. Desktop 1920×1080: shell 1,284.12 ms, first race 324.48 ms, restart 175.35 ms, editor open/test 848.91/180.31 ms; normal FPS min/mean/p95 57/59.31/60 with frame-work mean/p95 2.71/4.45 ms and draw calls mean/p95 357.15/381; stress FPS 57/59.31/60 with 3.42/4.73 ms and 561.62/593 draws. Emulated mobile 390×844 at 2×: shell 1,363.59 ms, first race 123.89 ms, restart 157.23 ms, editor 855.33/176.38 ms; normal FPS 58/59.54/60 with 1.80/3.16 ms and 202.38/238 draws; stress FPS 59/59.77/60 with 2.30/3.25 ms and 266.46/338 draws. Both profiles recorded six heap stages and no console messages. The mobile profile is local viewport evidence, not physical-mobile performance. |
| Continuous 30-minute Rival soak | PASS | `artifacts/performance/final-30m-soak.json` records headless Chromium 149.0.7827.55, 1,800,607 ms active workload / 1,809,937 ms wall duration, 176 samples, 58 completed races, 0 timeouts, and restart min/mean/p95/max 3,365.95/3,583.24/3,908.50/4,208.60 ms. All 12 automated release criteria passed; there were 0 failed requests, HTTP errors, harness errors, or unexpected console/page messages. Four known headless `ReadPixels` warnings were separated. Manual trend and headless fixed-step caveats are recorded in Section 7. |
| `npm run assets:build` | PASS | The documented pipeline replaced only the LF-normalized 9,141-byte Basis license with the pinned TypeScript pipeline output already recorded in the asset inventory: 9,197 bytes, SHA-256 `a7d00bfd54525bc694b6e32f64c7ebcf5e6b7ae3657be5cc12767bce74654a47`. No GLB, KTX2, or asset-manifest content changed. |
| Final `npm run assets:verify` | PASS | The post-adjustment pipeline output matches the committed asset inventory, including the 9,197-byte Basis license and its recorded hash. |
| Final normal `npm run build` | PASS | After the final scenery-density adjustment, that earlier `1.0.0-rc.2` snapshot completed asset verification, strict typecheck, Vite production compilation, and notice generation. Its `dist/THIRD_PARTY_NOTICES.txt` was 38,475 bytes with SHA-256 `c959caf9bfbb3d9b051921453e8a19132c1b40fec2ef3f3db41676d3492f84a1`; this historical row does not describe the latest permitted build. |
| Dependency audit | PASS | `npm audit --audit-level=high` completed with exit code 0 and reported 0 vulnerabilities. |
| Normal-build release manifest | PASS | The retained `artifacts/history/release-manifest-1.0.0-rc.2-earlier-snapshot-format-1.json` records format 1, `RIVET RIDGE RALLY` `1.0.0-rc.2`, 21 files, 4,689,233 bytes, and aggregate SHA-256 `6a55c00ea36debe543ab946dee06dc5fa73cf8a13d45b047aec399469c9931a3`. It is historical evidence for that earlier normal build only and does not match current `dist/`. |
| Installed-Chrome production smoke | PASS | `artifacts/production-smoke/chrome-smoke.json` records installed Google Chrome 150.0.7871.115 against the earlier normal build. It is not current-candidate smoke evidence. |
| Production browser inspection | PASS | The earlier normal build at `http://127.0.0.1:4173/` was inspected at desktop 1280×720 and emulated portrait 390×844. This remains historical responsive/control evidence only; it does not qualify the current tutorial/visual work or physical mobile. |

The headed performance and soak artifacts qualify their earlier QA-enabled build; the retained manifest and Chrome smoke qualify a separate earlier normal build. They must not be treated as one byte-identical artifact or as qualification of the current candidate.

## 5. Gameplay, controls, and persistence

| ID | Requirement | Status | Current evidence |
|---|---|---:|---|
| GP-01 | First-play tutorial demonstrates required mechanics in under three minutes and remains replayable | UNVERIFIED | Source now creates exactly one tutorial lap, holds the engine and race clock paused behind the Lesson 1 intro, and requires normalized Ride/throttle input at usable grounded speed so Turbo-only acceleration cannot clear Lesson 1. A separate active-lesson gate ignores future-lesson evidence until that lesson begins, enforces ordered jump/grass/recovery signals, measures actual coast/mud/grass speed loss, and freezes the simulation atomically for the 550 ms lesson handoff without discarding held Ride or Turbo. The named lane-choice barrier, bump, grass return, and final crash/recovery use explicit events; earned completion requires 12 lessons plus both drills; progress uses text/symbols in addition to color; the final adaptive recap lists all six controls; hydration failure enters tutorial session mode; and the pause-menu exit is explicitly labeled as a skip. Critical heat latches within its active lesson, so immediate Turbo release remains safe without sustained red-zone heat or overheat. Deferred helper/browser source covers early lane evidence, held Ride beyond the handoff, heat release, all 12 lessons, recap, and finish decisions, but none was run; the early-finish integration path also remains unexecuted. Manual inspection passed only the earlier intro/12-marker/Lesson 1 surface. The complete timed sequence, storage-failure behavior, and touch/gamepad lesson paths remain unqualified while testing is paused. |
| GP-02 | Campaign and every track/mode remain complete | UNVERIFIED | Earlier isolated suites passed 3/3 and 18/18 across the applicable matrix. Current engine changes have not received a regression run while testing is paused. |
| GP-03 | Loading and countdown hold simulation/race time until `GO` | UNVERIFIED | Source now keeps each non-tutorial engine paused until the compressed rider loads or a five-second deadline selects the built-in bike. Each engine owns an isolated Three.js loading manager; timeout and engine disposal abort its request and terminate the KTX2 loader, while a defensive late-result handler prevents post-readiness replacement. Canyon/tutorial readiness also waits no more than five seconds for its isolated panorama loader, then uses the already-present generated sky on failure/timeout/unsupported decode and closes late or disposed bitmaps. `GameEngine.start()` now emits the selected simulation's authoritative target, field size, and lap count before scheduling the first frame, replacing countdown placeholders. The accessible `3 → 2 → 1 → GO` gate then commits `GO` and allows two animation frames to paint before entering `racing`, so input and the fixed-step clock begin only after the visible signal; hiding the page during the gate leaves the race paused. Focused initial-HUD, stalled-bike, and failed-panorama regression source exists, but no runtime or automated assertion was executed while testing is paused. |
| GP-04 | Crash causes, telemetry, and results copy remain truthful | UNVERIFIED | Simulation now retains a typed first cause for landing, obstacle, rider-contact, overheld-wheelie, or external crashes through the active crash. The engine turns wheelie timeout into a precise crash caption, sound, and camera cue, and suppresses duplicate telemetry only for a crash landing observed in the same tick, so a stale landing marker cannot hide the next crash. Results reserve “Podium finish” for a multi-rider Rival/Mastery field and use “Run complete” for Solo misses, Practice, Custom, and single-rider runs. Focused simulation/engine/component regression source exists, but no audio, camera, gameplay, telemetry, or rendered-results execution ran during the pause. |
| CA-01 | Keyboard lane defaults use the arrow cluster | UNVERIFIED | A preceding scripted tutorial-surface capture confirmed the visible `← Left / → Right` map; source review confirms ArrowLeft/ArrowRight defaults and no hidden A/D lane fallback. Live final-candidate regression input was not run, so the full gate remains unverified. |
| CA-02 | Custom keyboard remapping remains supported | UNVERIFIED | Earlier focused Chromium remapping passed 1/1; current changes have not received a regression run. |
| CA-03 | Existing exact A/D defaults migrate without overwriting custom pairs | UNVERIFIED | Earlier migration cases passed 4/4; current changes have not received a regression run. |
| CA-04 | Gamepad and touch behavior remain correct | UNVERIFIED | Source now selects the first connected gamepad for play, pause, and optional vibration instead of hard-coding browser slot zero. Gamepad activity is derived from the exact mapped throttle/turbo/lane/pitch/recovery thresholds, so sub-command stick drift, sub-threshold triggers, and unmapped analog values cannot claim the gamepad. A 2026-07-15 source review found that active mapped gamepad input still ran before touch arbitration; source now evaluates active touch first, so a held touch action wins and an already-held keyboard command or active controller resumes only after touch is released. Focused first-connected-pad, drift/trigger-boundary, touch/gamepad-precedence, and held-key/touch-release unit-test source exists. The earlier focused emulated-gamepad group passed, but none of these current cases was run, touch-project cases were intentionally skipped, and no physical gamepad or touch device was exercised. |
| RL-02 | Sequential database migrations preserve rider data | UNVERIFIED | Earlier migration coverage passed through schema v4 only. Current source adds Dexie v5/native IndexedDB 50 and schema-v2 custom tracks. Structurally valid v1 tracks that meet current semantics upgrade in place by changing only `schemaVersion`; formerly valid center-contained tracks that need the stricter full-footprint repair remain stored as v1, normalize to editable v2 data in memory, and are not quarantined. Save/Test Ride/export still require current v2 validation, and future versions fail closed. Migration and legacy-repair journeys were **NOT RUN** while qualification is paused. |
| RL-03 | Initial persistence retry cannot overwrite an unread valid profile | UNVERIFIED | Source suppresses profile writes after an initial load failure, reopens storage, and re-reads settings/progress inside one transaction with quarantine. Either future schema aborts before mutation; invalid current records must be preserved; only missing/invalid sides receive session fallbacks; and a valid or concurrently updated companion is untouched and adopted. Focused selective-write, concurrent-companion, and future-schema regression source exists but was not run. |
| RL-04 | Session-mode editor keeps valid in-memory Test Rides playable | UNVERIFIED | A validated immutable snapshot enters the custom-race path immediately while its best-effort save runs independently. Before launch, a separately cloned tab-scoped editor session retains the exact thumbnail-bearing draft, undo/redo stacks, active category/module, and valid placement selection. Custom pause and results return directly to Track Builder, restoring that session even when IndexedDB is unavailable. A follow-up source review found and corrected a generic retry path that could previously mark persistence healthy without retrying the failed track: the exact cloned snapshot now remains pending, the operation-aware warning names it, retry reconciles the profile then saves that snapshot, failure keeps it pending, and only successful same-track save/export clears it. The latest permitted normal build compiles this path. Focused snapshot/retry and existing session-restoration regression source was **NOT RUN** while automation remains paused. |
| RL-05 | Persistence and interchange fail closed without destroying recoverable data | UNVERIFIED | Dexie auto-open is disabled behind one explicit open promise; a blocked version open rejects after a two-second grace period so Boot can enter retryable session mode. Strict full-record schemas reject future numeric profile versions without writing. Profile recovery and corrupt custom-track recovery now re-read current data inside their write transactions; invalid records must be quarantined before replacement/deletion, valid cross-tab replacements survive, and replay insertion/pruning is one transaction. Legacy center-contained v1 tracks needing only the new full-footprint repair remain readable/editable rather than being treated as corrupt. Track Builder discloses retained portable recovery packages whose tagged JSON representation preserves cycles and non-JSON structured-clone values. Import applies byte/depth/item/string preflight and strict unknown-field rejection before semantic validation. The latest permitted build compiles bounded depth/node/string/output accounting, Blob/File size preflight before byte reads, cycle-safe lazy Map/Set/array traversal, and fail-closed unmeasurable aggregate records. Focused source exists, but no unit, migration, or browser failure path was run. |
| ED-01 | Existing placements, full accepted route range, authored shaping, and placement validity remain editable | UNVERIFIED | The editor now raycasts placed descendants before the route surface, exposes an accessible placed-module picker, focuses selected modules, and provides orbit/zoom plus longitudinal drag, Shift-wheel, and 0–20,000 m range navigation. Schema-v2 checkpoints expose bounded Route turn/rise controls with undo/redo; new drafts and all three examples use safe nonzero anchors. The Builder generates a sampled dirt/shoulder/lane ribbon, routes modules/camera/pointer placement through it, and preserves incomplete drafts' far working range. Shared validation announces ✓ Valid or ! Invalid and requires every non-race module's full rotated parent footprint to lie strictly between Start and Finish. Focused validator/editor source exists, but interaction, save/reopen/import/export, legacy repair, long-route rendering cost, and browser geometry were not executed. |
| RL-13 | Restart, finish, visibility, renderer, input, storage, and offline lifecycle remain reliable | UNVERIFIED | A simulation finish now emits its immutable result synchronously. The store commits progression exactly once against the current attempt before the 650 ms presentation timer; retry/menu navigation cancels or invalidates only the presentation, and an old attempt cannot reopen Results. Mutable `progress.tracks` is no longer an engine-effect dependency, so profile reconciliation from paused Settings cannot reconstruct the live attempt; a focused store regression and paused-Settings lifecycle regression were added. Source also cancels/disposes stalled rider and panorama work, closes late/disposed bitmaps, removes quarantined custom-track records transactionally, normalizes custom replay pruning, keeps modal race content inert, ignores repeated keyboard pause events, orders active touch ahead of active gamepad input, and enforces exact `shell-v30` offline acknowledgement/current-cache preference with the panorama in the core cache. A later service-worker `controllerchange` now clears the page's readiness claim immediately and restores conservative copy until a fresh exact-generation acknowledgement. Synthetic page-side invalidation, repeat-key, and touch/gamepad-precedence regression source exists; real worker activation/cache transitions remain part of the paused production/offline gate. Earlier Chromium lifecycle/reliability/offline passes remain historical; none of these current regressions was run while testing is paused. |

## 6. Visual and browser acceptance

| Gate | Status | Required closure evidence |
|---|---:|---|
| Concept-art fidelity | UNVERIFIED | **NOT ACCEPTED.** The pre-final fixed-viewport captures, manually reviewed after generation, show improved full-bike camera framing, rider articulation/number identity, sky depth, venue separation, crowd pockets, cooling glow, surface contrast, mud readability, HUD hierarchy, and grouped mobile controls. They predate the graded renderer-only `CoursePresentationRoute`, distinct multi-part obstacle silhouettes/contacts, expanded festival/per-theme density and signature kits, separate non-color dirt-height/aligned-rut/campaign-berm pass, fitted authored guides, current generated bike, and the schema-v2 winding/graded Builder/Test Ride centerline. The latest source retains scalar gameplay authority while presenting positive campaign grades, module-specific forms, denser themed venues, and one shared authored route for Builder and Test Ride. No current runtime/browser capture, visual comparison, accessibility/custom-track review, performance check, similarity/legal review, or owner acceptance was performed. The complete environment/material finish remains unaccepted. |
| Rider/rival racing-distance fidelity | UNVERIFIED | Working source now gives all five rivals the fallback player's treads, brakes, panels, and exhaust; coherent palette pairs; real distinct two-digit jersey/rear-plate numbers; and the same six-pivot snapshot-driven rider articulation as the player. No current capture shows pack readability, number legibility, limb attachment, jump/lean/crash motion, or Reduced Motion, and no draw-call/frame-time measurement qualifies the added fixed field detail. Owner acceptance remains open. |
| Visual regression | FAIL | The retained controlled run/repeat differ from obsolete frozen baselines by 61% desktop, 36% portrait, and 39% high contrast. The apparent black wedge seen through one image decoder is absent from the clean source PNG and is not a product bug. The obsolete-baseline/frozen-composition failure remains unresolved; baselines stay untouched until a revised composition is accepted and rerun. |
| Five-track visual identity | UNVERIFIED | Fixed-viewport headless start/midcourse captures exist for all five tracks and show distinct palettes and venue props after manual review, but they precede the shared course-presentation renderer. Their clean diagnostics do not qualify the current bytes, current route presentation, or owner acceptance. Material richness, surface detail, concept-level prop/crowd density, and current five-track visual behavior remain unverified. |
| Five-track-by-mode campaign matrix | UNVERIFIED | Earlier isolated suites passed 3/3 and 18/18, including all 16 applicable combinations. The current engine changes have not been rerun. |
| Full cross-browser/device matrix | UNVERIFIED | Selected journeys historically passed in Firefox, WebKit, mobile-chrome, mobile-safari, and tablet-chrome, but the complete suite was not rerun across every project. Bundled engines/emulated viewports also do not qualify installed Safari/Firefox/Edge or physical devices. |
| Accessibility and responsive layouts | UNVERIFIED | Source now gives opacity-zero setting toggles a visible sibling focus ring, keeps Skip training at least 44×44 px, moves tutorial captions into a sticky in-card live region on narrow layouts, makes the short-phone pause dialog safe-area-aware and vertically scrollable, and uses rem endpoints so 140% UI scale affects critical narrow telemetry. Focused source assertions cover 320/621/780 px, 320×568, focus, target size, captions, and pause containment. Pre-final scripted headless Chrome captures completed at fixed 1672×941 and 390×844 viewports without captured runtime/request errors, but they predate these final bytes and did not exercise accessibility behavior, headed/native-device rendering, or physical touch. Historical focused accessibility passed 7 checks with 1 project skip; the new assertions were **NOT RUN**. Physical touch, all target projects, final-byte geometry, sticky-scroll behavior, and full manual accessibility review remain unverified. |
| Browser/runtime error gate | UNVERIFIED | The preceding headless installed-Chrome fixed-viewport capture sessions recorded no console errors, page errors, failed requests, or HTTP errors. Historical focused lifecycle, reliability, offline, race, editor, campaign, soak, Firefox/WebKit, and selected mobile/tablet scopes remain comparison evidence. The aggregate gate stays open because the captures preceded the final source/build, were not the paused test suite, and do not cover the final automated matrix, offline path, installed non-Chrome browsers, or physical devices. |

The previously accepted `rc.1` visual baselines and fidelity ledger do not close the new feedback. They remain historical evidence until the `rc.2` compositions receive fresh review; the retained screenshot deltas were not accepted by silently rewriting those baselines.

## 7. Performance, artifact, and source gates

| Gate | Severity | Status | Required closure evidence |
|---|---:|---:|---|
| Asset inventory consistency | P1 | UNVERIFIED | The latest permitted `npm run build` passed the current `assets:verify` contracts for generated GLB/KTX2/manifest/transcoder entries, the independent exact 3D manifest/directory contract, pinned shipped/source-title art and app-icon bytes, Canyon panorama bytes/PNG/provenance markers, Barlow Condensed WOFF2 bytes/signatures/hashes, and complete OFL bytes/markers. This remains build-time structural evidence rather than cryptographic C2PA proof, rendered-font/fallback proof, complete source/license/static audit, or human similarity/legal acceptance. |
| Final whole-project static and unit validation | P1 | UNVERIFIED | Current strict typecheck passed through `npm run build`. Current lint and Vitest were intentionally not run, so the combined gate remains unverified; earlier lint and 9 files / 78 tests are historical only. |
| Final whole-project browser validation | P1 | FAIL | Earlier isolated campaign qualification passed, but current browser testing is paused. The retained controlled visual run/repeat remain 1 pass / 3 obsolete-baseline failures, and the complete current cross-browser/device matrix is unverified. |
| Headed local desktop/emulated-mobile performance | P1 | UNVERIFIED | The retained headed artifact passed its earlier build, but the current camera/scenery/HUD work has not been remeasured. Physical-mobile performance remains unverified. |
| Continuous 30-minute Rival soak | P1 | UNVERIFIED | The retained 30-minute artifact passed its earlier build. Current simulation/tutorial/rendering changes have not received a new soak while testing is paused. |
| Dependency audit | P1 | UNVERIFIED | Earlier audit exited 0 with 0 vulnerabilities. The current lockfile and final installed tree require a fresh candidate audit. |
| Final non-QA artifact and production smoke | P1 | UNVERIFIED | The latest permitted normal build/notices are a current PASS only for build-time asset verification, strict typecheck, Vite compilation, and notices verification; they are not official manifest, immutable artifact, or smoke evidence. The format-2 guard sources its own detached clean install/build rather than hashing ambient `dist/`; production smoke now requires an internally consistent format-2 sidecar, binds the runtime Git commit and every served file/aggregate before Chrome, opens cached Practice offline, reverifies served bytes after the journey, waits for both post-countdown racing gates, and atomically promotes each run under its manifest hash without overwriting tracked history. Neither fixture nor real path ran. No current official sidecar/isolated bytes, `shell-v30` offline evidence, dependency audit, or installed-browser smoke exists. The retained format-1 hash `6a55c00ea36debe543ab946dee06dc5fa73cf8a13d45b047aec399469c9931a3` qualifies only the earlier build. Regenerate and qualify the complete evidence set together after the candidate freezes. |
| Immutable source identity | P1 | UNVERIFIED | The guard now requires the clean final commit and annotated `v1.0.0-rc.2` tag at `HEAD`, but neither final identity nor executed format-2 artifact evidence exists. |
| Deployable predecessor rollback archive | P1 | UNVERIFIED | `v1.0.0-rc.1` identifies historical source and reports, but no immutable artifact locator, retrieval record, byte verification, or staging rollback rehearsal is recorded. Supply and verify the actual predecessor package before deployment; do not infer rollback readiness from a tag. |

The retained earlier soak's `releaseGate.status` is `PASS`, but that artifact intentionally does not automate the required trend judgment. Heap used bytes were 14,761,964 first, 28,469,480 last, and 56,992,116 maximum; the first-to-last 36-sample window means increased by 14,407,287.22 bytes. Dispatch-delay mean/p95/max was 168.81/198.50/240.50 ms and its window mean fell 5.92 ms; keydown-to-frame mean/p95/max was 2.84/3.70/4.50 ms and its window mean fell 0.20 ms. The cumulative dropped-simulation counter deliberately sums each restarted race and ended at 706,442 ms; the per-attempt maximum was 9,231–14,091 ms and its first-to-last window mean fell 1,441.92 ms. Because this was a headless offscreen run, sampled FPS and cumulative dropped time are diagnostic stability evidence, not current-candidate, headed-performance, or physical-device qualification.

## 8. Unverified physical/application environments

| Environment | Status | Missing evidence |
|---|---:|---|
| Actual installed Firefox | UNVERIFIED | Separate current installed-application run |
| Actual Edge | UNVERIFIED | Current installed-application run |
| Actual Safari | UNVERIFIED | Current installed Safari run; Playwright WebKit is not equivalent |
| Physical Android Chrome | UNVERIFIED | Real touch/audio/storage/performance/thermal session |
| Physical iPhone/iPad Safari | UNVERIFIED | Real touch/audio/storage/performance/thermal session |
| Physical gamepad | UNVERIFIED | Controller model, prompts, stuck-input cleanup, and vibration where supported |
| Subjective audio, touch comfort, visual fidelity, and race fairness | UNVERIFIED | Human review on representative physical devices and inputs |

## 9. External owner actions

| Owner action | Status | Required evidence |
|---|---:|---|
| Final trademark, trade-dress, copyright, and generated-output review | EXTERNAL BLOCKER | Owner/counsel approval and archived provenance decision |
| Top-level product license | EXTERNAL BLOCKER | Owner-selected root `LICENSE` and final notice review |
| Generated-image account/agreement provenance | EXTERNAL BLOCKER | Governing account terms and prompt/session or receipt evidence |
| Neutral public workspace/repository name | PASS | The canonical workspace uses the neutral `Rivet Ridge Rally` directory name; the legacy prohibited-name folder has been retired. Fresh release scans must still reject stale absolute paths. |
| Production hosting, DNS/TLS, security headers, credentials, and deployment target | EXTERNAL BLOCKER | Owner-selected host, verified response headers, and tested immutable deploy/rollback procedure |
| Public support, release, rollback, and incident ownership | EXTERNAL BLOCKER | Named release/support/rollback/incident owners and public support path |
| Physical/application test-matrix access | EXTERNAL BLOCKER | Owner-provided devices, gamepad, installed browsers, and assigned human review |

## 10. Historical `v1.0.0-rc.1` evidence

The annotated `v1.0.0-rc.1` tag identifies the predecessor's immutable source, and historical reports record 67 Playwright passes with 0 failures, 76 Vitest passes, headed performance, a successful post-fix 30-minute Rival soak, a 21-file non-QA checksum manifest, and an installed-Chrome production smoke. Those records remain comparison evidence only for the predecessor's named bytes. Because no durable artifact locator plus retrieval/hash rehearsal is recorded, they do **not** establish a currently deployable rollback archive. The tag must not be moved, rebuilt, or relabeled as `rc.2` evidence.

## 11. QA conclusion

The retained evidence supports the earlier `rc.2` snapshot in its named scopes, including arrow-cluster defaults, migrations, campaign coverage, performance, soak, manifest, and Chrome smoke. The latest permitted normal build ran from 10:26:16 to 10:26:21 CEST and passes exact asset/title/icon/panorama/font/license verification, strict typecheck, the 142-module Vite production compilation, and 43,825-byte notices verification. It produces 24 files / 6,679,904 raw bytes with `shell-v30`; `dist/sw.js` is 5,722 bytes with SHA-256 `f5cc9b23c2d507b9cede7a11cd72f62d05549c4ae72888853b5191d4cb6b78ca`. It embeds commit `62664b1f8ba814f3ec685d228fa4e600e6e7dc84` with `dirty=true`, so it is development/build evidence only. In addition to the previously recorded tutorial, input, persistence, lifecycle, and visual source, this build contains schema-v2 checkpoint-authored centerlines shared by Builder and Test Ride, exact identity for omitted anchors and migrated v1 tracks, strict full rotated parent-footprint Start/Finish containment, legacy repair access without destructive quarantine, the bounded full-range editor continuation and orbit-safe fit, touch-aware prompt fallback, pause-key focus handling, and numbered/articulated rivals. The working source at build time also contained pre-write production-smoke symlink containment, but that Node smoke path was not exercised by the normal build. Earlier scripted captures remain comparison evidence only; they predate this work and are not bound to the current bytes. They do not substitute for the paused lint/unit/migration/browser suites, runtime/visual/accessibility qualification, performance/soak, official manifest/`shell-v30` offline smoke, audit, or owner visual/legal acceptance.

The current 1,418-byte `dist/index.html` has SHA-256 `4252cb572300d9e2dd838b7e39f8440b8b40b6762556e17cd9f3d7b44690f433`. The latest build additionally compiles positive campaign grade, graded broad ground and surface framing, shared multi-section obstacle contacts and distinct silhouettes, normalized Bump Row penalties, Offset Barrier AI planning, expanded festival/Pine/Foundry/Summit density and signatures, the shared schema-v2 Builder/Test Ride authored route, and the final editor/input corrections. The working source also contains the release-safety corrections, whose separate Node paths were not executed. This is build/source evidence only; runtime appearance and behavior remain **UNVERIFIED**.

The preceding 04:53, 05:53, 06:55, 07:54, 08:57, and 10:09 builds are historical scoped evidence only. The permitted 10:26 build is the latest normal build and includes the current source corrections and `shell-v30`, but its embedded `dirty=true` marker prevents immutable-release qualification; every current-candidate manifest/offline/smoke closure must target one clean, exact shell-v30 artifact.

The latest permitted build compiles active-scoped ordered tutorial clearance with an input-preserving 550 ms handoff, the critical-heat latch, repeated-pause guard, mapped-threshold device arbitration, held-touch precedence, touch-release keyboard restoration, service-worker-controller invalidation, `shell-v30` generation, Canyon-only two/three/four-row grandstand tiers, and staffed elevated cooling-gate watchtowers. It also compiles the safe 62 normal-Ride heat ceiling, pairwise AI contact policy, numbered start treatment, shared portrait predicate, 320–780 px / 140% control bounds, semantic touch/Recovery states, and fail-closed fresh-reload service-worker install. The working source separately contains format-2-manifest-bound production smoke, but that Node path was not part of the client compilation. Focused unit, E2E, smoke-support, install, browser, offline, input, accessibility, and visual checks were **NOT RUN**; no source-only item is promoted to `PASS`.

An earlier source-only audit found and the permitted 05:53 build compiled four additional P1 closures: per-tick race-world coupling, conflict-safe legacy arrow migration, authored-Test-Ride panorama isolation, and complete focal-bike fallback geometry. It also compiled the deterministic licensed display font, `shell-v30` font precache, and non-overwriting atomic smoke bundle path. These are build/source facts only; every behavioral and visual acceptance gate for them remains **UNVERIFIED**.

The earlier three-way static audit found no P0 and identified 16 unique P1 defects across gameplay/results, editor/persistence, accessibility/input, and responsive presentation. All 16 have production-source corrections and focused regression source: accurate initial countdown HUD values, competitive-only podium copy, typed wheelie/crash feedback and consecutive-crash counting; selectable long-route editor placements and non-color preview validity; bounded blocked database opens, future-version-safe full-record validation, atomic corrupt-track preservation/export, strict bounded import preflight; and short-phone pause/focus/target/caption/UI-scale corrections. The 05:53 build proved that corrective slice typechecks and compiles, not that its behaviors pass.

The 2026-07-15 follow-up audits also found no P0 and corrected held-touch precedence, selective atomic profile recovery, current-snapshot cross-tab track recovery, transactional replay pruning, the shared 500-module boundary, exact pending Test Ride retry, and budget-before-materialization recovery encoding. The latest permitted build proves those sources typecheck, compile, and package with the asset/notices contracts. It also compiles shared multi-section module contacts and distinct silhouettes, positive campaign grade, expanded venue density/signatures, schema-v2 checkpoint-authored Builder/Test Ride routes, strict full rotated parent-footprint containment, exact identity for omitted/migrated anchors, and non-destructive legacy repair access. Their unit, migration, component, browser, persistence-failure, cross-device, visual, performance, and manual gates remain **UNVERIFIED** while the requested pause continues.

A later source-only tutorial/input audit found five focused gaps and now has corrective source: Rider School Settings from both the fresh intro and training pause preserves one mounted tutorial session and returns focus to its originating surface; recovery instructions no longer produce `Hold hold`; remap buttons expose action-specific accessible names and reject unreliable menu/browser/system keys with explicit errors; gameplay default suppression follows every effective mapped action rather than a fixed arrow/Space list; and Gamepad Start claims prompt priority through `InputManager`, with disconnect returning the device state to the remembered non-gamepad device, Keyboard or Touch. Focused source coverage was added in `src/game/input/__tests__/InputManager.test.ts`, `src/game/input/__tests__/keyLabels.test.ts`, `src/ui/game/__tests__/tutorialProgress.test.ts`, `e2e/accessibility-controls.spec.ts`, `e2e/tutorial.spec.ts`, and `e2e/gamepad.spec.ts`. The final permitted normal build typechecked and compiled this slice. Per the active QA pause, none of those unit or Playwright checks—and no browser session, screenshot, performance run, audit, manifest, or smoke—was executed. This slice remains **UNVERIFIED**.

A subsequent source-only concept-fidelity slice replaced the rival two-bar placeholders with distinct real two-digit numbers and authored palette pairs, extended the fallback tread/brake/panel/exhaust treatment to every rival, and replaced whole-group-only rider posing with a bounded six-pivot render-only rig. It did not modify simulation, AI, collision, input, timing, classification, replay, or Builder source. The final permitted normal build typechecked and compiled this slice, but no runtime qualification followed; number rendering, joint attachment, Reduced Motion, draw calls, performance, and owner fidelity acceptance remain **UNVERIFIED**.

The code-owned QA status is **UNVERIFIED**. Full tutorial integrity, current lint/unit/browser checks, concept-art acceptance, the complete cross-browser and physical-device matrix, manual/physical input and accessibility review, current performance/soak, current manifest/offline smoke, and immutable `rc.2` source identity remain **UNVERIFIED**. The obsolete visual-baseline gate remains **FAIL** until an accepted composition is captured and rerun. The release decision is **NOT READY**.
