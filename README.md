# RIVET RIDGE RALLY

RIVET RIDGE RALLY is an original 3D arcade motocross game for desktop and mobile browsers. It ships five festival tracks, Solo and Rival campaign progression, Practice and Summit mastery, a fixed-60 Hz arcade simulation, keyboard/gamepad/touch controls, local persistence, accessibility options, procedural audio, and a local 3D track builder.

The current working candidate is **1.0.0-rc.2**. The annotated `v1.0.0-rc.1` predecessor and its evidence remain immutable historical records; retained performance, soak, manifest, dependency-audit, and full-browser results qualify an earlier `rc.2` snapshot only. On macOS 26.5.2 arm64 with Node v26.4.0 and npm 11.17.0, the permitted `npm run build` ran from 2026-07-15 10:26:16 to 10:26:21 CEST. Exact asset, title, icon, panorama, font, and license verification passed; strict typecheck and the 142-module Vite production compilation passed; the 43,825-byte notices have SHA-256 `5fa8f7b4aaf95b4cc28b9b36a10c7f451e0ef83e6ac959d530ed3fdceacb60d0`. The build produced 24 files / 6,679,904 raw bytes with `shell-v30`; `dist/index.html` is 1,418 bytes with SHA-256 `4252cb572300d9e2dd838b7e39f8440b8b40b6762556e17cd9f3d7b44690f433`, and `dist/sw.js` is 5,722 bytes with SHA-256 `f5cc9b23c2d507b9cede7a11cd72f62d05549c4ae72888853b5191d4cb6b78ca`. The 282,888-byte game chunk is `GameView-Cy_z8oq_.js`, SHA-256 `3517031f108f19e570938c10d31fb6e58af56ecb8859ea25a09355b6ef575bd4`; the 612,073-byte shared route chunk is `CoursePresentationRoute-CMRHtjX1.js`, SHA-256 `87e856adf652bb8a49b45ed3d96256221a360ca7edff9a94341a17fa63af71b8`; and the 56,935-byte editor chunk is `TrackEditorScreen-niBUL8yv.js`, SHA-256 `6c800124e378c8e16dc420210b1e696e3418d4843c537ebab3218e3797b1f5f0`. The build embeds commit `62664b1f8ba814f3ec685d228fa4e600e6e7dc84` with `dirty=true`, so it is development/build evidence rather than an immutable release artifact.

The current working source postdates that historical build. It contains the original Blender Canyon kit, shared five-variant Rival/Mastery pack, and a revised original hero bike/rider guided by the project-generated canonical, mounted-pose v2, and action-state v3 references. The isolated hero pipeline reduces a 2,379,208-byte raw GLB to a 517,664-byte required-Meshopt runtime GLB while preserving 49,780 triangles: 39,912 bike triangles (including 14,284 across both wheels) and 9,868 rider triangles, plus 88 nodes, 28 render primitives, ten opaque PBR materials, two distinct wheel hooks, and six pose pivots. The 15,165-byte schema-v2 asset manifest binds source-pair UUID `31000dd4-2c42-4c74-ad5d-28b5e4aec32f` and runtime SHA-256 `538be4269927544fa91cfc405742523a60422d77d546ce9c8859f407deb2e6e8`; its current manifest SHA-256 is `7622e9ceeabc7db2861049cb12c969e6e73e9149799b9ab4018cf11c18cb4b2a`. `npm run assets:verify`, lint, typecheck, and the current 450 deterministic tests/fixtures pass on the working tree.

Current browser evidence passes the authored hero, hard-failure, invalid-contract, timeout-fallback, late-ready, articulation, Reduced Motion, and crash-precedence paths. The 15-frame manifest at `artifacts/visual-review/hero-motion-action-states-v3-headed-20260717t095050z/manifest.json` passes its scoped dirty-tree capture contract through public keyboard/touch input, with the authored Canyon kit and panorama ready and browser audio muted. The retained non-QA production journey on port 4173 still reaches Canyon Practice with version `1.0.0-rc.2`, no QA API, no browser/network errors, and the exact authored hero rather than fallback, but its served bytes predate this latest source pass. A headed Apple M1 Max diagnostic completes desktop High and emulated-mobile Low normal/stress paths at approximately 60 FPS with stable served bytes and no harness errors. These are working-tree diagnostics, not immutable release evidence. The hero still has materially blocky anatomy, flat solid-color materials, simplified mechanics, rear-camera occlusion, and weak landing/crash/recovery readability relative to the concept. Physical-device review, owner visual acceptance, and legal acceptance remain open. The candidate remains **NOT READY / VISUAL FIDELITY NOT ACCEPTED / OWNER UNVERIFIED**.

A security/PWA review also found and corrected cache-boundary defects: rejected, cross-origin-final, and 5xx navigations now fall back to the last complete shell without masking genuine same-origin 4xx responses; runtime writes validate both the requested and final static URL instead of only the requested origin. `shell-v35` limits each warming request to 128 query-free static-shell/`/assets/` URLs and the complete current cache to 192 entries, leaves arbitrary same-origin API paths untouched, rejects `no-store`/`Vary: *` responses, and uses variance-insensitive matching only behind that strict static allowlist so ordinary CDN `Vary` headers do not break offline installation. Controller changes and later reconnects request a guarded preparation pass, including one remembered rerun when preparation is already active. The 34 production-smoke/service-worker/release-scope fixtures pass; exact-product installed/offline smoke remains unrun because there is no clean tagged format-2 candidate.

The same review found local-data P1s and corrected them without changing IndexedDB or portable track schema versions. Settings/progress writes are serialized per tab, transactionally merge base→next leaves—including disjoint key-remap actions—into the latest rows, and reconcile the actual merged database result without overwriting a newer local mutation. Progress gains remain monotonic, best-time splits stay bound to the winning time, explicit resets retain ordering, and mutations made during a slow recovery retry are rebased before persistent mode can return. Track saves compare an exact persisted base and create a named UUID conflict copy instead of overwriting a newer edit; confirmed deletion refuses a changed stored record. Save, Test Ride, and destructive library operations make the editor inert while their exact snapshot is pending, and identity-changing Open/import/conflict-copy transitions reset document history. A failed Test Ride retains both its snapshot and exact base for retry. Focused unit and browser persistence/editor paths pass on the working tree; true multi-tab stress and clean-candidate repetition remain **UNVERIFIED**.

The visual evidence workflow can build a separate clean-commit QA candidate, re-inventory and serve its exact bytes from a dedicated loopback origin, and capture an exact 11-frame Practice/Rival/Canyon matrix with observed runtime state and Chromium response-body binding. Snapshot mutation is disabled. The current clean-source technical capture for commit `13c01173d991c1b18b32c32f1032d10b075a4c34` passed 11/11 with manifest SHA-256 `3506b9aa90d02d50329f80a2f783f1da99a7c40a81f7ebc67166670ef8f2fa9c`; this is not owner acceptance or a promoted baseline. The missing curved-Canyon baseline can be created only by the guarded external owner-approval workflow documented in `docs/OPERATIONS.md`; no acceptance record exists and no baseline was created, promoted, or waived.

That historical build includes the comprehensive Rider School and Arrow-cluster input work; touch-aware gamepad-disconnect prompts and pause-key focus handling; five fixed rival palettes with distinct two-digit identities and shared detailed/articulated bike-rider rigs; the shared graded campaign presentation route and broad graded ground; exact multi-section obstacle contacts shared by player and AI; distinct Tabletop, Double Jump, Jump Chain, Trail Bump, Bump Row, Offset Barrier, and Sky Kicker silhouettes; normalized Bump Row penalties; Offset Barrier lane planning; and the tier-safe festival-density and Pine/Foundry/Summit signature pass. It also includes schema-v2 authored routes with checkpoint-only Route turn/rise anchors shared by Builder and Test Ride, exact identity mapping for omitted anchors and migrated v1 routes, strict full rotated parent-footprint containment between Start and Finish, and legacy-track repair access without destructive quarantine. Track Builder now uses a route-sized floating island, separate local/overview zoom, a module-inclusive orbit-safe **Fit route**, adaptive raycaster-only pick geometry, and a bounded visible/pickable continuation across the accepted 0–20,000 m navigation range. Current lint, unit/fixture, type, asset, focused browser, production-smoke diagnostic, and headed-performance checks pass in their stated dirty-working-tree scopes. Current runtime behavior is technically exercised, but concept fidelity remains **NOT ACCEPTED / OWNER UNVERIFIED**, and the candidate remains **NOT READY**. The authoritative status lives in `QA_REPORT.md` and `LAUNCH_READINESS.md`.

Source inventory in the latest permitted build also includes these unqualified safeguards: normal Ride heat approaches the safe 62 ceiling while Turbo can exceed it; every unfinished AI pair receives grounded same-lane rear-impact resolution after each live and classification fixed step; campaign/Rider School get course-anchored presentation-only lane numerals `1–4` plus a transverse start treatment while authored Test Rides are excluded; one pure viewport predicate selects portrait framing for portrait orientation or the retained sub-680 px fallback; and the narrow layout bounds normal/mirrored touch controls and Heat at 320–780 px / 140% UI scale while retaining 44 px minimum targets. Native semantic touch buttons expose hold state, lane pulses, and an adaptive Recovery progressbar. Production-smoke source now binds the runtime commit and a valid format-2 manifest, verifies every served file and aggregate before and after the Chrome journey, and opens cached Practice offline; `shell-v35` installation fetches fresh same-origin bytes and deletes a partial current cache on failure. At that earlier build checkpoint these paths were unrun; the current working-tree unit, browser, smoke-support, and service-worker fixture results are summarized above, while exact-product offline and release qualification remain open.

Current working source provisionally recalibrates production Solo/par targets to Canyon 190/181 s, Pine 208/198 s, Coastline 224/214 s, Foundry 239/228 s, and Summit 259/247 s. Summit Mastery uses seven 257/255/253/251/249/248/247 s targets while starting heat rises from 35% to 65%; Rookie/Rider/Ace retain their existing profile settings. Source-level all-tier progression, full-field classification, and deterministic representative target completions have focused coverage, and the isolated campaign matrix passes 18/18. Physical/manual fairness review and frozen-candidate repetition remain unverified, so retained earlier campaign results still do not qualify the current candidate for release.

The same working source adds a canvas-described Track Builder path for keyboard placement using a labeled lane selector and the current Route view, revalidates keyboard and pointer candidates before commit, keeps status feedback and complete authoring/recovery controls available in narrow layouts, and adds browser coverage for those paths. It also adds a keyboard-focusable title link to a combined Support / Privacy / Accessibility / About screen with candidate build identity and accurate code-owned local-data disclosures. Current focused editor, keyboard, narrow-layout, and automated-accessibility paths pass. Verified public contact, operator/data-controller identity, host/request-log policy, retention/deletion commitments, product license, manual screen-reader review, and frozen-candidate acceptance remain open.

The same build also contains an active-lesson tutorial gate: earlier evidence cannot pre-clear later lessons, multi-part requirements are ordered, coast/mud/grass require measured slowdown, and each completed lesson freezes atomically for a 550 ms handoff without discarding held Ride or Turbo input. Critical heat latches within its active lesson so the player can release Turbo safely. Every incomplete riding lesson exposes **Retry this lesson**, which resets the current simulation and active evidence while preserving earlier lesson markers and cumulative recap credit. Keyboard capture and the global pause shortcut ignore interactive controls, so native button activation remains available even when Space is remapped. The portrait touch cluster now uses six original inline rally pictograms while retaining visible Ride/Turbo text, stable accessible names, hit areas, safe-area placement, and mirroring. It also contains one-transition-per-press keyboard pause handling, mapped-threshold gamepad arbitration that cannot be seized by neutral drift, held-key recovery after touch release, and immediate offline-readiness invalidation whenever the controlling service worker changes. Current scoped tutorial, input, touch-emulation, and visual-diagnostic paths now run and pass as recorded above; physical touch ergonomics, real service-worker transitions, accepted visuals, and frozen-candidate qualification remain **UNVERIFIED**.

A final source-only tutorial-path review moved the canonical cooling gate to 241–259 m and the training bump to 295–305 m. Fixed-step analysis places the required 78% heat warning near 186.7 m after Ride, coast, and lane-change lead-in, leaving about 54 m of dirt. The production tutorial and regression now share one typed seven-obstacle sequence; the regression uses Turbo alone, proves coast slowdown, and validates every adjacent interval. Across split resumed runs, all five focused scenarios have passing evidence, including the comprehensive earned journey below three in-game minutes; a single frozen-candidate invocation remains open.

An earlier gameplay completion audit found no remaining source-fixable P0/P1 in its gameplay/input/tutorial scope and source-corrected live **Retro recovery tapping** propagation. A later launch-boundary review superseded its persistence/offline conclusion: source now uses exact-base custom-track writes and deletes, conflict copies, serialized profile writes, durable single-flight retry reconciliation, and editor storage-operation locks so delayed writes cannot erase newer drafts or the mounted attempt. Independent final static re-reviews reported no remaining P0/P1/P2 in those settled paths. Current corrective fixtures and focused browser paths pass in their stated dirty-working-tree scopes; true concurrent-tab, exact-product offline, and frozen-candidate qualification remain open.

To close another direct concept-fidelity gap, handcrafted Canyon/Rider School festival pockets now reuse their existing five instanced batches for outward-rising timber grandstands and staffed elevated cooling-gate watchtowers. Low/Medium/High use two/three/four tier rows; each watchtower raises the existing deck/canopy/four posts, uses a mirrored teal/coral roof, and moves two/three/four existing spectators onto the deck. A 5.6 m safety rule rejects any route pocket that approaches a showcase; the current production-High placements already clear it, so all 22 route pockets remain alongside four watchtowers for 26 pockets / 104 tiers. Non-Canyon and editor-authored routes receive zero Canyon-specific watchtowers/tier pockets; separate per-theme density rules remain active. Current structural, runtime, desktop/portrait capture, and performance diagnostics pass in their stated dirty-working-tree scopes, but owner concept/legal acceptance, physical-device review, and frozen-candidate evidence remain open; visual fidelity is still **NOT ACCEPTED**.

Release-provenance hardening is now present in source but remains **UNVERIFIED** as a real candidate gate. Node `26.4.0` and npm `11.17.0` are pinned by `.node-version` and `packageManager`. The format-2 guard captures clean `HEAD`, creates a detached checkout of that commit, then reads and revalidates the exact package, lockfile, and Node pin only from that checkout before accepting annotated `v1.0.0-rc.2`. It captures the annotated tag object's identity/type and requires that exact object to remain unchanged through the build. It resolves the absolute npm CLI to its physical `npm` package, requires the pinned package metadata and canonical `bin/npm-cli.js`, and binds the complete package tree with deterministic relative-path directory/file/symlink records, counts, byte totals, and SHA-256 without recording a local installation path. Absolute, dangling, external, and special npm-package entries fail closed; the CLI identity and whole tree are revalidated after version, install, build, sidecar creation, and final worktree cleanup. Empty isolated user/global npm configs keep install scripts explicitly enabled while `npm ci --no-audit --no-fund` and sanitized non-QA `npm run build` run. Phase checks reject every ignored checkout input except generated `node_modules` after install and `node_modules`/`dist` after build; source inputs and both checkouts are revalidated. Only regular distribution files may replace root `dist/`, and every manifest record now binds raw bytes/SHA plus deterministic gzip level-9 bytes/SHA under the pinned Node toolchain. Recursive guarded cleanup removes both root `dist/` and even a malformed sidecar directory on failure, while temporary worktree state is always pruned. The ignored canonical manifest cannot dirty tagged source; the earlier format-1 record remains at `artifacts/history/release-manifest-1.0.0-rc.2-earlier-snapshot-format-1.json`. The 44 hostile-config/fake-npm-package and provenance fixtures pass, but no clean annotated candidate, real format-2 guard run, matching manifest, or installed/offline production smoke exists.

The current release-hardening source remains **UNVERIFIED** as an exact-product release. Schema-4 performance measurement targets the exact non-QA product bytes, drives public UI after a validated ephemeral progress setup, rejects shipped QA/performance APIs, and retains bounded raw per-frame `requestAnimationFrame` callback-work samples for independently recomputed five-second mean-FPS and p95 gates. Schema-4 Rival soak uses the same product path, accumulates only visible `data-race-gate-phase="racing"` time, and independently recomputes memory/input/fixed-step summaries and trends. The callback-work metric covers synchronous rAF callback CPU time, not style/layout, paint, GPU, workers, or microtasks. Schema-5 production smoke binds installed Google Chrome/channel/version/viewport, runtime title/version/commit, `shell-v35`, complete served inventories, offline Practice, and four integrity-checked screenshots. Schema-v3 final attestation binds these completed runs plus the mandatory QA, readiness, approval, and rollback records. Current release-manifest, release-attestation, production-smoke, and service-worker fixtures pass, and the one-second headed working-tree diagnostic meets its numeric budgets but remains overall `FAIL` for dirty source/runtime binding. No clean candidate, complete exact-product QA record, predecessor archive/data-safety proof, approval, or attestation exists.

An earlier corrective slice stores the standing Solo personal-best lap and checkpoint arrays, replaces them only with a strictly faster final time, and shows current timings beside signed prior-PB comparisons. Rival-pair contact now uses a symmetric forgiveness distance after every live and post-finish classification fixed step. At 140% UI scale, pixel-bounded HUD/control rules cover 320–780 px and Rider School retains a touch-scrollable action area on a 320×568 short phone. Release tooling now records and repeatedly revalidates the resolved child Node executable hash, requires a credential-free dedicated root, proves `/` matches manifest `index.html`, rejects origin drift, and derives both service-worker index keys from one fresh response. Current focused behavior/browser and release-fixture paths pass; installed exact-product, physical-device, and frozen-candidate evidence remain **UNVERIFIED**.

The latest permitted build additionally compiles per-fixed-tick environment/player/AI/contact coupling, a conflict-safe native-v3 lane-binding migration, explicit panorama exclusion for editor-authored Test Rides, and a concept-complete built-in bike fallback. It also verifies and ships Barlow Condensed 700/900 Latin WOFF2 subsets and their complete SIL OFL 1.1 text, using the same family for DOM and CanvasTexture display labels and precaching both files in `shell-v30`. Current focused regressions, fallback capture, visual diagnostics, and scoped production-preview smoke have run; exact-product offline transition, accepted font/concept rendering, and frozen-candidate smoke remain open.

A final source-only gameplay, accessibility, editor, and persistence audit found no P0 defects and identified 16 P1 defects. Source now addresses the initial HUD snapshot, truthful results language, precise crash feedback and repeat-crash telemetry; focus visibility, short-phone pause/tutorial geometry, scalable narrow telemetry, and caption placement; existing-module selection, full-range editor navigation, pointer-derived placement validity; bounded import parsing, explicit blocked-database recovery, strict persisted-record validation, atomic corrupt-track quarantine, and user-downloadable recovery records. Current focused unit and browser paths pass in their stated working-tree scopes. Physical-device, manual accessibility, concurrent-tab, and frozen-candidate evidence remain **UNVERIFIED**, so the release decision stays **NOT READY**.

A later source-only follow-up also found no P0 and corrected seven additional data/input boundaries: held touch now precedes an active controller; settings/progress recovery is selective and atomic; corrupt-track recovery rechecks the current cross-tab snapshot; replay insertion/pruning is atomic; editor creation, duplication, validation, persistence, import, and Test Ride share a 500-module ceiling; a failed Test Ride save retains and retries the exact snapshot; and recovery export applies depth/node/string/output budgets before reading binary payloads while unmeasurable aggregate records fail closed. The same build source-addresses campaign grade, jump/module form, festival density, Pine/Foundry/Summit venue signatures, the shared schema-v2 winding/graded Builder/Test Ride centerline, and strict full rotated parent-footprint boundary validation. Legacy v1 tracks that need the stricter repair remain readable/editable and are not quarantined, but cannot be saved or Test Ridden until repaired. Current migration, unit, browser, campaign, and scoped visual/behavior paths pass; true concurrent-tab, physical-device, accepted visual, and frozen-candidate evidence remain **UNVERIFIED**.

## Run and verify

Use Node `26.4.0` from `.node-version`, npm `11.17.0` from `packageManager`, and the committed npm lockfile for release work.

The list below is the command interface, not a safe release order. For a release candidate, first track every intended source/asset/tooling/provenance input and create the clean, untagged visual-review commit. Run the initial checks, capture that exact review candidate, obtain the external owner decision, and use the guarded two-file visual promotion described in `docs/OPERATIONS.md`. Commit only those two promotion outputs. From the resulting clean final product commit, run the complete pre-tag asset, typecheck, lint, unit, coverage, browser, dependency-audit, and normal-build gates, then create annotated `v1.0.0-rc.2` at that exact `HEAD`. Only then may the guard generate the manifest-bound bytes. Run performance, soak, and production smoke against those exact post-tag bytes; those evidence formats require the annotated tag and cannot qualify an untagged build. Publish the immutable product and attestation tags only after every gate and approval passes; never move or rebuild a published tag. See `docs/OPERATIONS.md` for the exact sequence. `npm run test` includes Vitest and the Node release fixtures, while `npm run test:coverage` reports Vitest coverage only.

```sh
npm ci
npm run dev

npm run typecheck
npm run lint
npm run test
npm run test:coverage
npm run test:e2e
npm run assets:verify
npm run audit

npm run build
npm run release:manifest
npm run preview
npm run smoke:production
```

Use `npm run assets:build` only when intentionally regenerating the checked-in hero bike-and-rider, shared rival-pack, and Canyon GLB pipelines, then rerun `npm run assets:verify`. The hero step consumes the versioned Blender source/raw export and writes `/assets/3d/hero-bike-rider.glb` plus its manifest; it does not regenerate the modeling reference or editable source. The verification command enforces all three exact 3D source/runtime contracts plus the Canyon panorama, shipped/retained-source title artwork, app icon, bundled fonts, and font-license contracts without regenerating those production-art inputs. The isolated hero, rival, and Canyon optimizers completed, including their internal round-trip checks. Their independent verifiers and the full `npm run assets:verify` command passed the current working-tree bytes on 2026-07-16 under Node v26.4.0/npm 11.17.0; this does not qualify runtime loading, visual fidelity, performance, legal acceptance, or an immutable release candidate. The headed measurement and 30-minute Rival soak commands are documented in `docs/OPERATIONS.md`; their JSON and screenshot evidence belongs under `artifacts/performance/`.

`npm run build` produces a development-review production site in `dist/` and writes its complete `THIRD_PARTY_NOTICES.txt`. From a clean exact-tag checkout under the pinned toolchain, `npm run release:manifest` does not trust those ambient bytes: it performs its own isolated clean install and non-QA build, replaces root `dist/`, rejects provenance mismatches, non-regular entries, source maps, missing notices, QA markers, and local paths, then writes the ignored format-2 sidecar with raw and deterministic gzip level-9 size/SHA records. With those exact generated bytes running on the documented preview port, `npm run smoke:production` uses installed Google Chrome to verify the exact browser/runtime/cache identity, boot/version, race restart, editor open, service-worker-controlled offline reload, cached Practice, and complete pre/post served inventories. Deploy `dist/` as one immutable unit at the root of a dedicated origin; do not deploy the Vite development server or mount the app below a URL prefix. The browser tests build and serve their own QA-mode candidate. Performance and attestation evidence contracts are documented in `docs/OPERATIONS.md` and `docs/RELEASE_ATTESTATION.md`.

## Product scope

- Five two-lap launch tracks: Canyon Kickoff, Pine Run, Coastline Clash, Foundry Flight, and Summit Showdown.
- Solo Challenge, Rival Main Race, Practice, campaign unlocks, and seven-tier Summit mastery.
- Four-lane racing with throttle, a safe 62 normal-Ride heat ceiling, turbo heat, cooling, wheelies, airborne pitch, landings, terrain, hazards, crashes, and asymmetric player/AI plus pairwise AI rider collisions.
- Route-following and pursuing AI with Easy, Standard, and Expert decision profiles.
- Interactive first-play tutorial, immediate retry/restart, results/coaching, and persistent personal bests.
- Keyboard controls with arrow-cluster lane defaults, full remapping, standard gamepad support, vibration where available, touch controls, and mirrored touch layout.
- Local 3D editor with 25 modules, 50-action undo/redo, validation, 1–9 laps, examples, safe JSON interchange, library persistence, thumbnails, and test play.
- Reduced motion/shake, high contrast, colorblind-safe signals, captions, UI scale, and separate master/music/SFX volume controls.
- No account, payments, ads, tracking, public sharing, or server dependency.

## Architecture

The game uses strict TypeScript, Vite, React for application UI, direct Three.js/WebGLRenderer for 3D presentation, Zustand for app flow, a custom fixed-step simulation, IndexedDB/Dexie for versioned local data, and Web Audio for original procedural sound. Gameplay and rendering stay outside React's render loop. Nonessential race/editor code is lazy loaded; the current authored player-bike-and-rider GLB uses Meshopt geometry, 10 opaque solid-color PBR materials, and zero textures; handcrafted Canyon/Rider School independently load an authored Meshopt modular kit with complete procedural fallback; a quality-sized PMREM supplies bounded PBR fill; and the Canyon/tutorial matte uses a bounded ImageBitmap-backed 2D background path with a procedural fallback.

## Documentation

- [GAME_BIBLE.md](GAME_BIBLE.md) — creative direction and player experience.
- [GAME_SPEC.md](GAME_SPEC.md) — normative behavior and acceptance criteria.
- [ARCHITECTURE.md](ARCHITECTURE.md) — runtime boundaries and data flow.
- [ASSET_LICENSES.md](ASSET_LICENSES.md) — source, rights, hashes, and bundle inventory.
- [QA_REPORT.md](QA_REPORT.md) — executed test and performance evidence.
- [LAUNCH_READINESS.md](LAUNCH_READINESS.md) — release-gate decision and remaining actions.
- [AGENTS.md](AGENTS.md) — repository contribution rules.
- [docs/OPERATIONS.md](docs/OPERATIONS.md) — build, deploy, cache, support, backup, and rollback runbook.
- [docs/ASSET_PIPELINE.md](docs/ASSET_PIPELINE.md) — reproducible GLB/KTX2 pipeline.
- [docs/design/PRODUCTION_ART_PROVENANCE.md](docs/design/PRODUCTION_ART_PROVENANCE.md) — Canyon panorama generation brief, source review, runtime constraints, and open human-review gates.
- [docs/RIVET_RIDGE_RALLY_DESIGN_REPORT.docx](docs/RIVET_RIDGE_RALLY_DESIGN_REPORT.docx) — template-based design review, current evidence scope, recommendations, and **NOT READY** decision.

## Identity and legal hygiene

Only **RIVET RIDGE RALLY** is approved shipped branding. The local workspace was renamed to a neutral product-title directory on 2026-07-14. Keep public CI logs, support exports, and source maps free of legacy third-party working names and absolute local paths. The owner must also select the repository's top-level product license and complete final legal, trademark, and trade-dress review.
