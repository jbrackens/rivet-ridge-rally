# RIVET RIDGE RALLY

RIVET RIDGE RALLY is an original 3D arcade motocross game for desktop and mobile browsers. It ships five festival tracks, Solo and Rival campaign progression, Practice and Summit mastery, a fixed-60 Hz arcade simulation, keyboard/gamepad/touch controls, local persistence, accessibility options, procedural audio, and a local 3D track builder.

The current working candidate is **1.0.0-rc.2**. The annotated `v1.0.0-rc.1` predecessor and its evidence remain immutable historical records; retained performance, soak, manifest, dependency-audit, and full-browser results qualify an earlier `rc.2` snapshot only. On macOS 26.5.2 arm64 with Node v26.4.0 and npm 11.17.0, the permitted `npm run build` ran from 2026-07-15 10:26:16 to 10:26:21 CEST. Exact asset, title, icon, panorama, font, and license verification passed; strict typecheck and the 142-module Vite production compilation passed; the 43,825-byte notices have SHA-256 `5fa8f7b4aaf95b4cc28b9b36a10c7f451e0ef83e6ac959d530ed3fdceacb60d0`. The build produced 24 files / 6,679,904 raw bytes with `shell-v30`; `dist/index.html` is 1,418 bytes with SHA-256 `4252cb572300d9e2dd838b7e39f8440b8b40b6762556e17cd9f3d7b44690f433`, and `dist/sw.js` is 5,722 bytes with SHA-256 `f5cc9b23c2d507b9cede7a11cd72f62d05549c4ae72888853b5191d4cb6b78ca`. The 282,888-byte game chunk is `GameView-Cy_z8oq_.js`, SHA-256 `3517031f108f19e570938c10d31fb6e58af56ecb8859ea25a09355b6ef575bd4`; the 612,073-byte shared route chunk is `CoursePresentationRoute-CMRHtjX1.js`, SHA-256 `87e856adf652bb8a49b45ed3d96256221a360ca7edff9a94341a17fa63af71b8`; and the 56,935-byte editor chunk is `TrackEditorScreen-niBUL8yv.js`, SHA-256 `6c800124e378c8e16dc420210b1e696e3418d4843c537ebab3218e3797b1f5f0`. The build embeds commit `62664b1f8ba814f3ec685d228fa4e600e6e7dc84` with `dirty=true`, so it is development/build evidence rather than an immutable release artifact.

That build includes the comprehensive Rider School and Arrow-cluster input work; touch-aware gamepad-disconnect prompts and pause-key focus handling; five fixed rival palettes with distinct two-digit identities and shared detailed/articulated bike-rider rigs; the shared graded campaign presentation route and broad graded ground; exact multi-section obstacle contacts shared by player and AI; distinct Tabletop, Double Jump, Jump Chain, Trail Bump, Bump Row, Offset Barrier, and Sky Kicker silhouettes; normalized Bump Row penalties; Offset Barrier lane planning; and the tier-safe festival-density and Pine/Foundry/Summit signature pass. It also includes schema-v2 authored routes with checkpoint-only Route turn/rise anchors shared by Builder and Test Ride, exact identity mapping for omitted anchors and migrated v1 routes, strict full rotated parent-footprint containment between Start and Finish, and legacy-track repair access without destructive quarantine. Track Builder now uses a route-sized floating island, separate local/overview zoom, a module-inclusive orbit-safe **Fit route**, adaptive raycaster-only pick geometry, and a bounded visible/pickable continuation across the accepted 0–20,000 m navigation range. No lint, unit, browser, migration, accessibility, audit, manifest, smoke, performance, or soak command was run after those changes because the requested QA pause remains active. Current runtime appearance, concept fidelity, and behavior are therefore **NOT ACCEPTED / UNVERIFIED**, and the candidate remains **NOT READY**. The authoritative status lives in `QA_REPORT.md` and `LAUNCH_READINESS.md`.

Source inventory in the latest permitted build also includes these unqualified safeguards: normal Ride heat approaches the safe 62 ceiling while Turbo can exceed it; every unfinished AI pair receives grounded same-lane rear-impact resolution after each live and classification fixed step; campaign/Rider School get course-anchored presentation-only lane numerals `1–4` plus a transverse start treatment while authored Test Rides are excluded; one pure viewport predicate selects portrait framing for portrait orientation or the retained sub-680 px fallback; and the narrow layout bounds normal/mirrored touch controls and Heat at 320–780 px / 140% UI scale while retaining 44 px minimum targets. Native semantic touch buttons expose hold state, lane pulses, and an adaptive Recovery progressbar. Production-smoke source now binds the runtime commit and a valid format-2 manifest, verifies every served file and aggregate before and after the Chrome journey, and opens cached Practice offline; `shell-v30` installation fetches fresh same-origin bytes and deletes a partial current cache on failure. Focused unit, browser, smoke-support, and service-worker fixture source exists, but none of it was run; no behavior, accessibility, visual, offline, performance, or release qualification is claimed.

The same build also contains an active-lesson tutorial gate: earlier evidence cannot pre-clear later lessons, multi-part requirements are ordered, coast/mud/grass require measured slowdown, and each completed lesson freezes atomically for a 550 ms handoff without discarding held Ride or Turbo input. Critical heat latches within its active lesson so the player can release Turbo safely. Every incomplete riding lesson exposes **Retry this lesson**, which resets the current simulation and active evidence while preserving earlier lesson markers and cumulative recap credit. Keyboard capture and the global pause shortcut ignore interactive controls, so native button activation remains available even when Space is remapped. The portrait touch cluster now uses six original inline rally pictograms while retaining visible Ride/Turbo text, stable accessible names, hit areas, safe-area placement, and mirroring. It also contains one-transition-per-press keyboard pause handling, mapped-threshold gamepad arbitration that cannot be seized by neutral drift, held-key recovery after touch release, and immediate offline-readiness invalidation whenever the controlling service worker changes. Focused regression source exists; a later read-only review found and corrected active-gamepad-before-touch arbitration, but the regressions, touch ergonomics, fresh visual comparison, and real service-worker transition flow remain **UNVERIFIED** while automated QA is paused.

To close another direct concept-fidelity gap, handcrafted Canyon/Rider School festival pockets now reuse their existing five instanced batches for outward-rising timber grandstands and staffed elevated cooling-gate watchtowers. Low/Medium/High use two/three/four tier rows; each watchtower raises the existing deck/canopy/four posts, uses a mirrored teal/coral roof, and moves two/three/four existing spectators onto the deck. A 5.6 m safety rule rejects any route pocket that approaches a showcase; the current production-High placements already clear it, so all 22 route pockets remain alongside four watchtowers for 26 pockets / 104 tiers. Non-Canyon and editor-authored routes receive zero Canyon-specific watchtowers/tier pockets; separate per-theme density rules remain active. Exact deferred diagnostics cover tiers, tower counts/staffing, quality presets, and authored isolation. This source is built and independently reviewed, but it is **NOT CAPTURED / NOT ACCEPTED / UNVERIFIED** because the requested browser and automated-QA pause prevents the frontend skill's required desktop/portrait comparison.

Release-provenance hardening is now present in source but remains **UNVERIFIED**. Node `26.4.0` and npm `11.17.0` are pinned by `.node-version` and `packageManager`. The format-2 guard captures clean `HEAD`, creates a detached checkout of that commit, then reads and revalidates the exact package, lockfile, and Node pin only from that checkout before accepting annotated `v1.0.0-rc.2`. It captures the annotated tag object's identity/type and requires that exact object to remain unchanged through the build. It verifies the absolute npm CLI, rechecks its file identity and SHA-256 after every invocation, uses empty isolated user/global npm configs with install scripts explicitly enabled, and runs `npm ci --no-audit --no-fund` plus sanitized non-QA `npm run build`. Phase checks reject every ignored checkout input except generated `node_modules` after install and `node_modules`/`dist` after build; source inputs and both checkouts are revalidated. Only regular distribution files may replace root `dist/`, and non-path-leaking tag/build/toolchain provenance is recorded with the hashes. Recursive guarded cleanup removes both root `dist/` and even a malformed sidecar directory on failure, while temporary worktree state is always pruned. The ignored canonical manifest cannot dirty tagged source; the earlier format-1 record remains at `artifacts/history/release-manifest-1.0.0-rc.2-earlier-snapshot-format-1.json`. Deferred hostile-config/fake-CLI fixtures cover orchestration, stale-root replacement, commit-derived inputs, ignored-input phases, same-commit tag replacement, npm-CLI mutation, cleanup, provenance failures, and invalid artifacts. The normal build regenerated notices, but no release guard, hostile fixture, manifest, or smoke path was executed while qualification is paused.

The current release-hardening source remains behaviorally **UNVERIFIED**. Vite derives a public runtime commit/dirty marker directly from Git. Performance measurement and soak bind clean source identity to hashed local and pre/post served bytes, require the runtime commit marker, force and record explicit non-`auto` quality, capture host/device/GPU fields, and fail request/HTTP/page/console gates with a nonzero status; only a 30-minute Rival soak can report release `PASS`, while Practice remains diagnostic. Production smoke binds the runtime commit to the format-2 manifest, opens cached Practice offline, reverifies every served byte after the browser journey, and uses focused race-gate/atomic-promotion/post-journey fixtures. App-shell source and the latest permitted build select `shell-v30`; the 04:53 `shell-v29` build remains historical evidence only. Operations separates the immutable product tag from an evidence-only attestation commit/tag. Predecessor rollback remains **UNVERIFIED** because no deployable archive locator plus retrieval/hash rehearsal is recorded. The latest permitted build established only build-time asset verification, strict typecheck, Vite compilation, and notices verification; no test, browser, performance, soak, manifest, audit, or smoke command was run for this hardening.

An earlier corrective slice stores the standing Solo personal-best lap and checkpoint arrays, replaces them only with a strictly faster final time, and shows current timings beside signed prior-PB comparisons. Rival-pair contact now uses a symmetric forgiveness distance after every live and post-finish classification fixed step. At 140% UI scale, pixel-bounded HUD/control rules cover 320–780 px and Rider School retains a touch-scrollable action area on a 320×568 short phone. Release tooling now records and repeatedly revalidates the resolved child Node executable hash, requires a credential-free dedicated root, proves `/` matches manifest `index.html`, rejects origin drift, and derives both service-worker index keys from one fresh response. These changes compile in the latest permitted build, but every focused behavior/browser/release fixture remains **NOT RUN / UNVERIFIED**.

The latest permitted build additionally compiles per-fixed-tick environment/player/AI/contact coupling, a conflict-safe native-v3 lane-binding migration, explicit panorama exclusion for editor-authored Test Rides, and a concept-complete built-in bike fallback. It also verifies and ships Barlow Condensed 700/900 Latin WOFF2 subsets and their complete SIL OFL 1.1 text, using the same family for DOM and CanvasTexture display labels and precaching both files in `shell-v30`. The working source separately contains manifest-specific atomic production-smoke bundles. None of the focused regressions, browser/font rendering, fallback capture, offline transition, production smoke, or concept comparison was run while the owner's pause remains active.

A final source-only gameplay, accessibility, editor, and persistence audit found no P0 defects and identified 16 P1 defects. Source now addresses the initial HUD snapshot, truthful results language, precise crash feedback and repeat-crash telemetry; focus visibility, short-phone pause/tutorial geometry, scalable narrow telemetry, and caption placement; existing-module selection, full-range editor navigation, pointer-derived placement validity; bounded import parsing, explicit blocked-database recovery, strict persisted-record validation, atomic corrupt-track quarantine, and user-downloadable recovery records. Focused regression source was added, but none of those tests or affected browser/device flows was run during the pause. The corrections therefore remain **UNVERIFIED** and do not change the **NOT READY** release decision.

A later source-only follow-up also found no P0 and corrected seven additional data/input boundaries: held touch now precedes an active controller; settings/progress recovery is selective and atomic; corrupt-track recovery rechecks the current cross-tab snapshot; replay insertion/pruning is atomic; editor creation, duplication, validation, persistence, import, and Test Ride share a 500-module ceiling; a failed Test Ride save retains and retries the exact snapshot; and recovery export applies depth/node/string/output budgets before reading binary payloads while unmeasurable aggregate records fail closed. The latest permitted build typechecks and compiles these changes, but their regression source was **NOT RUN**. The same build source-addresses campaign grade, jump/module form, festival density, and Pine/Foundry/Summit venue signatures. It also compiles the shared schema-v2 winding/graded Builder/Test Ride centerline and strict full rotated parent-footprint boundary validation. Legacy v1 tracks that need the stricter repair remain readable/editable and are not quarantined, but cannot be saved or Test Ridden until repaired. All affected migration, unit, browser, visual, and behavior evidence remains **NOT RUN / UNVERIFIED**.

## Run and verify

Use Node `26.4.0` from `.node-version`, npm `11.17.0` from `packageManager`, and the committed npm lockfile for release work.

The list below is the command interface, not a safe release order. For a release candidate, first track every intended source/asset/tooling/provenance input, commit once, confirm a clean final commit, run all pre-tag qualification (including performance and soak), then create annotated `v1.0.0-rc.2` at that exact `HEAD`. Only then may the guard generate the manifest-bound bytes. Preview and smoke those exact bytes, and publish the immutable tag only after acceptance; never move or rebuild a published tag. See `docs/OPERATIONS.md` for the exact sequence. `npm run test` includes Vitest and the Node release fixtures, while `npm run test:coverage` reports Vitest coverage only.

```sh
npm ci
npm run dev

npm run typecheck
npm run lint
npm run test
npm run test:coverage
npm run test:e2e
npm run assets:verify
npm run audit

npm run build
npm run release:manifest
npm run preview
npm run smoke:production
```

Use `npm run assets:build` only when intentionally regenerating the checked-in GLB/KTX2 pipeline, then rerun `npm run assets:verify`. The verification command also enforces the exact shipped Canyon panorama, shipped/retained-source title artwork, app icon, bundled fonts, and font-license contracts without regenerating them. The headed measurement and 30-minute Rival soak commands are documented in `docs/OPERATIONS.md`; their JSON and screenshot evidence belongs under `artifacts/performance/`.

`npm run build` produces a development-review production site in `dist/` and writes its complete `THIRD_PARTY_NOTICES.txt`. From a clean exact-tag checkout under the pinned toolchain, `npm run release:manifest` does not trust those ambient bytes: it performs its own isolated clean install and non-QA build, replaces root `dist/`, rejects provenance mismatches, non-regular entries, source maps, missing notices, QA markers, and local paths, then writes the ignored format-2 sidecar. With those exact generated bytes running on the documented preview port, `npm run smoke:production` uses installed Google Chrome to verify boot/version, race restart, editor open, and a service-worker-controlled offline reload. Deploy `dist/` as one immutable unit at the root of a dedicated origin; do not deploy the Vite development server or mount the app below a URL prefix. The browser tests build and serve their own QA-mode candidate. Performance commands and their environment limitations are documented in `QA_REPORT.md`.

## Product scope

- Five two-lap launch tracks: Canyon Kickoff, Pine Run, Coastline Clash, Foundry Flight, and Summit Showdown.
- Solo Challenge, Rival Main Race, Practice, campaign unlocks, and seven-tier Summit mastery.
- Four-lane racing with throttle, a safe 62 normal-Ride heat ceiling, turbo heat, cooling, wheelies, airborne pitch, landings, terrain, hazards, crashes, and asymmetric player/AI plus pairwise AI rider collisions.
- Route-following and pursuing AI with Easy, Standard, and Expert decision profiles.
- Interactive first-play tutorial, immediate retry/restart, results/coaching, and persistent personal bests.
- Keyboard controls with arrow-cluster lane defaults, full remapping, standard gamepad support, vibration where available, touch controls, and mirrored touch layout.
- Local 3D editor with 25 modules, 50-action undo/redo, validation, 1–9 laps, examples, safe JSON interchange, library persistence, thumbnails, and test play.
- Reduced motion/shake, high contrast, colorblind-safe signals, captions, UI scale, and separate master/music/SFX volume controls.
- No account, payments, ads, tracking, public sharing, or server dependency.

## Architecture

The game uses strict TypeScript, Vite, React for application UI, direct Three.js/WebGLRenderer for 3D presentation, Zustand for app flow, a custom fixed-step simulation, IndexedDB/Dexie for versioned local data, and Web Audio for original procedural sound. Gameplay and rendering stay outside React's render loop. Nonessential race/editor code is lazy loaded, the generated player-bike GLB uses Meshopt geometry plus an embedded BasisLZ/ETC1S KTX2 texture, and the Canyon/tutorial matte uses a bounded ImageBitmap-backed 2D background path with a procedural fallback.

## Documentation

- [GAME_BIBLE.md](GAME_BIBLE.md) — creative direction and player experience.
- [GAME_SPEC.md](GAME_SPEC.md) — normative behavior and acceptance criteria.
- [ARCHITECTURE.md](ARCHITECTURE.md) — runtime boundaries and data flow.
- [ASSET_LICENSES.md](ASSET_LICENSES.md) — source, rights, hashes, and bundle inventory.
- [QA_REPORT.md](QA_REPORT.md) — executed test and performance evidence.
- [LAUNCH_READINESS.md](LAUNCH_READINESS.md) — release-gate decision and remaining actions.
- [AGENTS.md](AGENTS.md) — repository contribution rules.
- [docs/OPERATIONS.md](docs/OPERATIONS.md) — build, deploy, cache, support, backup, and rollback runbook.
- [docs/ASSET_PIPELINE.md](docs/ASSET_PIPELINE.md) — reproducible GLB/KTX2 pipeline.
- [docs/design/PRODUCTION_ART_PROVENANCE.md](docs/design/PRODUCTION_ART_PROVENANCE.md) — Canyon panorama generation brief, source review, runtime constraints, and open human-review gates.
- [docs/RIVET_RIDGE_RALLY_DESIGN_REPORT.docx](docs/RIVET_RIDGE_RALLY_DESIGN_REPORT.docx) — template-based design review, current evidence scope, recommendations, and **NOT READY** decision.

## Identity and legal hygiene

Only **RIVET RIDGE RALLY** is approved shipped branding. The local workspace was renamed to a neutral product-title directory on 2026-07-14. Keep public CI logs, support exports, and source maps free of legacy third-party working names and absolute local paths. The owner must also select the repository's top-level product license and complete final legal, trademark, and trade-dress review.
