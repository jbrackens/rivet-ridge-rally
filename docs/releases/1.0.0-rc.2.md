# RIVET RIDGE RALLY 1.0.0-rc.2 release candidate

**Status:** NOT READY — current working-tree command and scoped browser gates pass, but the candidate is not frozen or release-qualified.

**Release-note date:** 2026-07-19

This file describes the intended `1.0.0-rc.2` candidate and summarizes working-tree results whose authoritative scope is recorded in `QA_REPORT.md`. It is not evidence that a clean candidate was approved, tagged, or deployed.

Fresh static qualification for the unchanged product files on 2026-07-19 passed `npm run assets:verify && npm run typecheck && npm run lint && npm test && npm run audit && npm run audit:release-scope` at `9a8abdd68090b9b7b6a616b889aa410080ec3c9e`, immediately before the documentation-only evidence update. The batch includes 300 Vitest tests, 45 release-manifest fixtures, 71 release-attestation fixtures, 34 production-smoke/service-worker/release-scope fixtures, zero high-severity audit vulnerabilities, and release-scope audit PASS across 131 files / 15,208,175 bytes with aggregate SHA-256 `463b7d9e36d6c071efaf3c26711f05552e1aef8170033fa4dcf4d2b62028a240`. After the normal non-QA preview was restored, a later scoped `npm run audit:release-scope` pass over the same 131 files / 15,208,175 bytes recorded aggregate SHA-256 `a8718dd150b715e4bd8e38b092a34482af49c4ab94585c87776ea75a1ca9fcf6` and zero findings. On 2026-07-19 at `213ad3ccd3012c1e8ee403edc4ef5abe452db4b9`, the same bounded gate batch was rerun after the Track Builder worksite/camera slices: `npm run assets:verify && npm run typecheck && npm run lint && npm test && npm run audit && npm run audit:release-scope` passed, with 302 Vitest tests, 45 release-manifest fixtures, 71 release-attestation fixtures, 34 production-smoke/service-worker/release-scope fixtures, zero high-severity npm audit vulnerabilities, and release-scope audit PASS across 131 files / 15,233,055 bytes with aggregate SHA-256 `ce511848b97d1ed5c73988a6a05008f25d2afdc3c12958e8a1cf7162dc470416`. This is working-tree evidence only, not a frozen-candidate release pass.

## Candidate changes

- Default lane movement uses Left/Right Arrow, with Up/Down Arrow for airborne pitch, and remapping remains supported. A one-shot lane latch now delivers complete Arrow keydown/keyup taps between fixed-step samples in order, including neutral separators for repeated same-direction taps so each press can produce one lane move through the shared simulation latch, and clears queued taps on suspension. The focused `InputManager`/key-label suite now passes 27/27, and a corrected muted production-preview check advances the public lane lesson with one zero-delay Arrow press. Current focused Chromium input/remap paths pass; the eight-case migration scope passes fresh arrow defaults and exact legacy A/D migration; gamepad emulation and the emulated touch matrix also pass in their scoped projects.
- Rider School covers riding/coasting, lanes, Turbo heat and cooling, wheelies and barriers, airborne pitch/neutral landing, mud, grass, crashes/recovery, and rider-collision responsibility. Across split resumed runs, the two continuity/prompt cases passed first; the three formerly failing later-copy, deterministic-retry, and comprehensive-journey cases passed together headed 3/3 and individually headless. The comprehensive assertion passed below three in-game minutes; long functional cases use Low quality separately from High-quality visual QA. Emulated touch introduction passes 2/2.
- Normal Ride heat approaches a safe operating ceiling while Turbo can overheat. A raised front wheel clears the barrier with a speed cost rather than crashing; current focused tutorial/gameplay evidence passes that behavior. Production-length tuning and physical/manual handling review remain open.
- Live **Retro recovery tapping** changes reach the mounted simulation, and disabling the option clears the pending tap latch. It is covered by the current focused browser/input qualification and passing working-tree unit suite; frozen-candidate repetition remains pending.
- Production Solo/par targets are provisionally set to Canyon 190/181 s, Pine 208/198 s, Coastline 224/214 s, Foundry 239/228 s, and Summit 259/247 s. Seven Summit Mastery targets tighten from 257 s to 247 s while starting heat rises from 35% to 65%. Source-level all-tier progression now has deterministic coverage, the isolated Chromium campaign matrix passes 18/18 in 27.7 minutes after the Summit Mastery escalation path was made deterministic through a QA-only Rookie difficulty hook, deterministic source calibration classifies complete six-rider fields across all five launch tracks and Rookie/Rider/Ace, and a deterministic production-length representative rider completes every Solo target without crash or overheat. Physical/manual fairness and frozen-candidate repetition remain unverified.
- Track Builder adds labeled lane/Route-view keyboard placement, revalidates keyboard and pointer candidates, announces placement status, and retains authoring/recovery controls in narrow layouts. The current polish slice adds concept-style lane/rotation/height/lap steppers, a compact lane map, code-native SVG stepper icons, 54 non-colliding route-following worksite props, and a closer default route-diorama camera while preserving exact inputs and the explicit Fit Route overview. Current Chromium editor coverage passes placement, selection/editing, checkpoints, undo/redo, library actions, narrow layout, reload/export/import, Test Ride, examples, all-module circuits, corrupt import recovery, the new stepper controls, worksite-prop diagnostics, and camera-style diagnostics.
- A combined Support / Privacy / Accessibility / About screen has a native-button title entry and records candidate identity, local-data behavior, and owner-blocked public information. Focused browser coverage exercises the rendered entry, keyboard/responsive path, and automated accessibility assertions; public contact, operator/data-controller identity, host policy, retention/deletion commitments, product license, and manual screen-reader approval remain blockers.
- Original Blender-authored hero-bike/rider, shared five-variant rival pack, and Canyon venue sources replace the predecessor focal path and primitive-only rival presentation. `npm run assets:verify` passes the 49,780-triangle / 517,664-byte hero GLB (39,912 bike triangles, including 14,284 wheel triangles, plus 9,868 rider triangles), SHA-256 `538be4269927544fa91cfc405742523a60422d77d546ce9c8859f407deb2e6e8`; its 15,165-byte manifest, SHA-256 `005ed98f7c4e365abb104da79352eb78b3bc3d7340f9dc074a3e84c772918918`; source-pair UUID `31000dd4-2c42-4c74-ad5d-28b5e4aec32f`; the rival contract; Canyon 32,008 triangles / 427,028 bytes; and production art/provenance including the v3 action sheet. Headed hero action integration passes 5/5, and rival pack passes 2/2. Hero and rival use a safe five-second fallback and may atomically install a valid late result; Canyon kit and panorama use 12-second visual-art deadlines before procedural fallback. Release performance, legal review, and concept-art acceptance remain unverified.
- The corrected Canyon composition preserves 22 route pockets plus four cooling watchtowers for 26 pockets / 104 tiers on deterministic High while enforcing at least 5.6 m clearance. Authored marshal towers and the workshop were relocated, and authored structural decor fails closed inside that clearance. Targeted unit tests pass 3/3; quality-preset plus authored-Canyon runtime checks pass 2/2. A normal nonqualifying local preview reports QA API false, ready bike/Canyon/environment state, 42 Canyon-kit placements, 26 pockets, 104 tiers, advancing time, and working pause/resume. It is not production smoke, offline evidence, performance evidence, or an official artifact.
- The canonical v20 hero-motion review manifest at `artifacts/visual-review/hero-motion-action-states-v20-current-20260718t-mechanical-detail-qamode/manifest.json` is 121,832 bytes with SHA-256 `9a6beeb30396e57054d5f5aa4500ec32cd2d9f9ca8746eb48846f2f7cca24223` and records `PASS` for all 15 required action states. Each independent public-UI run used passive snapshots and headed Chromium with browser audio muted; normal frames require the authored hero, Canyon kit, panorama, PMREM material response, contact-shadow diagnostics, and `trail-burst-contact` dust ready, and only the final frame deliberately fails the hero request. It is a dirty-working-tree, nonbaseline technical pass only: the visuals are still **NOT ACCEPTED**, `visualAcceptance` remains `PENDING_OWNER_REVIEW`, and owner concept-art/similarity and legal approval remain open. Earlier exact-tip evidence remains retained and unresolved: after correction of the 84 m `qa-fast-race`/production-decor harness hybrid, the production-length route passes its 26-pocket / 104-tier / 1,320-safety-block prerequisites, but that historical suite still ends 5 failed / 5 skipped against 2%, and separate Chromium/Firefox/WebKit title checks fail. No baseline was created, changed, promoted, or waived.
- The clean-source controlled five-track visual candidate now passes technical capture for current source `a9a97ef98f5004a4fcca78074b9d93283bcb8331`. `npm run visual:candidate` produced a 33-file / 8,004,212-byte visual QA candidate with aggregate SHA-256 `f8005e5bf9f1e3991b4fa90c1a1259b2842f5fb492e32a0e4e12c659a3c5661f` and candidate manifest SHA-256 `9d0b13c2084ee421d8059af36cc57e298b4c0c739f23ae5996ec7b8b19026813`; `npm run visual:capture -- --output-dir artifacts/visual-review/rc2-current-a9a97ef-20260719t082620z --port 4375` passed 11/11 with manifest SHA-256 `5866dc0888245224a2e2c85f5883b3dfe07c5300b8d9034fe802ab5775761610`. It covers five start Practice frames, five midcourse Rival frames, and one curved Canyon baseline candidate. This is not owner acceptance or a promoted baseline.
- A non-acceptance `visual:approval:draft` helper now extracts the exact owner-review fields from a controlled capture into an external JSON draft without modifying the repository. The 2026-07-19 rehearsal against `artifacts/visual-review/rc2-current-a9a97ef-20260719t082620z/manifest.json` populated source `a9a97ef98f5004a4fcca78074b9d93283bcb8331`, candidate aggregate `f8005e5bf9f1e3991b4fa90c1a1259b2842f5fb492e32a0e4e12c659a3c5661f`, capture manifest SHA-256 `5866dc0888245224a2e2c85f5883b3dfe07c5300b8d9034fe802ab5775761610`, and Canyon Practice 500 screenshot SHA-256 `5f31240535d0c38569e9680dcef9846cecddbf92f797dcbab89dae2c004da3c3`; the generated draft remains `PENDING_OWNER_REVIEW`, so it is not promotable acceptance evidence.
- Current runtime visual-identity diagnostics now cover all five launch tracks. The Chromium QA smoke on isolated port 4178 starts each track at High quality and verifies Canyon sponsor/crowd/cactus/cut-bank scenery, Pine roots/markers, Coastline water/boardwalk/huts, Foundry gantries/furnace panels, and Summit finale equipment through canvas diagnostics. This is scoped current-byte evidence only; owner visual acceptance, promoted baselines, legal similarity review, and physical-device readability remain open.
- The current non-QA local `dist/` checkpoint contains 33 files totaling 7,999,709 raw bytes. `index.html` is 1,632 bytes with SHA-256 `c732e3be89777272a36de5085b63968b9f58c41260296f914ba642b29fca2c04`; the aggregate is `4a6535f70c54175e8773ec5cff66a60a5a808c4d194b25268b5bf577fcdfe23f`; `assets/3d/hero-bike-rider.glb` is 517,664 bytes with SHA-256 `538be4269927544fa91cfc405742523a60422d77d546ce9c8859f407deb2e6e8`; and the 43,872-byte notices have SHA-256 `f837ed705667d0f3976bbd419f42d8c63844a3eb4b52f76db88ed3a1d6e270c2`. A scoped live production-preview smoke on `127.0.0.1:4173` proved runtime commit `f5882c9ccb921fda6dd3b7e5b51e4f3b833a80ee` with `dirty=false`, absent QA/performance APIs, visible title controls, authored bike readiness, Rider School progress to Lesson 2, campaign menu access, Track Builder entry, visible Save/Test Ride/Fit Route/Route Complete controls, and no page/request/HTTP/unexpected-console failures after rebuilding normal non-QA bytes. This working-tree checkpoint is not a format-2 manifest, installed/offline smoke, immutable artifact, or release qualification.
- The current headed diagnostic at `artifacts/candidate-evidence/diagnostic/performance/current-f5882c9-headed-restored-20260719T0738Z.json` reports clean source/runtime binding to `f5882c9`, stable served bytes, authored hero readiness, zero request/HTTP/console/page errors, desktop normal/stress at 60.00/60.01 FPS with 4.0/2.6 ms p95, and emulated-mobile normal/stress at 120.00/120.00 FPS with 1.6/1.5 ms p95. It ends `FAIL` only because no format-2 release manifest exists yet; it is diagnostic evidence, not schema-4 release performance qualification or physical-device proof.
- Race completion commits the official result and progression independently from replay availability. Current focused core/results/lifecycle paths and the working-tree unit suite pass; frozen-candidate qualification remains pending.
- Release source includes path-private full-npm-package-tree-bound, gzip-bound format-2 product manifests; schema-5 manifest-bound production smoke; and a strict schema-v3 evidence-only attestation verifier for schema-4 exact-product raw-frame headed performance, visible-racing-time 30-minute Rival soak, ten candidate-bound QA checks/logs, installed-Chrome production/offline identity, the canonical visual-approval/baseline chain, human approvals, and predecessor rollback/data-safety evidence. Rollback separately binds the retrieved archive's positive byte count/SHA-256 and complete pre/post staged raw/gzip inventories to the predecessor manifest and one dedicated root URL. The 45 release-manifest, 71 release-attestation, and 34 production-smoke/service-worker/release-scope fixtures pass; the manifest fixtures now also prove early preflight failures preserve the existing launched root `dist/` while later invalid copied candidates are cleaned. A dirty-tree real `release:manifest` preflight on 2026-07-19 exited 1 without disturbing the clean f5882c9 preview. No current format-2 candidate, real smoke, rollback record, or attestation exists.
- Installable-app source adds portable raster, maskable, and Apple touch icons under service-worker generation `shell-v35`. The production-smoke/service-worker fixtures pass; real install/offline/update behavior, final icon crops, and exact-product smoke remain unverified. The referenced GLB/icon inputs are now tracked on this branch, but freeze-time clean-candidate identity and exact-product smoke remain pending.
- Local-data hardening serializes and delta-merges profile writes, preserves reset/retry ordering, creates conflict copies, requires exact-base deletion, guards editor operations, and retains failed Test Ride snapshots. Focused persistence, reliability, and editor-session browser paths plus the working-tree unit fixtures pass; true two-tab/retry/reset races and frozen-candidate qualification remain unverified.

## Known issues and open gates

- **EXTERNAL BLOCKER — release ownership/legal:** no owner-selected top-level product license, final trademark/trade-dress decision, public support path, production host/access plan, or accountable release/incident roster is recorded.
- **P1 — qualification:** working-tree typecheck, lint, 452 tests/fixtures (302 Vitest, 45 release-manifest, 71 release-attestation, and 34 production-smoke/service-worker/release-scope), asset verification, zero-vulnerability high-severity audit, focused browser scopes, campaign modes 18/18 in 27.7 minutes, and rival pack 2/2 pass. The v20 15-capture hero-motion contract is a dirty/nonbaseline technical pass, not visual acceptance. None of these dirty-tree results closes accepted visuals or exact-product clean-tag, format-2, performance, soak, production/offline-smoke, rollback, approval, or attestation gates for one frozen byte set.
- **P1 — production calibration:** the changed Solo/par constants, unchanged Rookie/Rider/Ace profiles, and seven Summit Mastery target/heat tiers now have focused target-feasibility, deterministic production-length representative-player completion, all-tier source progression, scoped campaign-browser evidence, and deterministic three-difficulty full-field source calibration, but still lack physical/manual fairness review and exact frozen-candidate repetition.
- **P1 — editor/support runtime acceptance:** scoped editor and Support keyboard/responsive/automated-accessibility paths pass, but physical touch, screen-reader/manual review, public content ownership, and frozen-candidate acceptance remain open.
- **P1 — visual acceptance:** the authored hero and Canyon slice materially improve source fidelity, but final runtime framing, rival/venue cohesion, physical-device readability, and human concept/legal acceptance remain open.
- **P1 — rollback:** no canonical immutable predecessor artifact locator with successful retrieval, exact archive byte count/SHA-256, source-bound format-2 manifest, complete matching pre/post staged raw/gzip inventories, or native-60→native-30 non-destructive data-safety proof over non-empty progress, custom-track, and replay fixtures has been recorded.
- **Accepted P2 — duplicate Basis transcoder:** the historical `shell-v30` bundle emitted a byte-identical hashed JS/WASM pair in addition to the stable public pair, adding 584,862 raw bytes (57,529 JS + 527,333 WASM). RC1 accepted the duplication to preserve explicit same-origin/offline behavior. The current `shell-v35` footprint must be remeasured; removal remains deferred until offline startup and notice inventory are requalified.
- **External coverage:** representative physical mid-range mobile, installed Safari/iOS/Firefox/Edge, physical gamepad, assistive technology, public-origin headers/TLS, hosting, and deployment evidence remain owner/device dependent; Playwright engines and emulated viewports do not substitute for them.

## Evidence required before promotion

Promotion requires the current passing working-tree gates to be repeated and bound to one exact frozen candidate, with outcomes recorded as `PASS` / `READY` in `QA_REPORT.md` and `LAUNCH_READINESS.md`: static/unit/build/audit and complete browser gates; production-length calibration; manual screen-reader and physical/input review; owner-approved visual baselines with a passing non-mutating rerun; an annotated product tag and gzip-bound format-2 manifest; schema-4 exact-product headed performance and 30-minute visible-racing-time Rival soak; all ten structured QA records/logs; schema-5 production/offline smoke; explicit QA/accessibility/legal approvals; predecessor-manifest-bound rollback retrieval/served inventories and native-60→native-30 data safety; and a passing schema-v3 attestation from the clean evidence-only tag.
