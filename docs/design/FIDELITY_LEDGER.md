# RIVET RIDGE RALLY — Visual Fidelity Ledger

**Review date:** 2026-07-18

**Review state:** NOT ACCEPTED — WORKING-TREE TECHNICAL SLICE VERIFIED; OWNER REVIEW OPEN

**Review authority:** owner feedback rejected the previous coarse-fidelity acceptance; the implementation review below records the response, not approval

**Candidate:** `1.0.0-rc.2` working source; untagged and not qualified for release

**Historical evidence:** annotated `v1.0.0-rc.1` and its screenshots remain the record of the superseded candidate

The concepts define composition, hierarchy, environmental richness, rider readability, track depth, atmosphere, and control presence. They are not pixel-accurate production screenshots. That distinction is not a blanket waiver for a materially emptier or coarser result.

The 2026-07-12 ledger accepted the RC1 renderer as a performance-safe low-poly interpretation even though its geometry and scenery were materially coarser and less dense than the gameplay concepts. The owner subsequently rejected that conclusion: the game graphics did not look sufficiently like the concept art. That direct acceptance result supersedes the implementation-only `PASS`. The current working-source changes described here reopen the comparison; code presence and regenerated snapshots cannot turn it back into a pass without renewed owner review.

## Historical RC1 comparisons — superseded

| Surface | Concept | RC1 browser evidence | Historical result and current disposition |
|---|---|---|---|
| Title and campaign | `concepts/title-background-production.png`, `concepts/campaign-menu.png` | `e2e/core-flow.spec.ts-snapshots/title-screen-chromium-darwin.png` | **HISTORICAL RC1 PASS; CURRENT BASELINE REOPENED.** The source art and campaign hierarchy were accepted for RC1, but the new persistent Rider School replay action changes the live main-menu composition and therefore requires a fresh capture before the current baseline can be accepted. |
| Desktop race | `concepts/gameplay-desktop.png` | `e2e/visual-regression.spec.ts-snapshots/race-screen-chromium-darwin.png` | **REJECTED / REOPENED.** RC1 contained the required HUD fields, four-lane route, rider, hazards, fencing, stands, and sightline, but the previous ledger excused substantially coarser geometry and lower scenery density. The owner rejected that tradeoff as visually unlike the concept. |
| Mobile race | `concepts/gameplay-mobile.png` | `e2e/visual-regression.spec.ts-snapshots/race-mobile-mobile-chrome-darwin.png` | **REJECTED / REOPENED.** RC1 kept the route and labeled controls visible, but its sparse course treatment and compact flat controls did not meet the owner's concept-fidelity expectation. |
| Track builder | `concepts/track-builder.png` | `e2e/visual-regression.spec.ts-snapshots/editor-screen-chromium-darwin.png` | **REJECTED / REOPENED.** The old shell-level acceptance did not establish the concept's compact connected winding course. The previous editor and Test Ride placed modules on an endless flat straight, so current acceptance requires a fresh Builder/Test Ride comparison. |
| High contrast and color redundancy | desktop race concept | `e2e/visual-regression.spec.ts-snapshots/race-high-contrast-chromium-darwin.png` | **HISTORICAL FUNCTIONAL PASS; CURRENT BASELINE PENDING.** RC1's real WebGL guides and redundant hazard silhouettes remain historical accessibility evidence. The changed course composition requires a new capture and review before current visual acceptance. |

The paths above identify the checked-in RC1 evidence at the time of its review. If current-candidate baselines replace files at those paths, retain the tagged RC1 versions as the immutable historical comparison rather than describing the new bytes as RC1 evidence.

The current visual-regression source also names `race-curved-course-canyon.png`, but no accepted baseline exists at that path. It must remain absent until the paused runtime qualification resumes, a fresh exact-candidate Canyon bend is captured with the hero, authored Canyon kit, and festival panorama all confirmed ready, and the owner explicitly reviews and accepts that image against the approved concepts. Creating or updating the file merely to make the snapshot command pass is prohibited. Until those steps occur, the curved-Canyon visual gate is **NOT READY / UNVERIFIED**.

Snapshot mutation is disabled in `playwright.config.ts` with `updateSnapshots: 'none'`, and the visual suite checks the effective setting before any screenshot assertion. A command-line override therefore fails before it can create or replace a checked-in baseline. The missing curved-Canyon baseline may be created only by `npm run visual:promote:canyon`, which accepts one externally authored owner-approval record and one schema-v3 PASS capture manifest. It re-inventories the fixed QA candidate, independently validates the exact 11-frame Practice/Rival/Canyon matrix and each browser response set, requires the approval after capture and promotion within 24 hours, and is hard-coded to create only `e2e/visual-regression.spec.ts-snapshots/race-curved-course-canyon-chromium-darwin.png` plus `docs/design/RACE_CURVED_CANYON_BASELINE_APPROVAL.json`. The committed record embeds both complete validated manifests before the baseline is copied, preserving evidence that otherwise lives under ignored paths. If a hard interruption leaves only that uncommitted record, the operator must confirm the baseline is absent, delete only the orphaned record, restore the clean approved commit, and rerun guarded promotion; manual baseline copying is prohibited. Neither output exists yet.

The external approval must declare `authentication: "external-manual-trust-boundary"`. Hashes, timestamps, manifests, and the external-path requirement establish internal consistency only; they do not authenticate the named person or prove that concept review occurred. Genuine product-owner authorship and review remain a manual **EXTERNAL BLOCKER**. No signature, key, or owner acceptance is inferred from tool success.

Baseline approval deliberately precedes final release tagging. The owner reviews the exact clean-commit Canyon Practice frame at 500 m against the concepts, records `ACCEPT` against its hashes and readiness evidence, and then runs the guarded promotion. Commit those two promoted files, create the final annotated product tag from that later commit, rebuild the isolated QA candidate, and rerun the non-mutating visual suite. The pre-promotion capture is the approval input; it is not proof that the later tagged commit passed. Only the final tagged rerun can establish that the promoted pixels still match the checked-in baseline.

## Current working-source response — not yet accepted

### 2026-07-17 executed Canyon comparison slice

This subsection supersedes any older “not captured,” “not run,” or “no renderer measurement” wording in the long-form rows below only for the named Canyon working-tree slice. It does not qualify the other four venues, a frozen release candidate, physical devices, or owner acceptance. The captures are local ignored review artifacts; no checked-in baseline was created, changed, or promoted.

| Comparison point | Working-tree evidence and result | Remaining concept-fidelity gap |
|---|---|---|
| Camera and first-frame composition | Fresh desktop start and bend captures frame both wheels, retain all four lanes, add a slight three-quarter view, and keep the near route readable: `artifacts/visual-iteration/working-tree-canyon-start-v4b.png` and `artifacts/visual-iteration/working-tree-canyon-bend-v3.png`. | The owner has not accepted the composition, and motion/sightline comfort still needs physical-device review. |
| Horizon, shoulder terrain, and atmosphere | The Canyon festival panorama is visibly installed at runtime; its painted clouds and layered mesas now sit lower in frame, and duplicate procedural clouds are removed. The current 2026-07-19 shoulder slice adds two route-following red-rock cut-bank ribbons plus High-quality 78 shelf, 184 rock, and 52 agave/yucca presentation instances, with diagnostics verified in `/tmp/rivet-ridge-canyon-shoulder-frozen-practice-v4-1280x720.png` and the live 4173 smoke. | The far layer and shoulder color are materially closer to the concept, but the foreground still lacks fully authored three-dimensional cut banks, strong parallax, varied erosion forms, and dense hand-placed vegetation/prop clusters. Similarity/legal and owner review remain open. |
| Start-festival, route banner, and crowd density | Bilateral stands, spectators, tents, and service props are closer to the route and legible in `working-tree-canyon-start-v4b.png`. The current 2026-07-19 textured-banner slice upgrades the 76 route-following vertical sponsor banners/poles outside the Canyon safety wall to four original canvas-textured variants with ridge/lightning motifs and visible festival colors. The route-crowd slice adds 22 route-following rail-bleacher groups with 198 enlarged spectators and 44 tiers outside gameplay space. Diagnostics were verified in frozen QA 500 m capture and the live `4173` preview. The clearest reviewed implementation frame is `/tmp/rivet-ridge-route-crowd-rails-bend-500m-clear-v2-1280x720.png`, compared directly against `concepts/gameplay-desktop.png`. | The repeated low-detail geometry and open midground remain less dense and less authored than the concept; banners and crowd are still procedural geometry/canvas textures rather than separate hand-authored fabric/signage/crowd assets, and owner/legal acceptance remains open. |
| Cooling-gate language | Runtime diagnostics report `per-lane-open-arch`; Rider School installs four arches and two-lap Canyon installs twelve. `working-tree-canyon-cooling-approach-v5.png` shows separate open lane arches, physical cyan frames, and snowflake cues rather than one opaque full-width wall. Focused serial Playwright assertions pass for both counts. | Emission, material depth, surrounding festival dressing, and action VFX remain below the concept; owner review is open. |
| Dirt, ruts, and safety treatment | Fresh captures show darker compacted dirt, paired ruts, clearer cream lane guides, a smaller red/cream edge wall that no longer dominates the track, and a new non-colliding red-rock shoulder band outside the drivable lanes. The current 2026-07-19 dirt-texture slice adds deterministic `layered-rut-pebble-v2` color/height treatment with 512×512 terrain maps, extra pebble flecks, worn rut highlights, and denser height clods; QA frozen Practice and live `4173` smoke both reported the new material diagnostics ready with no page or request errors, and `/tmp/rivet-ridge-dirt-texture-frozen-practice-1280x720.png` was inspected against the desktop concept. | The surface and barriers still repeat too uniformly and need richer authored route-following modules, stronger material breakup/normal detail, localized contact deformation, and owner/legal acceptance. |
| Hero rider and bike | The lower offset camera improves the rear silhouette and keeps the full rear wheel visible in desktop and portrait. The current authored asset verifier passes at 49,312 triangles / 511,736 bytes; the enriched v20 set technically proves all 15 required states for the current hero/material/dust runtime with authored Canyon kit/panorama ready, PMREM/contact-shadow diagnostics, `trail-burst-contact` dust readiness, and the complete forced hero fallback. The latest source pass improves tank/fender graphics, brake rotor/hub detail, rear shock coil/mount detail, chain rollers, radiator hoses, exhaust/fork hardware, handguard inserts, helmet goggle/vent framing, rider armor panels, knee/boot segmentation, side/rear number readability, material response, and crash/recovery dust readability while preserving the 28-render-primitive budget. | This remains the largest focal mismatch: proportions and material response are still blocky/flat versus the reference, rear framing still hides some pitch/mechanical nuance, landing/crash/recovery read weakly against the action reference, and the solid-color materials have no authored base-color/ORM/normal texture atlases. |
| Portrait composition and controls | `artifacts/visual-iteration/working-tree-canyon-portrait-final.png` keeps the four-lane route, gate line, Heat/readout, and all touch controls unobstructed. | This is emulated portrait evidence, not physical touch, safe-area, reach, or comfort acceptance. |
| Renderer cost | A 21-second headed hardware-backed Canyon High run on Apple M1 Max held seven of seven samples at 60 FPS, with synchronous frame work averaging 2.16 ms and peaking at 2.54 ms, draw calls averaging 241.43 and peaking at 301, zero dropped simulation time, and no page/console error. Quality preset and authored-Canyon reliability tests pass serially. | This is a dirty working-tree diagnostic, not a clean manifest-bound candidate measurement. The full five-venue and physical-mobile performance matrix remains open. |

The executed slice establishes that the current Canyon runtime is loadable, measurable, and materially improved. It does not establish that the illustrative concept target has been met.

### 2026-07-17 executed hero pass-v2 diagnostic slice

`artifacts/visual-review/hero-remodel-2026-07-17-qa-pass-v2/manifest.json` is a 10,268-byte format-1 `hero-motion-visual-review` manifest with SHA-256 `527327c298edcb2404bf7853611db3e7785336b4888723fabcea67f017a015de` and status `PASS`. Its six bound captures cover desktop authored neutral, grounded tuck with a measured `0.17` steering roll, grounded Reduced Motion, portrait authored neutral, portrait grounded tuck, and the complete desktop procedural fallback after a forced authored-asset request failure. The manifest explicitly identifies itself as dirty-working-tree QA evidence, not an immutable release or promoted baseline; the fallback capture's expected forced-request console errors are not treated as normal-path errors.

The focused hero-motion browser run passed 3/3, targeted reliability passed 4/4, and production smoke passed. Headed current-build diagnostics recorded clean desktop normal/stress profiles at 59.97/59.99 FPS with 2.2/2.6 ms p95 frame work, and mobile normal/stress profiles at 59.95/59.92 FPS with 1.6/1.9 ms p95 frame work. The release-level result remains `FAIL` solely because the working tree/source binding is dirty rather than frozen. These are meaningful technical results, but side-by-side review against the supplementary mounted-pose reference and current Blender contact sheet still finds blocky/flat proportions and materials, slight pose differentiation, and no current wheelie, airborne, landing, crash, or recovery visual matrix. Visual fidelity therefore remains **NOT ACCEPTED / OWNER UNVERIFIED**.

### 2026-07-17 executed hero action-state v3 diagnostic slice

`artifacts/visual-review/hero-motion-action-states-v3-headed-20260717t095050z/manifest.json` is a 114,426-byte format-3 `hero-motion-action-state-review` manifest with SHA-256 `79b9b19f441c7368e1ba495e7903c6a9e3c934eb3348b55a298cb0ab790cc47a` and status `PASS`. Its 15/15 public-UI captures cover neutral, tuck, both leans, wheelie, airborne up/down/neutral, clean landing, crash, recovery hold, recovering transition, Reduced Motion, portrait touch wheelie, and complete procedural fallback after a forced hero-GLB failure. Headed Chromium used browser-level muted audio; every normal capture requires `data-bike-asset`, `data-canyon-kit-asset`, and `data-environment-asset` to be ready, and every screenshot is bracketed by matching passive state snapshots without state injection, relocation, freeze, query setup, or baseline promotion.

The five-case headed hero action integration passes using real public controls. Technical state coverage is therefore complete for this dirty working tree. Visual comparison is still rejected: the rear camera hides front-wheel and engine detail, airborne pitch differences are subtle, landing compression is barely visible, the crash leaves bike/rider tangled and near-upright rather than clearly separated on the ground, recovery remains visually close to the crash, and materials/anatomy remain far flatter and blockier than the canonical and supplementary references. The manifest correctly records `conceptFidelityClaimed: false`, `visualAcceptance: PENDING_OWNER_REVIEW`, and `baselinePromoted: false`.

### 2026-07-18 executed hero action-state v12 diagnostic slice

`artifacts/visual-review/hero-motion-action-states-v12-current-20260718t192834z/manifest.json` is a 121,461-byte format-3 `hero-motion-visual-review` manifest with SHA-256 `2e155c59dc9d29aaa50c48e9532de7101fd5690bc7fdf5c0edd7c0c499b400c7` and status `PASS`. Its 15/15 public-UI captures covered neutral, tuck, both leans, wheelie, airborne up/down/neutral, clean landing, crash, recovery hold, recovering transition, Reduced Motion, portrait touch wheelie, and complete procedural fallback after a forced hero-GLB failure. This record is now superseded by v20 for the current mechanical-detail hero, lane-lean, and landing-pulse presentation code.

### 2026-07-18 executed hero action-state v20 diagnostic slice

`artifacts/visual-review/hero-motion-action-states-v20-current-20260718t-mechanical-detail-qamode/manifest.json` is a 121,832-byte format-3 `hero-motion-visual-review` manifest with SHA-256 `9a6beeb30396e57054d5f5aa4500ec32cd2d9f9ca8746eb48846f2f7cca24223` and status `PASS`. Its 15/15 public-UI captures cover neutral, tuck, both arrow-key lane-change leans, wheelie, airborne up/down/neutral, readable clean landing, crash, recovery hold, recovering transition, Reduced Motion, portrait touch wheelie, and complete procedural fallback after a forced hero-GLB failure. Headed Chromium used browser-level muted audio and an isolated `VITE_QA_MODE=1` server on port 4174; every authored capture requires `data-bike-asset`, `data-canyon-kit-asset`, `data-environment-asset`, PMREM material response, expected shadow style, and `trail-burst-contact` dust readiness to be ready, and every screenshot is bracketed by matching passive state snapshots without state injection, relocation, freeze, query setup, or baseline promotion. The manifest records visible public-button activation because Playwright browser actionability waits were unreliable during WebGL startup; no game state was injected.

The new hero source-pair UUID for the latest mechanical-detail pass is `793e14d9-b198-45c0-b1ee-c5c4b0748953`. The 2,353,516-byte raw GLB (`a81c7984442c3dcaa0b6dd5c4fba76994f3b66949e39e3abf779b7f5f965c976`) optimizes to the 511,736-byte required-Meshopt runtime GLB (`5537ba34639053a1b28b40978853ceade33f9d3ac40d2d2c477e59ce206c024a`) with 49,312 triangles: 39,984 bike including 14,428 wheel triangles, and 9,328 rider triangles. It retains 88 nodes, 28 mesh-bearing nodes, 28 render primitives, 10 opaque PBR materials, both wheel hooks, and six pose pivots. `npm run assets:build`, `npm run assets:verify`, `npm run build`, a non-QA live preview smoke, and the v20 headed action-state matrix passed for this dirty working tree.

Direct `view_image` comparison against the canonical production reference, latest source-review contact sheet, and live 1280×720 preview screenshot shows a real but incomplete improvement. The side/rear/front `22`, tank stripe/cap, rear-fender cream flash, exhaust bands/bolts, fork tubes/axle caps, brake rotors/hubs, handguard inserts, radiator hoses, rear shock banding, chain rollers, helmet goggle frame, rider armor panels, boot/knee segmentation, and raised side graphics now read better in source review and in the live racing camera. The current runtime also adds PMREM material response, contact-shadow diagnostics, a held lane-change lean for arrow-key readability, a longer presentation-only landing compression pulse, and a pooled `trail-burst-contact` dust style for rough landings, crashes, and recovery settle; the v20 manifest proves those states carry through the public-control matrix. Concept fidelity remains **NOT ACCEPTED** because the game asset still lacks the reference's textured material depth, shaped plastics, complex fabric/armor paneling, and accepted side-by-side runtime approval for material/contact/dirt interaction.

| Area | Post-RC1 change present in the working source | Evidence required for acceptance | Current result |
|---|---|---|---|
| First-frame camera and composition | Desktop and portrait profiles use separate field of view, height, trailing distance, aim, and rider scale. The camera now consumes the same renderer-only route sample as the road and riders, so the complete bike, four lanes, near hazards, and look-ahead must be reassessed in the changed composition. | Owner review of fresh post-route first-frame and midcourse desktop/portrait captures, followed by physical-device motion and sightline review. | **CURRENT CANYON DESKTOP/PORTRAIT CAPTURED; TECHNICAL SLICE PASS / NOT ACCEPTED** |
| Hero player bike and rider | The canonical project-generated production reference plus supplementary v2 mounted-pose and v3 action-state sheets guide an original Blender 4.5.11 LTS hero pair; no reference image is shipped or sampled into the model. The pinned authoring procedure scrubs machine-local Blender workspace residue before save. Source-pair UUID `793e14d9-b198-45c0-b1ee-c5c4b0748953` binds the current 2,353,516-byte raw GLB to a 511,736-byte required-Meshopt GLB (`5537ba34639053a1b28b40978853ceade33f9d3ac40d2d2c477e59ce206c024a`) with 49,312 triangles: 39,984 for the bike including 14,428 across both wheels, and 9,328 for the rider. It retains 88 nodes, 28 mesh-bearing nodes, 28 render primitives, 10 opaque PBR materials, both wheel hooks, and six pose pivots. The current 15,164-byte schema-v2 asset manifest (`1d75490d8b2af28b12c2b53fb9b85ba7b7d365c668f7c5e0ffbf8f32eb7997b7`) binds the canonical production reference, scrubbed source pair, primary preview, four fixed-angle panels, contact sheet, tooling, package, and pinned Basis inputs; production-art verification separately binds both supplementary sheets. Independent asset verification, headed action integration 5/5, and the enriched v20 15-state matrix pass their dirty-working-tree scopes for the current runtime. | Freeze one exact tracked candidate, then complete side/three-quarter maximum Ride/Turbo, wheelie/barrier clear, clean/hard landing, crash/recovery, Low/Medium/High, dark-venue, physical-device, and owner/legal review. Compare against the canonical, supplementary v2/v3, and gameplay references; inspect silhouette, contact points, helmet/armor, tires, mechanics, number readability, material response, shadows, draw calls, memory/frame time, and third-party resemblance. | **ASSET / ACTION INTEGRATION / V20 15-STATE CAPTURE PASS (DIRTY WORKING TREE); VISUAL FIDELITY NOT ACCEPTED / OWNER AND RELEASE UNVERIFIED** |
| Rival identity | An original Blender 4.5.11 LTS lower-detail base replaces the primitive field only after all five Rival/Mastery variants prepare successfully. Its pinned authoring procedure scrubs machine-local workspace residue and binds source-pair UUID `c7b02b0e-1ae0-46ef-abc9-73547f432967`. The 193,884-byte required-Meshopt GLB (`09043547981c80d66fca23aa208971b6b2f457890cafabeed4258dbc0fdbb805`) preserves 19,588 triangles, 26 nodes, 12 render primitives/shared geometries, five PBR materials, one embedded project-generated number field, two wheel hooks, and six rider-pose hooks. Runtime clones the base five times with shared geometry, fixed palettes, and generated `17`, `31`, `46`, `58`, and `73` number textures. Any failure leaves the detailed procedural field active. Its 6,661-byte schema-v2 manifest (`122518c2552685af9a9e9469368c232c503fd5d8c53de4973e0fc299d751329a`) binds source, previews, tooling, and package commands. `node scripts/verify-rival-pack-assets.mjs` passed these working-tree bytes on 2026-07-18 under Node v26.4.0/npm 11.17.0; the focused Chromium QA runtime gate passed 2/2 on 2026-07-18 for authored five-variant install and hard-failure fallback. | Repeat the verifier after freezing one exact tracked candidate, then capture fresh desktop/portrait Rival and Mastery frames and live-review every rival at start, pack, jump, lane-change, crash, and Reduced Motion states; verify number orientation/legibility/uniqueness, palette separation, joint/contact attachment, shadows, atomic fallback, player-versus-pack quality balance, draw calls, memory/frame time, and third-party resemblance. | **ASSET VERIFIER / FOCUSED RUNTIME 2/2 PASS (DIRTY WORKING TREE); VISUAL FIDELITY NOT ACCEPTED / PERFORMANCE, OWNER, AND RELEASE UNVERIFIED** |
| Venue atmosphere | Canyon, Pine, Coastline, Foundry, and Summit retain separate fog/light/exposure profiles and now add quality-bounded density/signature treatment: four/five/six-row start stands with 56/90/132 spectators; merged service props; bump-aligned Pine roots and red markers; cyan/orange Foundry gantries plus visible opaque furnace panels; bilateral Summit finale equipment; and length-aware capped clutter. A follow-up source audit found hidden/intersecting terrace placements; the source now moves these props onto explicit visible tiers and keeps tents in protected route corridors. Pre-route captures made the five venues distinct but do not show this source. | Owner review of fresh post-route start and midcourse captures for all five tracks, including terrace contact, prop visibility, route clearance, quality-tier density, draw calls, and frame time. | **CURRENT CANYON CAPTURE/PERFORMANCE SLICE PASS; OTHER FOUR VENUES AND ACCEPTANCE OPEN** |
| Canyon festival horizon | A new 1774×887 original generated matte uses the accepted desktop/mobile gameplay concepts only as style/composition references and contains layered red-rock mesas, sky/clouds, teal festival stands/tents/towers, flags, and sparse vegetation with no road, rider, UI, logo, visible text, watermark, or obvious third-party branding found in source review. Canyon Kickoff and Rider School load it as a quality-tier 2D sRGB background with responsive centered cover cropping, a 12-second visual-art deadline, owned bitmap/texture disposal, and the generated sky as fallback. Editor-authored Test Rides now explicitly exclude it despite inheriting Canyon's visual identifier. It is presentation-only, precached in `shell-v35`, and protected by exact byte/PNG/C2PA-marker verification. Current non-QA preview smoke observed the authored environment ready. | Fresh desktop/portrait Canyon first-frame and midcourse comparison against both gameplay concepts; inspect crop, horizon placement, seam/stretch absence, 3D course separation, lane/hazard contrast, fallback, authored-Test-Ride isolation, memory/frame time, and human similarity/trade-dress concerns. | **SOURCE ART REVIEWED; CURRENT RUNTIME TECHNICALLY READY / NOT ACCEPTED / UNVERIFIED** |
| Authored Canyon modular kit and PBR lighting | An original production reference drives a versioned Blender 4.5.11 LTS source kit with 11 stable roots. The pinned authoring procedure scrubs machine-local workspace residue and carries authoring-script SHA-256 plus source-pair UUID `6d3823dd-f118-42fb-8bed-6cb2e3b2becf` through the editable source, raw GLB, 427,028-byte required-Meshopt runtime GLB (`283be67d91cce4b0dc7b0f7e291811c62c77384f5e3f226cca71d41ce890ff7d`), and 4,909-byte manifest (`7da4a7291711f0ca2d3ff4099cac50d6134599c5dfab3e5a050c435511208653`). The 32,008-triangle/20-material source is reduced from 264 editable meshes and a 2,310,368-byte raw GLB to 62 production meshes without changing coverage. Current source loads it only for handcrafted Canyon/Rider School, retains procedural gameplay/fallback authority, and precaches it in `shell-v35`. Successful install covers every gate, barrier, and preferred medium-ramp shell across every lap. `node scripts/verify-canyon-assets.mjs` and the full `npm run assets:verify` chain passed these working-tree bytes on 2026-07-18 under Node v26.4.0/npm 11.17.0; strict type/build, runtime, visual, and performance gates remain open. | Repeat the asset gates after freezing one exact tracked candidate, run strict type/build checks, then capture fresh desktop/portrait Canyon start, barrier, ramp, both cooling gates, and midcourse frames at Low/Medium/High. Compare against gameplay and production-reference art; inspect scale/orientation, clipping, duplicate procedural cues, emission/transparency, shadows, material response, asset-failure fallback, custom-course exclusion, offline precache, draw calls, GPU memory, frame time, and owner/legal similarity acceptance. | **AUTHORED / OPTIMIZED / WIRED IN SOURCE / VERIFIER AND FULL ASSET CHAIN PASS (WORKING TREE); RUNTIME NOT CAPTURED / NOT ACCEPTED / UNVERIFIED** |
| Course grounding and depth | The shared `CoursePresentationRoute` supplies C2 lateral bends and positive rolling grades between protected campaign obstacle/readability corridors, with Summit carrying the strongest bounded rise. The current source also adds schema-v2 checkpoint-authored smootherstep centerlines for Builder and Test Ride, with zero start/finish seams and scalar arc-length preservation. Its X/Y/Z derivatives provide yaw/pitch/up frames to route ribbons, modules, riders, camera, and effects without becoming gameplay authority. | Fresh desktop, portrait, high-contrast, tutorial, five-track, Builder, and Test Ride review at racing speed; verify coherent road/object alignment, four-lane and landing readability, clean lap seams, curved-surface placement, and legacy-flat preservation. | **BUILT; NOT CAPTURED / NOT ACCEPTED / UNVERIFIED** |
| Track Builder authored route | Schema v2 stores optional checkpoint-only Route turn/rise anchors; three examples and the new draft use safe nonzero anchors. The Builder constructs one sampled dirt/shoulder/lane surface on a route-sized floating island, places modules in its local frame, and follows it with the camera. Local and overview zoom are independent; **Fit route** uses orbit-safe world bounds that include raised modules. Adaptive raycaster-only pick geometry maps pointer placement, and a bounded visible/pickable continuation follows focus anywhere in the accepted 0–20,000 m range without constructing a permanent 20 km slab. Test Ride consumes the same typed centerline while fixed-step progress, contacts, gates, AI, timing, and replay remain scalar. Omitted anchors and v1 tracks remain exact identity routes; legacy v1 tracks needing the stricter full-parent-footprint repair stay editable without quarantine. | Fresh `track-builder.png` side-by-side Builder and Test Ride captures; route-control undo/redo, save/reopen/import/export, legacy repair, full-range guide/picking, Fit route under arbitrary orbit/aspect, raised modules, all rotations, shaped-surface placement, multi-lap seam, high-contrast, browser errors, performance, and owner review. | **BUILT; TESTS NOT RUN / NOT CAPTURED / NOT ACCEPTED / UNVERIFIED** |
| Dirt material and lane silhouette | The working source no longer reuses its sRGB dirt/paint texture as bump data. It now generates a separate deterministic, non-color, seamless height canvas for compacted soil, paired wheel ruts, and clods; color and height share periodic rut paths, and painted lane guides are explicitly flattened in the height layer. Three shallow instanced trapezoidal grass berm runs replace the former thin box strips in campaign/tutorial presentation without becoming collision or simulation geometry. Authored courses omit those generic berms so saved curve/bank meshes are not crossed by straight rails, and campaign internal high-contrast guides clear the maximum berm top. Custom-course high-contrast overlays follow saved curves, banks, heights, rotations, and laps through geometry-signature instancing. Their straight underlay gaps are selected independently in world space for each stripe, preserving fallback cues for buried, rotated, and negative-height cases. No standalone surface image or external asset was added for this material pass. The latest permitted normal build typechecked and compiled this source, but no runtime, visual, accessibility, custom-track, or performance qualification was run. | Fresh desktop/portrait near-field and long-sightline captures across all five tracks plus High-quality shadow and lower-tier checks; review rut alignment/scale, repetition, mip shimmer, lane-paint/hazard contrast, apparent-vs-authoritative surface clarity, memory/frame-time impact, campaign guide clearance, and authored straight/curved/banked/rotated/negative-height presentation. | **BUILT; NOT CAPTURED / NOT ACCEPTED / UNVERIFIED** |
| Scenery and safety treatment | Canyon retains bilateral staffed cooling-gate watchtowers, outward-rising route-pocket tiers, the continuous cream/coral/teal safety wall, and same-batch branched cacti. The broader atmosphere pass adds the separate quality-bounded start crowd/service treatment and venue signatures summarized above. Source-only review corrected furnace-panel occlusion, Summit equipment burial, start-stand/tent terrace crossings, service-cluster tier crossings, and shelf-buried clutter before the latest build. All additions remain project-authored code-native geometry; they do not enter collision, cooling, AI, timing, or replay. | Fresh Canyon/Rider School cooling-gate and start captures plus five-track live review. Confirm deck/crowd/prop ground contact, unobscured lanes and landing reads, no hidden or floating geometry, authored exclusion, and measured draw/frame budgets. | **BUILT; TESTS NOT RUN / NOT CAPTURED / NOT ACCEPTED / UNVERIFIED** |
| Runtime obstacle/module fidelity | Runtime rendering now preserves saved `moduleId` silhouettes for Tabletop, Double Jump, Jump Chain, Trail Bump, Bump Row, Offset Barriers, and Sky Kicker. One pure resolver supplies exact ordered sections to both player and AI; the visual parent is drawn once, dirt gaps/staggered lane openings remain distinct, sideways rhythm modules retain one route footprint, and offset-barrier planning uses the parent lane union. Four Bump Row penalties are normalized so the complete row retains the prior overall speed factor instead of compounding to 24%. | Focused resolver/rules tests, every-module Test Ride captures at 0/90/180/270°, player/AI behavior review, landing/readability review, and comparison with the editor preview. | **SOURCE AND REGRESSION SOURCE BUILT; TESTS NOT RUN / NOT CAPTURED / NOT ACCEPTED / UNVERIFIED** |
| HUD and mobile controls | The diagnostic input badge is now screen-reader-only; lane and pitch are separate sculpted rockers; Ride/Turbo use distinct teal/coral pods; and all six buttons use an original cream rally pictogram system. Ride/Turbo keep visible text, the pictograms are hidden from the accessibility tree, and the existing named buttons, hitboxes, safe areas, and mirrored meaning remain the interaction contract. Across 320–780 px, viewport/pixel caps bound the position, timing, target, pause, rocker, throttle, icon, padding, and Heat geometry while preserving at least 44 px button targets; deferred 320/621/780 px checks cover horizontal and vertical normal/mirrored separation at 140% scale. The Rider School panel has viewport-derived short-phone bounds, owns vertical touch scrolling without applying gesture suppression to the entire shell, and uses shrink-safe 12-column lesson markers with fixed pixel gaps. Pause dialogs stack above the transient `GO` card. Native hold controls expose `aria-pressed`, lane controls emit one-shot pulses, pitch toggles remain exclusive, and Recovery exposes a bounded labeled progressbar. | Fresh portrait capture against `concepts/gameplay-mobile.png`, plus the unrun 320/621/780 px, 320×568 active-lesson/actions, overlay-stacking, and semantic-control assertions; physical-device reach, visible-label and accessible-name checks, safe areas, mirrored layout, track visibility, high contrast, and gamepad/touch prompt review. Emulation alone does not prove comfort. | **IMPLEMENTED IN WORKING SOURCE; TESTS NOT RUN / NOT CAPTURED / NOT ACCEPTED / UNVERIFIED** |
| Deterministic display typography | Two official Google Fonts Barlow Condensed Latin WOFF2 subsets (700/900) replace optional local display-font lookup. DOM chrome and runtime CanvasTexture labels name the same `Ridge Display` family; the files are preloaded, precached in `shell-v35`, pinned by bytes/signature/SHA-256, inventoried under SIL OFL 1.1, and included in generated notices. The latest permitted build verified the build-time font and OFL contracts; the visual harness now requires both weights before it creates frozen race textures, but that harness was not run. | Fresh desktop/portrait/title/editor capture across Chromium, Firefox, WebKit, installed browsers, Android, and iOS; inspect actual computed family/weight, clipping, wrapping, CanvasTexture labels, load/failure behavior, offline availability, and concept hierarchy. | **BUILT / SOURCE PROVENANCE COMPLETE; NOT CAPTURED / NOT ACCEPTED / UNVERIFIED** |
| Start-grid and portrait framing | Campaign tracks and Rider School now receive original code-native lane numerals `1–4` plus a transverse cream/coral/teal start treatment in two course-anchored, presentation-only instanced batches. Authored Test Rides omit both batches and expose exclusion diagnostics. Resize and render share one pure camera predicate, choosing portrait framing for portrait orientation—including portrait tablets—or the retained sub-680 px narrow fallback. | Fresh desktop, portrait-tablet, portrait-phone, and authored-Test-Ride captures; confirm lane-number order, start-line readability, course anchoring, zero custom-course leakage, camera sightline, HUD/control clearance, and diagnostic values. Focused predicate source exists but was not run. | **BUILT; TESTS NOT RUN / NOT CAPTURED / NOT ACCEPTED / UNVERIFIED** |

These changes address specific gaps visible in the rejected RC1 comparison. They are not themselves proof that the concepts have been met, and this ledger does not pre-approve their exact tuning.

## Current mismatch ranking

The 2026-07-17 native-size side-by-side audit keeps concept fidelity open in this order:

1. The remodeled focal hero now has a technically complete v20 15-state public-control matrix and better number/panel/chain-drive readability, brake rotor/hub detail, rear shock/chain detail, PMREM material response, contact shadows, lane-change/landing readability, and crash/recovery dust readiness, but its blocky proportions, flat solid-color material treatment, rear-camera occlusion, and unaccepted crash/recovery silhouettes remain the largest mismatch. It still needs a stronger original modeling/material pass, likely including authored texture/normal/ORM detail, plus side/three-quarter gameplay evidence and Rival-pack review.
2. The Canyon panorama, route bend, camera, dirt, edge wall, festival start, and lane-open cooling gates now read together in fresh desktop and portrait frames. The playable foreground and midground still look like a repeated procedural corridor rather than authored route-following terrain with cut banks, shelves, berms, stands, vegetation clusters, and parallax.
3. Camera and atmosphere now show the complete bike and more track depth while retaining readable hazards. Contact depth, softer dust/action particles, material response, restrained grading, landing/wheelie bursts, and richer lighting have improved, but still need fresh accepted runtime captures before they can close the concept gap.
4. Canyon start density is materially improved and the live High slice stays within the measured working-tree renderer budget, but the other four venues have no equivalent current-byte start/midcourse comparison or renderer measurement.
5. The new lane-open cooling treatment removes the opaque crash-wall reading and passes exact runtime-count assertions. It still needs owner review of emission, readability at race speed, and physical-device motion.
6. The portrait frame retains the HUD/control hierarchy and clear track sightline, but emulation does not prove touch comfort, safe areas, or physical mobile performance.
7. V7 now captures Reduced Motion, touch wheelie, all airborne states, landing, crash/recovery, and complete procedural fallback. The shared authored five-rival pack, stronger visible pose separation, and accepted owner comparison remain outside the qualified slice.
8. The winding/graded Builder/Test Ride centerline remains outside this fresh capture slice; the historical Track Builder acceptance is still reopened.

These are acceptance gaps, not optional polish. The latest clean-source controlled capture for `a9a97ef` passed 11/11 with manifest SHA-256 `5866dc0888245224a2e2c85f5883b3dfe07c5300b8d9034fe802ab5775761610`, and direct inspection of `artifacts/visual-review/rc2-current-a9a97ef-20260719t082620z/curved-baseline-candidate/canyon-kickoff-practice-1280x720.png` against `docs/design/concepts/gameplay-desktop.png` shows a coherent, readable Canyon frame. It still remains visibly flatter and less textural than the concept in track-surface depth, hero material detail, and environmental richness, so visual fidelity remains **NOT ACCEPTED** until the owner reviews it side by side and every release-bound condition is met.

## Retained pre-route scripted capture evidence — manually reviewed, not accepted

Before the final nonvisual audio-unlock and tutorial grass-exit corrections, scripts launched installed Google Chrome 150.0.7871.115 headlessly at fixed 1672×941 and 390×844 viewports. First-frame and midcourse desktop/mobile captures are retained under the ignored `artifacts/visual-review/rc2-manual-latest/` path. A second scripted session retained start and midcourse captures for Canyon Kickoff, Pine Run, Coastline Clash, Foundry Flight, and Summit Showdown at 1672×941 under `artifacts/visual-review/rc2-manual-five-track-latest/`. The PNGs were then manually reviewed. Across those capture sessions, no console error, page error, failed request, or HTTP failure was recorded. These sessions predate `CoursePresentationRoute` and therefore provide no browser or performance evidence for the current route renderer.

The retained captures show the complete articulated rider/bike silhouette, `22` panel, visible wheels and hubs, dedicated thin rear fender, wider camera, the now-retired presentation-only distant S-curve vista, a straight near-field presentation, and a visibly distinct treatment for each venue. They do not show the new shared curved-course presentation. They also show that surface, material, crowd, vegetation, and prop density remained below the illustrative concepts. The 390×844 viewport is emulation, not physical-device evidence. These files are transient local review material, not durable release evidence or part of the authoritative asset inventory in `ASSET_LICENSES.md`; no screenshot is promoted to a regression baseline and no owner acceptance is inferred.

The in-app Browser connector could not initialize, so the capture scripts used the installed Chrome executable directly rather than the Browser surface. They used Playwright's library for scripted navigation but not the paused Playwright test runner. They do not establish final-byte runtime qualification, headed/native-device rendering, cross-browser, physical-device, performance, high-contrast, or automated regression results.

## Retained controlled RC2 visual evidence — obsolete-baseline comparison

Two independent controlled runs exercised the revised visual-regression harness against the same earlier RC2 working snapshot. The harness froze a live race at a named QA state without entering the paused UI or applying its dimming filter, forced the `High` quality preset, waited for `document.fonts.ready` and the compressed bike's `ready` state, and kept desktop race, editor, portrait race, and high-contrast coverage in separate tests. These controls removed the prior wall-clock, Auto-quality, asynchronous-asset, pause-filter, and test-short-circuit ambiguities for that snapshot.

The first race run is retained under `artifacts/visual-review/rc2-controlled/`; its independent repeat is under `artifacts/visual-review/rc2-controlled-repeat/`. Each failed race directory contains the Playwright `actual`, obsolete `expected`, and `diff` PNGs, its error context, and a video. Both race `.last-run.json` files record the same three failures. The independently repeated editor passes are recorded under `artifacts/visual-review/rc2-editor-controlled/` and `artifacts/visual-review/rc2-editor-controlled-repeat/`.

| Surface | Controlled RC2 result against untouched RC1 baseline | Repeatability | Repository evidence |
|---|---|---|---|
| Desktop race | **FAIL against obsolete baseline:** 558,095 pixels differ, ratio `0.61`, above the `0.02` allowance. This delta represents the deliberately changed camera, atmosphere, terrain depth, scenery, safety treatment, rider, and HUD copy rather than acceptance of the old composition. | The two actual PNGs are byte-identical: SHA-256 `c4299018e2b41f26f43914fa3f98c63b87f5195d578c4bd7ec8d53438a0aa269`. | `artifacts/visual-review/rc2-controlled/visual-regression-desktop--78d62-pted-production-composition-chromium/` and `artifacts/visual-review/rc2-controlled-repeat/visual-regression-desktop--78d62-pted-production-composition-chromium/` |
| High-contrast race | **FAIL against obsolete baseline:** 353,688 pixels differ, ratio `0.39`, above the `0.02` allowance. The controlled actual retains the strengthened HUD vignette, lane/edge guides, cooling snowflakes, and non-color hazard silhouettes, but still requires explicit review as part of the changed composition. | The two actual PNGs are byte-identical: SHA-256 `fb5996fa720a3c81b614fc04980a96c1c2e1605c0da87b21ff1152adcc670b42`. | `artifacts/visual-review/rc2-controlled/visual-regression-high-con-77f96--retains-readable-hierarchy-chromium/` and `artifacts/visual-review/rc2-controlled-repeat/visual-regression-high-con-77f96--retains-readable-hierarchy-chromium/` |
| Portrait race | **FAIL against obsolete baseline:** 121,374 pixels differ, ratio `0.36`, above the `0.02` allowance. The changed camera, venue density, rider framing, visible Heat label, and larger touch controls intentionally invalidate the superseded compact composition. | The two actual PNGs are byte-identical: SHA-256 `a5a6173bd4e2874800cd2eb59c4427ed260f49792c96a7080775cc09bd91e3bd`. | `artifacts/visual-review/rc2-controlled/visual-regression-portrait-2e235--accepted-touch-composition-mobile-chrome/` and `artifacts/visual-review/rc2-controlled-repeat/visual-regression-portrait-2e235--accepted-touch-composition-mobile-chrome/` |
| Track builder | **PASS:** the separate editor test matched `e2e/visual-regression.spec.ts-snapshots/editor-screen-chromium-darwin.png` in both controlled runs. Playwright produced no failure attachment for the passing surface. | Two independent passes; separating the editor test ensures a race failure no longer prevents this evidence from executing. | `artifacts/visual-review/rc2-editor-controlled/.last-run.json`, `artifacts/visual-review/rc2-editor-controlled-repeat/.last-run.json`, and the checked-in editor baseline above |

No file under `e2e/visual-regression.spec.ts-snapshots/` was replaced. The `expected` images copied into the failure directories are the obsolete RC1 baselines and remain comparison history, not an approved RC2 target. New race baselines may be accepted only after explicit owner review; byte-identical captures prove determinism, not concept fidelity.

The black wedge that appeared when one high-contrast capture was viewed through an image decoder is not present in the clean source PNG; it is a decoder/display artifact and not a recorded game-rendering defect. The retained 39% high-contrast comparison still fails because its frozen composition differs from the obsolete baseline. That stale comparison remains unresolved until an accepted current composition receives a fresh controlled run.

The retained controlled RC2 composition addressed brightness, canyon/festival density, course-edge depth, rider framing, four-lane hierarchy, cooling-gate prominence, and mobile control presence, but that pre-route snapshot remains visibly flatter and straighter, with substantially less surface, vegetation, rider, crowd, and prop detail than the illustrative concepts. It cannot qualify the changed renderer. That remaining difference is not waived by determinism and keeps visual acceptance open.

## Five-track identity review

Manually reviewed scripted start and midcourse captures for all five venues exist under `artifacts/visual-review/rc2-manual-five-track-latest/`. They provide scoped pre-route headless installed-Chrome comparison evidence that the venue treatments were visually distinct; they do not show `CoursePresentationRoute` and do not establish current-byte runtime qualification, owner acceptance, headed/native-device rendering, cross-browser consistency, physical-device behavior, performance, or parity with the illustrative concept density. The historical RC1 captures elsewhere under `artifacts/visual-review/` remain superseded comparison evidence.

| Track | Current acceptance focus | Result |
|---|---|---|
| Canyon Kickoff | Warm red-rock mesas, terraced canyon edges, festival treatment, readable cooling rhythm, and an open long sightline are distinct in the retained pre-route capture. | **SCOPED DISTINCTION SHOWN; NOT ACCEPTED / UNVERIFIED** |
| Pine Run | Cooler grey-green terraces and pine/timber treatment distinguish the route while retaining lane clarity; density and material finish remain below the concept. | **SCOPED DISTINCTION SHOWN; NOT ACCEPTED / UNVERIFIED** |
| Coastline Clash | Bright sand, teal open-water treatment, and coastal huts/canopies distinguish the venue and preserve readable heat/jump routes. | **SCOPED DISTINCTION SHOWN; NOT ACCEPTED / UNVERIFIED** |
| Foundry Flight | The dark industrial palette, stacks/gantries, furnace warmth, and safety treatment distinguish the venue, though the captured industrial density remains sparse relative to the concept. | **SCOPED DISTINCTION SHOWN; NOT ACCEPTED / UNVERIFIED** |
| Summit Showdown | Violet dusk, pale snow-capped mountain silhouettes, and high-risk obstacle treatment distinguish the finale from Canyon and Pine. | **SCOPED DISTINCTION SHOWN; NOT ACCEPTED / UNVERIFIED** |

## Acceptance criteria and constraints

Renewed concept-fidelity acceptance requires:

1. Owner side-by-side review of the desktop and portrait gameplay concepts against fresh post-route first-frame and midcourse captures, followed by physical-device review at the intended aspect ratios.
2. Fresh post-route high-contrast evidence plus owner review of new five-track start/midcourse captures, with the old RC1 and pre-route RC2 evidence retained as history.
3. Recorded review of camera composition, environmental density, terrain/course depth, atmosphere, rider silhouette, HUD hierarchy, hazard readability, and mobile control hierarchy.
4. Confirmation that greater richness preserves critical lane and landing reads, non-color cues, responsive layouts, quality presets, bundle limits, and recorded desktop/mobile performance.
5. Explicit owner acceptance of the resulting compositions before new screenshots become regression baselines.

The following remain technical/product constraints, not visual-fidelity waivers:

- Production rendering remains direct Three.js/WebGL with original, bright, chunky low-poly forms. The original Canyon matte may support far-field depth, but it cannot substitute for readable live 3D lanes, hazards, riders, scenery, or venue structure; the goal remains concept-aligned richness and composition rather than photorealism or reuse of the concept pixels themselves.
- Campaign `CoursePresentationRoute` shaping is laterally curved, positively graded, and renderer-only. Authored custom-track test play now uses schema-v2 checkpoint centerlines through the same presentation boundary; migrated v1 tracks stay identity-mapped. Saved local module transforms/collision footprints remain editor data while authoritative progress stays scalar as recorded in `ARCHITECTURE.md`.
- Physical-device brightness, display calibration, touch ergonomics, and motion comfort remain hardware-specific evidence gaps tracked in `QA_REPORT.md`.

## Current conclusion

Concept fidelity is **NOT ACCEPTED**. The 2026-07-19 clean-source controlled visual capture for `a9a97ef` passes the 11-frame five-track technical matrix, and the 2026-07-17/18 diagnostic slices add desktop start, cooling approach, bend, portrait, headed live-performance, and 15-state hero evidence. These are real technical results, but no regression baseline was created or promoted. The hero/rider's blocky/flat finish, lack of authored texture/normal/ORM material depth, rear-camera occlusion, subtle pitch/landing presentation, and crash/recovery fidelity remain material gaps, and the current Canyon frame is still less textural and densely authored than `gameplay-desktop.png`. Builder/Test Ride, high contrast, physical devices, frozen-candidate performance, similarity/legal review, and owner acceptance remain open. No launch-readiness claim is made here.
