# RIVET RIDGE RALLY — Launch Readiness

# NOT READY

**Decision date:** 2026-07-18

**Candidate:** `1.0.0-rc.2` source snapshot; no annotated release tag or release-bound rebuild

**Code-owned gate status:** UNVERIFIED

**Commercial readiness status:** NOT READY

**Authority:** `QA_REPORT.md`

**Decision owner:** Unassigned

Player feedback reopened default lane controls, first-play training, and visual fidelity after the immutable `v1.0.0-rc.1` predecessor. The changed work is therefore a new `1.0.0-rc.2` candidate; the predecessor's tag, artifact, and evidence remain historical and do not qualify these bytes.

An earlier `rc.2` snapshot produced the retained static, unit, Chromium, campaign, selected cross-engine, performance, soak, manifest, and installed-Chrome results listed below. Those results remain historical evidence for their exact recorded scopes and bytes. Current working-tree qualification passes focused Chromium functional, reliability, tutorial, editor, input, migration, persistence, and quality paths; campaign modes 18/18 in 27.7 minutes; current rival pack 2/2 on isolated port 4176; Firefox plus WebKit desktop functional 10/10; WebKit lifecycle 6/6; the emulated touch matrix 6/6; and emulated touch tutorial intro 2/2. On 2026-07-19, product files unchanged from `9a8abdd68090b9b7b6a616b889aa410080ec3c9e` passed the fresh working-tree gate batch `npm run assets:verify && npm run typecheck && npm run lint && npm test && npm run audit && npm run audit:release-scope`, immediately before the documentation-only evidence update; that includes 300 Vitest tests, 45 release-manifest fixtures, 71 release-attestation fixtures, 34 production-smoke/service-worker/release-scope fixtures, zero high-severity audit vulnerabilities, and release-scope audit PASS across 131 files / 15,208,175 bytes with aggregate SHA-256 `463b7d9e36d6c071efaf3c26711f05552e1aef8170033fa4dcf4d2b62028a240`. The current `npm run build` and a scoped non-QA live preview smoke also pass. The latest heat-tuning slice also passes focused simulation/Rider School tests after adding a slower critical-band turbo heat climb so the player gets more reaction time after the warning marker without moving the tutorial cooling gate. The headed diagnostic meets its numeric frame/FPS thresholds and a four-minute headed Rival calibration completes its scoped workload, but the performance artifact remains overall `FAIL` for dirty source/runtime binding. These are working-tree diagnostics; no qualifying clean-candidate performance, 30-minute soak, format-2 manifest, production/offline smoke, rollback, attestation, approval, or physical-device pass is claimed.

The current 2026-07-19 graphics revision includes the original Blender Canyon kit, authored hero and rival bike/rider assets, bounded loading/fallback, quality-tier placements, PMREM PBR lighting, and `shell-v35` precache source. The latest hero pass adds larger gameplay-distance saddle ribs, grip-end flashes, boot toe/latch accents, and a visor split while retaining the prior brake-rotor raised slots and hub bolts, rear shock coil bands and mount bolts, interleaved chain rollers, fork-top adjuster caps/slots, tank/fender graphics, exhaust/fork hardware, radiator hoses, handguard inserts, rider armor panels, boot/knee segmentation, and helmet goggle/vent framing. It preserves the no-texture solid-color PBR contract; tread repetition was reduced within the wheel contract so the bike remains under its 40,000-triangle cap. The hero contract now records 49,780 triangles: 39,912 for the bike including 14,284 across both wheels, and 9,868 for the rider. Its 517,664-byte Meshopt GLB has SHA-256 `538be4269927544fa91cfc405742523a60422d77d546ce9c8859f407deb2e6e8`; the 15,165-byte schema-v2 manifest has SHA-256 `7622e9ceeabc7db2861049cb12c969e6e73e9149799b9ab4018cf11c18cb4b2a` and source-pair UUID `31000dd4-2c42-4c74-ad5d-28b5e4aec32f`. `npm run assets:build`, `npm run assets:verify`, and `npm run build` pass for that hero, the rival contract, Canyon at 32,008 triangles / 427,028 bytes, and generated production art/provenance references. These are working-tree scoped results only; visual acceptance, offline, qualifying performance/soak, immutable-source, and owner/legal gates remain pending or `UNVERIFIED`. The revision therefore does not change **NOT READY**.

The canonical current working-tree hero visual diagnostic is `artifacts/visual-review/hero-motion-action-states-v20-current-20260718t-mechanical-detail-qamode/manifest.json`: 121,832 bytes, SHA-256 `9a6beeb30396e57054d5f5aa4500ec32cd2d9f9ca8746eb48846f2f7cca24223`, with all 15 required action states and manifest status `PASS`. The headed Chromium run used public UI flow, passive snapshots from an isolated `VITE_QA_MODE=1` server on port 4174, browser-level muted audio, and visible public-button click events, and required the authored hero, Canyon kit, panorama, PMREM hero material response, contact-shadow style, and `trail-burst-contact` dust contract to report ready; only the final fallback frame deliberately failed the hero GLB request and therefore waives authored-hero material/shadow diagnostics while still requiring the fallback path. That status proves the scoped technical capture contract for the current arrow-key lane-lean, landing-pulse presentation code, and mechanical-detail hero asset. The evidence is dirty-working-tree, nonbaseline diagnostic material with `conceptFidelityClaimed: false`, `visualAcceptance: PENDING_OWNER_REVIEW`, and `baselinePromoted: false`; it is not a promoted regression baseline or owner acceptance. Concept fidelity remains **NOT ACCEPTED**, and owner/legal review remains open.

The current controlled five-track visual-candidate path now passes its technical capture gate for clean source commit `8882c332671a476ff3a14e9777aa367f8bb9a6ca`, immediately before this documentation-only evidence update. On 2026-07-18, `npm run visual:candidate` correctly rejected the dirty tree; a later clean-branch attempt exposed and corrected an overbroad binary `ghp_` prefix false positive inside the Canyon GLB. The corresponding source fixtures now pass with `npm run test:release-manifest`: 45/45, including the pre-tag visual-candidate fixture, hostile ignored-input cases, and the short-token-prefix regression. The latest clean run produced a 33-file / 8,003,622-byte visual QA candidate with aggregate SHA-256 `5fadbb3d5b3108ca0c33759a7af3881db1c5767a584cd7cfc1a8c191eb1b32e0` and candidate manifest SHA-256 `c6b600228c6f314ddeb1571f72288be689ab33f6fb389ef4e8a513ccfd8c1087`; `npm run visual:capture -- --output-dir artifacts/visual-review/rc2-five-track-controlled-8882c33 --port 4373` passed 11/11 with manifest SHA-256 `054f3b983dd242baf461c68f640489754616e3ae2a9d9746d5c0e7e2f1390c09`. This remains technical visual evidence only; owner/legal acceptance and baseline promotion remain pending.

The Canyon provenance hardening intentionally changed source/raw/runtime bytes by adding the authoring-hash and source-pair chain. Its current 4,909-byte schema-v2 manifest has SHA-256 `8add9e79e1b84d0c48fdc654cb4d84034e609613b4e44e563fac17810a2de7e9`. The isolated optimizer, independent verifier, full asset chain, focused readiness/failure runtime paths, and campaign modes 18/18 pass; the working-tree performance diagnostics meet their numeric thresholds but remain nonqualifying. Accepted visual composition, release performance, and immutable-candidate qualification remain **UNVERIFIED**.

The 2026-07-17 Canyon comparison slice now has fresh ignored working-tree captures for desktop start, cooling approach, route bend, and portrait, plus a headed Apple M1 Max / Metal High sample. The runtime reports four open lane arches in Rider School, twelve across two-lap Canyon, and 62 Canyon-kit placements; focused serial quality and reliability cases pass. Seven of seven live samples hold 60 FPS, synchronous frame work averages 2.16 ms and peaks at 2.54 ms, draw calls average 241.43 and peak at 301, and dropped simulation time plus recorded page/console errors remain zero. This proves a technically healthy local Canyon slice, not an accepted visual baseline, clean-candidate performance result, production smoke, or owner approval. It does not change **NOT READY**.

All three current schema-v2 asset manifests statically bind the current 3,210-byte `package.json` (SHA-256 `9a73e0560628a87f863d9e5ab7ddc4168d5cd3ecdcf0ef99e0f9c9ff92bff2d4`). The asset inventory also separates the installed KTX-Parse v1.1.0 license source from Three's embedded v1.0.1 runtime identity and describes the checked-in complete Apache-2.0 Basis license text without falsely claiming upstream byte identity. Working-tree independent asset verification and distribution notices pass; immutable-source qualification and legal acceptance remain pending or **UNVERIFIED**.

Later read-only review found and source-corrected duplicate procedural/authored structures, incomplete lap/barrier coverage, an arbitrary tabletop placement, stale disposed-engine diagnostics, and premature Canyon capture readiness. Current source maps and atomically replaces every applicable obstacle shell across all laps, preserves cooling snowflake cues, anchors the tabletop to the preferred medium ramp and its resolved contact length, sizes each cooling gate from its lane envelope and authored clear span, rolls back partial visual commits, guards/clears metrics, and requires both Canyon assets before concept capture. Focused readiness/failure paths, typecheck, the isolated latest-source production compile, and campaign modes 18/18 now pass; visual/performance qualification and owner acceptance remain pending.

The retained format-1 release manifest at `artifacts/history/release-manifest-1.0.0-rc.2-earlier-snapshot-format-1.json` records 21 files, 4,689,233 bytes, and aggregate SHA-256 `6a55c00ea36debe543ab946dee06dc5fa73cf8a13d45b047aec399469c9931a3`; it does not match current `dist/`. The 2026-07-15 10:26 historical build completed its then-current asset, strict typecheck, Vite, and notices gates but embedded `dirty=true` and `shell-v30`, so it remains development/build evidence only. Current working-tree typecheck, lint, 450-check test command, audit, isolated production compile, browser, performance-budget, short-soak, current normal-build, and notices evidence are recorded separately above. A clean annotated tag, format-2 manifest, `shell-v35` production/offline smoke, clean-candidate performance, and the full 30-minute soak remain pending.

Across split resumed Chromium tutorial/input runs, Settings continuity and gamepad prompts passed first; the three formerly failing later-copy, deterministic-retry, and comprehensive-journey cases then passed together headed 3/3 and individually headless. The comprehensive assertion uses the authoritative recap clock and satisfies the under-three-minute gate; long functional cases use Low quality separately from High-quality visual QA. The emulated touch tutorial intro passes 2/2. Real worker transitions, physical inputs, assistive technology, a single full post-fix invocation, and frozen-candidate qualification remain open.

The current focused comprehensive tutorial run also passes after the latest arrow-control evidence refresh. On 2026-07-18, `npx playwright test e2e/tutorial.spec.ts -g "a new rider completes the comprehensive tutorial without skipping" --config <temporary isolated QA-port config> --project chromium` ran on isolated QA dev port 4178, did not touch the launched 4173 preview or `dist/`, and passed 1/1 in 3.7 minutes wall time. That single flow covers the complete earned Rider School path without skipping: intro controls, pause/restart focus behavior, Ride/coast, lane change, Turbo heat, cooling, wheelie, airborne pitch, clean landing, barrier handling, mud/grass slowdown, crash/recovery, rival contact quizzes, replayability, and the under-three-minute in-game recap assertion. It remains scoped QA-browser evidence rather than frozen-candidate acceptance.

The 2026-07-19 tutorial-clarity slice makes the post-ride rival-contact requirement visible instead of implicit: Rider School now advertises “12 lessons + 2 contact drills,” and the two contact-rule questions expose an accessible progress list showing current/completed drill state. `npx vitest run src/ui/game/__tests__/tutorialProgress.test.ts src/ui/game/__tests__/touchAccessibility.test.tsx` passed 13/13. `RRR_PLAYWRIGHT_PORT=4174 npx playwright test e2e/tutorial.spec.ts --project=chromium` then ran a fresh QA-mode Chromium server on port 4174 while leaving the launched `127.0.0.1:4173` preview untouched; it passed 5/5 executed tutorial cases with 1 project-scoped touch skip in 8.3 minutes, including the full no-skip comprehensive tutorial in 3.9 minutes wall time and explicit contact-drill progress assertions. This is stronger scoped tutorial evidence, not a frozen-candidate release pass.

The 2026-07-19 Canyon shoulder-fidelity slice adds a Canyon-only, presentation-only route-edge dressing layer: two route-following red-rock shoulder ribbons plus 78 cut-bank shelf instances, 184 shoulder-rock instances, and 52 agave/yucca clusters in High quality. The 3D rocks/plants are kept outside the safety wall while the flat ribbons add visible red-rock shoulder color without becoming obstacle-like. `npx eslint src/game/engine/GameEngine.ts e2e/visual-regression.spec.ts`, `npm run build`, and a scoped live production-preview smoke against `127.0.0.1:4173` passed; the live canvas reported authored hero, Canyon kit, environment, and `data-canyon-shoulder-dressing-style="route-following-cut-bank"` ready with no page or request errors. A QA frozen Practice capture at `/tmp/rivet-ridge-canyon-shoulder-frozen-practice-v4-1280x720.png` was inspected against `docs/design/concepts/gameplay-desktop.png` and `docs/design/concepts/canyon-production-asset-reference.png`. This improves the formerly flat foreground corridor, but it remains an incremental dirty-tree visual diagnostic, not owner acceptance or a promoted baseline.

The 2026-07-19 dirt texture/rut-fidelity slice adds deterministic `layered-rut-pebble-v2` terrain treatment: 512×512 color and height maps, additional pebble flecks, worn rut highlights, and denser clod relief. `npm run typecheck`, `npx eslint src/game/engine/GameEngine.ts e2e/visual-regression.spec.ts`, and `npm run build` passed. A frozen QA Practice capture on temporary port 4175 and a live smoke against the launched `127.0.0.1:4173` preview both reported `data-dirt-texture-detail-style="layered-rut-pebble-v2"`, `data-dirt-texture-resolution="512x512"`, `data-dirt-height-texture-resolution="512x512"`, authored bike/Canyon readiness, no page errors, and no failed requests. Screenshot: `/tmp/rivet-ridge-dirt-texture-frozen-practice-1280x720.png`. This makes the dirt read less flat and more rutted, but it is still an incremental procedural material pass; it is not owner acceptance, a promoted visual baseline, or proof that the concept-art target has been met.

The 2026-07-19 textured route-banner slice upgrades the Canyon-only, non-colliding route-side sponsor-banner layer: 76 vertical banner panels and 76 poles in High quality, placed outside the safety wall and transformed through the same course route as the trackside scenery. The panels now use four original generated-at-runtime canvas texture variants with ridge/lightning motifs and teal/coral/yellow/navy festival colors instead of flat vertex-color cards. `npm run typecheck`, `npx eslint src/game/engine/GameEngine.ts e2e/visual-regression.spec.ts`, and `npm run build` passed. A frozen QA 500 m bend capture on temporary port 4175 reported `data-canyon-route-banner-style="route-following-textured-sponsor-v2"`, `data-canyon-route-banner-count="76"`, `data-canyon-route-banner-texture-variant-count="4"`, authored bike/Canyon readiness, no page errors, and no failed requests. The live `127.0.0.1:4173` preview also reports the same textured-banner diagnostics with no page/request errors; screenshots include `/tmp/rivet-ridge-textured-banners-bend-500m-clear-v2-1280x720.png` and `/tmp/rivet-ridge-textured-banners-live-current-1280x720.png`. This improves concept-like festival signage along the bend, but the banners remain procedural canvas textures rather than separately authored fabric/signage assets and do not constitute owner acceptance or a promoted baseline.

The 2026-07-19 route-crowd rail slice adds Canyon-only, non-colliding spectator density along the route: 22 rail-bleacher groups, 198 enlarged spectators, and 44 simple tiers in High quality, placed outside the fence/safety wall and transformed through the production course route. `npm run typecheck`, `npx eslint src/game/engine/GameEngine.ts e2e/visual-regression.spec.ts`, and `npm run build` passed. A frozen QA 500 m bend capture on temporary port 4175 reported `data-canyon-route-crowd-style="route-following-rail-bleachers-v2"`, `data-canyon-route-crowd-group-count="22"`, `data-canyon-route-crowd-spectator-count="198"`, `data-canyon-route-crowd-tier-count="44"`, authored bike/Canyon readiness, no page errors, and no failed requests. The live `127.0.0.1:4173` preview repeated those diagnostics with no page/request errors; screenshots include `/tmp/rivet-ridge-route-crowd-rails-bend-500m-clear-v2-1280x720.png` and `/tmp/rivet-ridge-route-crowd-rails-live-current-1280x720.png`. This adds visible route-side activity while preserving gameplay readability, but the crowd remains procedural low-poly dressing rather than final authored crowd assets and does not constitute owner acceptance or a promoted baseline.

The 2026-07-19 desktop-pause affordance slice fixes a discoverability gap in the race HUD: the existing public pause/resume button is now visible and mouse-reachable on desktop, not only touch/narrow layouts. `npm run typecheck`, `npx eslint e2e/core-flow.spec.ts`, and `RRR_PLAYWRIGHT_PORT=4174 npx playwright test e2e/core-flow.spec.ts -g "fresh load completes a keyboard race, saves onboarding, and retries" --project=chromium` passed after hardening that flow away from a transient `GO` overlay assertion and onto the stable race phase. A live `127.0.0.1:4173` screenshot probe at 1280×720 recorded `aria-label="Pause race"`, `display: grid`, a 54.39×54.39 px button, no overlap with target/timing HUD cards, no page/request failures, and `/tmp/rivet-ridge-desktop-pause-affordance-live-4173-1280x720.png`. This improves desktop accessibility and menu-flow readiness, but it is scoped dirty-tree evidence rather than frozen-candidate qualification.

The 2026-07-19 settings-toggle hit-area slice expands the actual invisible checkbox targets in Settings to a minimum 44 px affordance while preserving the visual switch styling. `npm run typecheck`, `npx eslint e2e/accessibility-controls.spec.ts`, and `RRR_PLAYWRIGHT_PORT=4174 npx playwright test e2e/accessibility-controls.spec.ts -g "accessibility and volume controls apply immediately and persist" --project=chromium` passed. A live `127.0.0.1:4173` Settings probe at 1280×720 recorded the Reduced Motion checkbox target as 70.39×75.80 px, verified a lower-right edge click toggled the setting, found no page/request failures, and captured `/tmp/rivet-ridge-settings-toggle-hit-area-live-4173-1280x720.png`. This closes the source-fixable tap-target gap for Settings toggles in the current dirty tree; physical touch-device comfort and frozen-candidate qualification remain unverified.

The 2026-07-19 tabular-timer typography slice stabilizes the highest-frequency HUD numerals by applying `font-variant-numeric: tabular-nums` to the race clock, lap number, target/position numerals, final result time, and prior-best comparison timing text. `npm run typecheck`, `npx eslint e2e/core-flow.spec.ts`, and `RRR_PLAYWRIGHT_PORT=4174 npx playwright test e2e/core-flow.spec.ts -g "fresh load completes a keyboard race, saves onboarding, and retries" --project=chromium` passed, including computed-style assertions for the race clock, lap number, and target time. A live `127.0.0.1:4173` probe sampled eight changing clock values from `00:00.00` through `00:00.70`; each reported `tabular-nums` for clock/lap/target and held the clock at x `558.109375` with width `163.78125`, no page/request failures, and `/tmp/rivet-ridge-tabular-timer-live-4173-1280x720.png`. This reduces timer jitter in the current dirty tree; final cross-browser visual acceptance remains open.

The 2026-07-19 locked-progression readability slice removes the global disabled-button fade from locked campaign stops and locked race-mode rows while preserving their unavailable state. The title-screen locked track copy now stays visible as `Finish third to unlock`, and locked Rival mode keeps `Beat the Solo Challenge target to unlock.` readable while remaining disabled. `npm run typecheck`, `npx eslint src/ui/screens/MenuScreens.tsx e2e/accessibility-controls.spec.ts`, and `RRR_PLAYWRIGHT_PORT=4174 npx playwright test e2e/accessibility-controls.spec.ts -g "locked progression instructions remain readable" --project=chromium` passed; the browser test checks opacity/filter plus 4.5:1-or-better contrast for both instructions. A live `127.0.0.1:4173` probe recorded full opacity, no disabled grayscale filter, color `rgb(255, 240, 196)`, disabled Rival mode preserved, no page/request failures, and screenshots `/tmp/rivet-ridge-locked-campaign-readable-live-4173-1280x720.png` plus `/tmp/rivet-ridge-locked-mode-readable-live-4173-1280x720.png`. This improves progression clarity in the current dirty tree; final cross-browser and owner visual acceptance remain open.

The 2026-07-19 compact-results layout slice fixes a short-viewport clipping issue on the finish screen. `.results-screen` now uses `align-content: safe center`, a fixed app-shell height, and its own `overflow: auto` scroll container so the header starts onscreen and result actions remain reachable in compact desktop and phone-landscape layouts. `npm run typecheck`, `npx eslint e2e/core-flow.spec.ts`, and `RRR_PLAYWRIGHT_PORT=4174 npx playwright test e2e/core-flow.spec.ts -g "fresh load completes a keyboard race, saves onboarding, and retries" --project=chromium` passed with new 1280×600 and 844×390 assertions after a completed race. A temporary Playwright screenshot/probe on port 4174 recorded no page/request failures, `alignContent="safe center"`, zero horizontal overflow, 1280×600 `clientHeight=600` / `scrollTop=29` with Retry visible, and 844×390 `clientHeight=390` / `scrollTop=190` with Retry visible; screenshots are `/tmp/rivet-ridge-results-safe-center-1280x600.png`, `/tmp/rivet-ridge-results-safe-center-844x390.png`, and `/tmp/rivet-ridge-results-safe-center-844x390-bottom.png`. This improves finish-screen reachability in the current dirty tree; cross-browser visual acceptance and physical-device comfort remain open.

The 2026-07-19 mobile campaign-rail slice prevents the title screen itself from panning sideways on phones. Under tablet/narrow breakpoints, `.campaign-rail` now uses its own `overflow-x: auto` strip, scroll snap, and a route line sized to the scrollable stop row, while the document remains fixed to the viewport width. `npm run typecheck`, `npx eslint e2e/accessibility-controls.spec.ts`, and `RRR_PLAYWRIGHT_PORT=4174 npx playwright test e2e/accessibility-controls.spec.ts -g "primary menus fit" --project=chromium` passed. A 360×640 live `127.0.0.1:4173` probe recorded no page/request failures, campaign rail `scrollWidth=653`, `clientWidth=360`, `scrollLeft=293`, `overflowX="auto"`, document horizontal overflow `0`, and the final campaign stop fully visible after rail scroll; screenshots are `/tmp/rivet-ridge-campaign-rail-scroll-start-360x640.png`, `/tmp/rivet-ridge-campaign-rail-scroll-end-360x640.png`, and `/tmp/rivet-ridge-campaign-rail-scroll-live-4173-360x640.png`. This improves phone menu reachability in the current dirty tree; physical-device comfort and full cross-engine visual acceptance remain open.

The 2026-07-19 title first-paint slice preloads the production title backdrop and reduces display-font layout shift risk. `index.html` now preloads `/assets/art/title-background.png` as a high-priority PNG image, `.menu-sky` keeps that production art as the top background layer with canyon/sky fallback gradients underneath, and both bundled `Ridge Display` font faces now use `font-display: optional` instead of `swap`. `npm run typecheck`, `npx eslint e2e/accessibility-controls.spec.ts`, and `RRR_PLAYWRIGHT_PORT=4174 npx playwright test e2e/accessibility-controls.spec.ts -g "primary menus fit" --project=chromium` passed. A live `127.0.0.1:4173` probe recorded the title preload `{as:"image", type:"image/png", fetchPriority:"high"}`, title resource `initiatorType="link"`, both font-face weights `700` and `900` with `display="optional"`, the production image plus radial/linear fallback gradients in `.menu-sky`, no page/request failures, and screenshot `/tmp/rivet-ridge-title-first-paint-live-4173-1280x720.png`. This improves first-paint stability in the current dirty tree; Lighthouse LCP/CLS and full release performance gates remain open.

The 2026-07-19 selector-semantics slice exposes active UI choices to assistive technology. Settings section buttons and Track Builder category/module selector buttons now set `aria-pressed` according to their active state, while preserving the existing visual `.active` styling. `npm run typecheck`, `npx eslint src/ui/screens/MenuScreens.tsx src/ui/editor/TrackEditorScreen.tsx e2e/accessibility-controls.spec.ts`, and `RRR_PLAYWRIGHT_PORT=4174 npx playwright test e2e/accessibility-controls.spec.ts -g "settings and editor selector buttons expose" --project=chromium` passed. A live `127.0.0.1:4173` probe recorded no page/request failures, Settings `audio=true` with `accessibility/play=false`, editor category `jumps=true`, selected module `Medium Ramp=true`, sibling modules false, and screenshot `/tmp/rivet-ridge-aria-pressed-selectors-live-4173-1280x720.png`. This improves current-tree selector semantics; manual screen-reader review and full accessibility acceptance remain open.

The 2026-07-19 campaign-tagline touch slice keeps title-screen campaign guidance readable on tablet/touch devices that do not support hover. Unlocked inactive campaign taglines still use the desktop hover/focus reveal on pointer-accurate devices, while coarse/no-hover devices now see the guidance without a mouse. `npm run typecheck`, `npx eslint e2e/accessibility-controls.spec.ts`, and `RRR_PLAYWRIGHT_PORT=4174 npx playwright test e2e/accessibility-controls.spec.ts -g "campaign taglines stay readable" --project=chromium --project=tablet-chrome` passed 2/2. A live `127.0.0.1:4173` tablet-touch probe recorded `pointerCoarse=true`, `anyHoverNone=true`, visible campaign guidance, no page/request failures, zero document overflow, and screenshot `/tmp/rivet-ridge-campaign-taglines-tablet-touch-live-4173-1024x768.png`. This improves current-tree touch readability; physical iPad/Safari comfort and full cross-device acceptance remain open.

The 2026-07-19 tutorial-live-region slice scopes Rider School announcements to one concise hidden status node instead of the full lesson card. The tutorial card no longer carries `aria-live`; the new `.sr-only` `role="status"` region uses `aria-live="polite"` and `aria-atomic="true"` and updates from intro text to the active lesson summary when training starts. `npm run typecheck`, `npx eslint src/ui/game/GameView.tsx e2e/accessibility-controls.spec.ts`, and `RRR_PLAYWRIGHT_PORT=4174 npx playwright test e2e/accessibility-controls.spec.ts -g "tutorial lesson updates use" --project=chromium` passed. A live `127.0.0.1:4173` probe recorded card `aria-live=null`, status `aria-live="polite"`, status text changing from `Rider School intro. Twelve lessons and two contact drills.` to `Lesson 1 of 12: Ride and read the HUD. active. Hold W`, no page/request failures, and screenshot `/tmp/rivet-ridge-tutorial-live-status-region-live-4173-1280x720.png`. This improves current-tree assistive semantics; manual VoiceOver/NVDA review remains open.

The 2026-07-19 editor-confirmation slice removes Track Builder's browser-native confirmation dialogs from destructive actions. Recovery-record removal, saved-track deletion, and clear-all now share a styled in-app modal dialog with `role="dialog"`, `aria-modal="true"`, focus on the destructive action, Escape/cancel handling, and inert editor chrome behind the modal. `npm run typecheck`, `npx eslint src/ui/editor/TrackEditorScreen.tsx e2e/editor-coverage.spec.ts`, and `RRR_PLAYWRIGHT_PORT=4174 npx playwright test e2e/editor-coverage.spec.ts -g "duplicate, rename|saved-track deletion|invalid tracks" --project=chromium` passed 3/3, and `rg "window\\.confirm" src/ui/editor/TrackEditorScreen.tsx` returned no matches. A live `127.0.0.1:4173` probe recorded the clear-all modal title/message/confirm text, focused `Clear all` action, toolbar/workspace inert state, no native dialogs, no page/request failures, and screenshot `/tmp/rivet-ridge-editor-confirm-dialog-live-4173-1280x720.png`. This improves current-tree Track Builder UX consistency; full editor acceptance remains release-unverified.

The 2026-07-19 BootScreen message slice gives lazy route fallbacks destination-specific copy without losing the store-driven boot/recovery default. `BootScreen` now accepts an optional `message` prop; Track Builder uses `Opening Track Builder…`, Rider School uses `Opening Rider School…`, and race/pause/settings-over-race routes use `Preparing the race…`. `npm run typecheck`, `npx eslint src/app/App.tsx src/ui/screens/MenuScreens.tsx e2e/accessibility-controls.spec.ts`, `RRR_PLAYWRIGHT_PORT=4174 npx playwright test e2e/accessibility-controls.spec.ts -g "chunk loading screens" --project=chromium`, and `npm run build` passed. A live `127.0.0.1:4173` delayed Track Builder chunk probe recorded fallback text `Opening Track Builder…`, loaded editor readiness, no console/page/request failures, and screenshot `/tmp/rivet-ridge-bootscreen-track-builder-message-live-4173-1280x720.png`. This is scoped Suspense-copy evidence, not a full throttled cross-browser navigation acceptance pass.

The 2026-07-19 authored-icon slice replaces platform-rendered icon glyphs in icon-only controls with one inline SVG family. `RallyIcon` now covers back, undo, redo, close, pause, and active/play progress markers across menus, Settings, Track Builder, the local-track drawer, the race HUD, and Rider School/contact progress. Instructional control copy still uses readable text labels. `npm run typecheck`, `npx eslint src/ui/icons/RallyIcon.tsx src/ui/screens/MenuScreens.tsx src/ui/editor/TrackEditorScreen.tsx src/ui/game/GameView.tsx e2e/accessibility-controls.spec.ts e2e/tutorial.spec.ts`, `RRR_PLAYWRIGHT_PORT=4174 npx playwright test e2e/accessibility-controls.spec.ts -g "icon-only controls" --project=chromium`, `RRR_PLAYWRIGHT_PORT=4174 npx playwright test e2e/tutorial.spec.ts -g "missed one-shot|new rider completes" --project=chromium`, and `npm run build` passed. A live `127.0.0.1:4173` probe recorded authored SVG selectors for Settings back, editor back/undo/redo, drawer close, and race pause; the close button had no stray text; no console/page/request failures occurred; screenshots were captured at `/tmp/rivet-ridge-authored-svg-icons-editor-live-4173-1280x720.png` and `/tmp/rivet-ridge-authored-svg-icons-pause-live-4173-1280x720.png`. This is scoped UI-icon consistency evidence, not a full cross-engine visual acceptance pass.

The 2026-07-19 screen-motion slice adds a short festival-style entrance to top-level screen swaps and a fade/panel settle to the race settings overlay without violating reduced-motion settings. Top-level route surfaces now mount through `.screen-surface` with a 180 ms opacity/translate entrance; `.race-settings-overlay` uses a 160 ms fade and its settings panel uses a 180 ms settle. `html[data-reduced-motion="true"]` and `prefers-reduced-motion: reduce` explicitly set these new animations to `none`. `npm run typecheck`, `npx eslint src/app/App.tsx e2e/accessibility-controls.spec.ts`, `RRR_PLAYWRIGHT_PORT=4174 npx playwright test e2e/accessibility-controls.spec.ts -g "screen swaps" --project=chromium`, and `npm run build` passed. A live `127.0.0.1:4173` probe recorded `screen-surface-enter`, `settings-overlay-fade`, `settings-overlay-panel`, reduced overlay/panel `animationName="none"`, no console/page/request failures, and screenshot `/tmp/rivet-ridge-screen-motion-overlay-live-4173-1280x720.png`. This is scoped motion-contract evidence, not full subjective feel or cross-engine visual acceptance.

The 2026-07-19 Heat HUD threshold slice removes a simulation/UI drift point: the white heat warning marker now uses the exported `CRITICAL_HEAT_WARNING` value through `--heat-warning-threshold` instead of a separate stylesheet literal. `npm run typecheck`, `npx eslint src/ui/game/GameView.tsx src/ui/game/__tests__/heatMeterThreshold.test.tsx e2e/accessibility-controls.spec.ts`, `npx vitest run src/ui/game/__tests__/heatMeterThreshold.test.tsx`, `RRR_PLAYWRIGHT_PORT=4174 npx playwright test e2e/accessibility-controls.spec.ts -g "captions and critical race state" --project=chromium`, and `npm run build` passed. The browser gate now proves the intended critical-warning state instead of requiring full overheat during the probe, preserving the slower warning-to-overheat tuning. A live `127.0.0.1:4173` probe served `assets/index-BtNkWncw.js` and `assets/index-CxQk2mki.css`, recorded `--heat-warning-threshold="78%"`, a visible `.heat-warning`, colorblind-safe heat fill, no console/page/request failures, and screenshot `/tmp/rivet-ridge-heat-warning-threshold-live-4173-1280x720.png`. This is scoped HUD consistency evidence, not release-bound gameplay acceptance.

The 2026-07-19 pre-race loading gate slice gives slow race prep a readable, non-static loading state without adding start-line delay to fast-ready runs. Non-tutorial races now reveal the shared `.loading-track` after 150 ms, hold it for at least 400 ms once visible, then remove it before countdown; races that become ready before 150 ms skip the loading card entirely. `npm run typecheck`, `npx eslint src/ui/game/GameView.tsx e2e/core-flow.spec.ts`, `RRR_PLAYWRIGHT_PORT=4174 npx playwright test e2e/core-flow.spec.ts -g "slow race prep" --project=chromium`, and `npm run build` passed. A live `127.0.0.1:4173` probe with delayed hero/Canyon assets recorded the sequence `loading/loading/true → countdown/countdown/false → racing/null/false`, served then-current `assets/index-DlCGuHVg.js` and `assets/index-C2e4audF.css`, observed no console/page/request failures, and captured `/tmp/rivet-ridge-race-gate-loading-live-4173-1280x720.png` plus `/tmp/rivet-ridge-race-gate-countdown-live-4173-1280x720.png`. This is scoped gate-flow evidence, not a full cold-cache performance qualification.

The 2026-07-19 heat-tuning response extends the usable turbo window by keeping the original 8 heat/second climb up to the 78% warning marker, then slowing the warning-to-overheat climb to 4 heat/second. `npx vitest run src/game/simulation/__tests__/simulation.test.ts src/game/engine/__tests__/tutorialHeatPath.test.ts` passed 27/27, proving the bike no longer overheats during the earlier 14-second turbo window, still overheats under prolonged abuse, still recovers at 35% heat, and still reaches Rider School's critical-heat lesson with more than 30 m of dirt before the 241 m cooling-gate entry. A scoped rendered Playwright probe against `127.0.0.1:4173` confirmed the rebuilt preview loads with authored hero/Canyon/environment readiness, 49,780 hero triangles, and no page/request errors; it is not a physical-device or release-bound tuning acceptance.

The 2026-07-19 wheelie/barrier response extends the existing barrier-clear rule so a visibly lifted front wheel can clear a barrier with a speed penalty before the internal `wheelie` latch is fully true. The shared player/AI obstacle outcome now treats `wheelie || pitch >= 0.18` as front-wheel clearance. `npx vitest run src/game/engine/__tests__/barrierContactIntegration.test.ts src/game/engine/__tests__/aiCollisionScheduling.test.ts src/game/engine/__tests__/aiRules.test.ts src/game/simulation/__tests__/simulation.test.ts` passed 43/43, including a new lifted-front-wheel barrier case that applies the 0.6 retained-speed penalty and does not call crash. `npm run build` and a scoped production-preview smoke also passed. This closes the immediate source-level complaint that raising the front wheel still crashed on barriers, but physical feel and full-track tuning remain unverified.

The retained headed Chromium 149 measurement and 30-minute headless Rival soak passed their earlier QA-enabled build in the recorded scopes. The current local-only schema-4 artifact, `artifacts/candidate-evidence/diagnostic/performance/current-0049739-headed-rebuilt-20260719T060319Z.json`, measures the rebuilt current non-QA working-tree preview in headed Chromium 149/Apple Metal and is cleanly bound to source/runtime commit `00497390f5c5c843ccf668dd2801920f1b999c82`. Desktop High normal/stress mean FPS is 60.00/60.01 with p95 synchronous frame work 2.8/3.1 ms; emulated-mobile Low is 60.00/60.00 with p95 3.7/4.4 ms. The artifact records stable served bytes, authored hero ready in both profiles, 5,460,155 gzip bytes, and zero request, HTTP, console, or page errors. It correctly remains overall `FAIL` only because the real format-2 release manifest is absent, producing the expected `no-harness-error` failure: `Production smoke requires a readable format-2 release manifest.` A headed four-minute Rival calibration separately records 240,005 ms active racing, one validated 02:36.45 first-place result, zero overheats/timeouts/dropped simulation, and no unexpected network/console/harness errors; it is 26 minutes short. Visual acceptance, clean manifest binding, physical mobile, and qualifying performance/soak remain **UNVERIFIED**.

The current local non-QA `dist/` checkpoint contains 33 files and 7,999,709 raw bytes, with aggregate SHA-256 `a4bc58a50b19c2722e8c36a3ce1a5a19fa74f01b22cad66d6a720389709fe589`. Its 1,632-byte `index.html` has SHA-256 `f142305990f5509d1bfa16c5ede182a4d93fbfb5e30b6d8865f0e2b759f78451`; its 43,872-byte `THIRD_PARTY_NOTICES.txt` has SHA-256 `f837ed705667d0f3976bbd419f42d8c63844a3eb4b52f76db88ed3a1d6e270c2`; its 71,180-byte `assets/index-C2e4audF.css` has SHA-256 `e30c28c622457d6f943ae9a8501dc05aacc7de72d1b6ebffe9334b799ae14615`; its 474,703-byte `assets/index-LAPupqRa.js` has SHA-256 `af2cd145d571f295d7556353e2399bc502fa3aed5dd115e6308ad820f2ac20aa`; its 214,954-byte `assets/GameView-DFtn8QNu.js` has SHA-256 `bfb15d0f587c4d7497ae9fd7d381dda64cc193327a3a7624046dc8caa984a62e`; its 63,283-byte `assets/TrackEditorScreen-nejtni-b.js` has SHA-256 `241ac419ffdf715950a9440f23ae92a2e59769a0c55dd4dcfc16cedd3dbb0d24`; its 17,403-byte `assets/CoursePresentationRoute-D_L4szlK.js` has SHA-256 `257193efb4054918a10df8f982e4540416579bcd7ce1197ae0bc52d738ffe082`; and `assets/3d/hero-bike-rider.glb` is 517,664 bytes with SHA-256 `538be4269927544fa91cfc405742523a60422d77d546ce9c8859f407deb2e6e8`. A post-candidate probe caught the live 4173 preview serving visual-QA bytes; `npm run build` restored normal non-QA bytes and the relaunched preview returned HTTP 200. A scoped Playwright preview/probe at `/tmp/rrr-live-editor-smoke-992ce65-rerun/result.json` found no page/request/HTTP/unexpected-console failures and observed runtime commit `992ce654e6391d9a0f1baf01d8b42324dc35cc1c` with `dirty=false`, absent QA/performance APIs, authored bike ready, visible Rider School lesson progress, campaign menu access, Track Builder entry, and visible Save/Test Ride/Fit Route/Route Complete editor controls. This working-tree checkpoint is not an official artifact, manifest-bound installed-browser/offline smoke, or release qualification.

The 2026-07-18 rendered fallback smoke against the same `127.0.0.1:4173` preview used Playwright because Browser control was listed but the required browser execution tool was not exposed by tool discovery. The earlier tutorial-focused smoke confirmed page title `Rivet Ridge Rally`, nonblank Rider School content, no framework overlay, ready hero/Canyon/environment canvas attributes, the then-current 36,640-triangle hero, 34 Rider School Canyon-kit placements, and four Rider School cooling-gate arches. Starting Lesson 1 and holding `W` advanced the visible tutorial to Lesson 3, moved `data-tutorial-forward-position` from `0.0` to `1.9`, and kept page errors, failed requests, and HTTP errors at zero; the only console entries were repeated browser WebGL `ReadPixels` performance warnings. After the latest mechanical-detail hero pass, a scoped live production-preview smoke observed the updated 49,780-triangle hero counters with authored hero, Canyon kit, and environment assets ready and no console/page errors. The focused source-level input/tutorial gate then passed 29/29 with `npx vitest run src/game/input/__tests__/InputManager.test.ts src/game/engine/__tests__/tutorialLessonGate.test.ts src/game/engine/__tests__/tutorialHeatPath.test.ts`. A live non-QA attempt to run the stale-arrow spec could not exercise its QA-only `data-demonstrated-mechanics` assertion, so it was rerun on isolated QA dev port 4177 without touching the launched preview or `dist/`: `npx playwright test e2e/tutorial.spec.ts -g "a later lesson requires fresh evidence" --config <temporary isolated QA-port config> --project chromium` passed 1/1 in 47.2 seconds, proving stale ArrowLeft input before the active lane lesson does not clear it and a fresh ArrowRight advances to `Turbo and heat`.

The release remains **NOT READY**. Concept fidelity is **NOT ACCEPTED / OWNER UNVERIFIED**. The v20 15-state hero manifest passes its scoped technical checks with authored Canyon-kit/panorama readiness, PMREM/contact-shadow diagnostics, arrow-key lean readability, readable landing pulse, `trail-burst-contact` dust readiness, public-control crash/recovery states, portrait touch wheelie, and the complete forced fallback, and the fresh Canyon frames remain useful diagnostic evidence, but neither is a promoted or owner-accepted baseline. The latest source improves side/rear `22` readability, helmet striping, rider panel separation, handlebar silhouette, tire sidewall detail, brake rotor/hub detail, rear shock/chain detail, engine depth, the left-side chain-drive silhouette, raised cream lightning/star graphics on the side plastics, fork guards, fenders, and rider limbs, material highlight separation, lane-change readability, landing readability, and contact/dust readability; flat untextured material treatment, blocky anatomy, rear-camera occlusion, and unaccepted crash/recovery fidelity against the action reference remain visible gaps. The other four venues also remain open. The corrected production-length visual harness passes its production-asset readiness prerequisites, but the retained exact-tip comparisons still target obsolete baselines: that historical suite ended 5 failed / 5 skipped, and curved Canyon has no accepted baseline. Separate Chromium/Firefox/WebKit title checks also fail. No baseline was created, changed, promoted, or waived.

The guarded baseline-promotion source can re-inventory candidate bytes, validate all 11 controlled frames and response sets, enforce approval chronology/freshness, and preserve complete manifests in a committed record. It requires `authentication: "external-manual-trust-boundary"` because hashes, timestamps, external-file placement, and manifest agreement prove only internal consistency. They do not authenticate a person or prove concept review. Genuine product-owner authorship and visual acceptance remain a manual **EXTERNAL BLOCKER**, regardless of promoter success.

The current working tree contains a smaller continuous cream/coral safety wall along both Canyon Kickoff and Rider School course edges with explicit authored-Test-Ride exclusion. Fresh desktop and portrait frames show it outside the four readable lanes without presenting as a full-width obstacle, and the headed Canyon High slice includes its renderer cost. Clean-candidate counts, high-contrast/manual accessibility review, physical-device review, and owner acceptance remain **UNVERIFIED**.

The corrected Canyon composition deterministically preserves production-High's 22 route pockets plus four cooling watchtowers for 26 pockets / 104 tiers while enforcing at least 5.6 m clearance. Authored marshal towers and the workshop were relocated, and authored structural decor fails closed inside a festival pocket's protected clearance. Targeted unit tests pass 3/3; the current focused quality and authored-Canyon reliability cases pass serially. The current nonqualifying two-lap runtime reports ready bike/Canyon/environment state, 62 Canyon-kit placements, twelve lane-open cooling arches, 26 pockets, 104 tiers, advancing race time, and working pause/resume. Fresh captures and the headed High measurement qualify only this dirty-tree diagnostic scope—not production smoke, offline evidence, an official artifact, physical-device proof, or owner acceptance.

Format-2 release-provenance hardening is present in source and remains **UNVERIFIED** as a real candidate gate. It captures clean `HEAD`, binds package/lock/Node reads to a detached checkout and revalidates them, preserves the exact annotated tag object/type through the build, and resolves the absolute npm launcher to the pinned physical package and canonical entrypoint. A deterministic, path-private full-tree hash covers directory modes, regular-file sizes/content hashes, and safe internal symlink target hashes; counts and regular-file byte totals are also bound. The launcher and entire package are revalidated after every invocation, after manifest creation, and after cleanup. Both npm config scopes remain isolated with scripts enabled, and unexpected ignored inputs fail at all three build phases. Only regular isolated output can replace root `dist/`; guarded recursive cleanup removes stale `dist/` and malformed sidecar directories on failure. The current `npm run test:release-manifest` pass records 45/45 fixtures, including hostile-config/fake-package, same-commit retagging, launcher and implementation mutation, pre-tag visual-candidate cases, and short token-prefix false-positive coverage. Historical evidence remains unchanged; the real `npm run visual:candidate` guard was attempted on 2026-07-18 and correctly rejected the dirty/untracked working tree before producing candidate bytes, then later exposed the binary-prefix false positive after source was committed.

Current focused browser runs exercise the safe normal-Ride heat path, barrier/wheelie handling, campaign/Rider School start flow, portrait/sub-680 presentation, touch controls, and Recovery semantics; the complete working-tree unit/static gates also pass. The exact-product smoke/install/offline path, accepted visual evidence, performance, and physical devices remain open. These scoped passes do not change **UNVERIFIED**, **NOT ACCEPTED**, or **NOT READY** status.

An earlier source-only review identified and the same build compiled four additional P1 corrections: fixed-step catch-up now advances environment, player, track, AI, and contacts once per due simulation tick; exact legacy A/D lane defaults migrate without destroying actions already assigned to the arrows; authored Test Rides cannot inherit the Canyon panorama; and the built-in bike fallback now retains the focal knobby-wheel, panel, exhaust, brake, and rear-`22` silhouette. The deterministic licensed display font and candidate-specific atomic smoke-evidence layout are also present. Current focused browser, migration, unit/fixture, and build gates cover the applicable automated paths; accepted fallback/font rendering and real production smoke remain open.

The latest three-way static audit found no P0 and identified 16 unique P1 defects. Current focused Playwright covers the scoped countdown/results, crash/recovery, editor placement/navigation/import/recovery, persistence failure, responsive geometry, focus/target, and tutorial-caption paths recorded in `QA_REPORT.md`, and the complete working-tree unit/fixture command passes. Subjective audio/camera, manual accessibility, true cross-tab behavior, exact-product evidence, and physical devices remain open, so the candidate remains **NOT READY**.

A follow-up static audit also found no P0. Current focused migration, editor, Test Ride, persistence, touch, and browser runs pass the covered input precedence, route authoring, placement containment, recovery, and module-boundary paths; the unit suite and campaign modes 18/18 also pass. True cross-tab/replay atomicity, accepted visual/accessibility evidence, frozen-candidate repetition, and performance remain **UNVERIFIED**.

## Gate summary

| Gate | Required outcome | Status | Current evidence |
|---|---|---:|---|
| Resumed scoped browser qualification | Focused current behaviors pass without broadening to release readiness | PASS (SCOPED) / RELEASE UNVERIFIED | Current working-tree runs passed focused Chromium functional, reliability, tutorial, editor, input, migration, persistence, and quality paths; campaign modes 18/18 in 27.7 minutes; current rival pack 2/2 on isolated port 4176; 10/10 Firefox-plus-WebKit desktop functional; 6/6 WebKit lifecycle; 6/6 emulated touch matrix; and 2/2 emulated touch tutorial intro. |
| Candidate identity | Final bytes tied to a clean source commit and annotated `v1.0.0-rc.2` tag | UNVERIFIED | The 45 release-manifest fixtures pass, and format-2 source enforces exact-tag identity through an isolated detached clean-install/build. No annotated tag, clean release-bound rebuild, or current matching sidecar/smoke exists. Intended fixture, provenance, retained artifact, art, font, and Node-pin inputs still require final candidate inventory. |
| Strict typecheck | Whole-project strict TypeScript check passes | PASS (WORKING TREE) / RELEASE UNVERIFIED | Current `npm run typecheck` passes. It must be repeated and bound to one clean frozen candidate. |
| Latest PB/AI/responsive/release corrective slice | Standing PB details, per-step rival contact, narrow/short-phone UI, and exact served-root/toolchain binding pass their focused gates | PASS (SCOPED) / RELEASE UNVERIFIED | Current functional, results, lifecycle, persistence, responsive, touch, editor, and complete working-tree unit/fixture paths pass in their stated scopes. Exact served-root/offline product evidence remains pending. |
| Final static gameplay/editor/persistence/accessibility P1 slice | All source-corrected defects pass focused behavioral and browser gates | PASS (SCOPED) / RELEASE UNVERIFIED | Current focused Chromium editor, migration, persistence, reliability, tutorial, and accessibility/control paths pass, including keyboard/pointer authoring, narrow layout, Test Ride/recovery, corrupt/newer storage, and responsive touch emulation; typecheck, unit/fixtures, and the isolated production compile also pass. Screen-reader/manual review, cross-tab stress, and frozen-candidate evidence remain open. |
| Lint | Whole-project lint passes | PASS (WORKING TREE) / RELEASE UNVERIFIED | Current `npm run lint` passes; clean frozen-candidate repetition remains required. |
| Unit tests | Whole-project deterministic suite passes | PASS (WORKING TREE) / RELEASE UNVERIFIED | `npm test` passes 450 checks: 300 Vitest tests, 45 release-manifest fixtures, 71 release-attestation fixtures, and 34 production-smoke/service-worker/release-scope fixtures. The real release workflows and clean-candidate repetition remain separate gates. |
| Asset pipeline and verification | Every source-tree runtime asset matches the committed licensed inventory | PASS (WORKING TREE) / RELEASE UNVERIFIED | `npm run assets:verify` passes the current 49,780-triangle / 517,664-byte hero with the exact manifest/source-pair contract, the rival contract, Canyon 32,008 triangles / 427,028 bytes, and production art/provenance including the v3 action sheet. Headed hero action integration passes 5/5, and the v20 15-state enriched capture passes technically for the current hero/material/dust runtime. Cryptographic C2PA validation, accepted visual fidelity, rendered-font proof, human similarity/legal review, and immutable-candidate qualification remain open. |
| Latest-source build and preview checkpoint | Production-mode source compiles, notices regenerate, and public Rider School / Track Builder reachability passes rendered checks | PASS (SCOPED) / RELEASE UNVERIFIED | `npm run build` passes asset verification, strict typecheck, Vite production build of 145 modules, and notice generation with no Vite chunk-size warning after the Three manual-chunk split. Repository `dist/` is the current 33-file checkpoint: 7,999,709 raw bytes, aggregate `a4bc58a50b19c2722e8c36a3ce1a5a19fa74f01b22cad66d6a720389709fe589`, 1,632-byte index hash `f142305990f5509d1bfa16c5ede182a4d93fbfb5e30b6d8865f0e2b759f78451`, 71,180-byte stylesheet `assets/index-C2e4audF.css` hash `e30c28c622457d6f943ae9a8501dc05aacc7de72d1b6ebffe9334b799ae14615`, 474,703-byte main chunk `assets/index-LAPupqRa.js` hash `af2cd145d571f295d7556353e2399bc502fa3aed5dd115e6308ad820f2ac20aa`, 214,954-byte GameView chunk `assets/GameView-DFtn8QNu.js` hash `bfb15d0f587c4d7497ae9fd7d381dda64cc193327a3a7624046dc8caa984a62e`, 63,283-byte Track Builder chunk `assets/TrackEditorScreen-nejtni-b.js` hash `241ac419ffdf715950a9440f23ae92a2e59769a0c55dd4dcfc16cedd3dbb0d24`, 17,403-byte CoursePresentationRoute chunk `assets/CoursePresentationRoute-D_L4szlK.js` hash `257193efb4054918a10df8f982e4540416579bcd7ce1197ae0bc52d738ffe082`, 598,505-byte Three core chunk `assets/vendor-three-_L_YAsy5.js` hash `ad793e90e46ee2c215ce5a818f9d7ebc7ac608fa5c8dde2723c2fd9cf975d5bd`, 135,491-byte Three addons chunk `assets/vendor-three-addons-kpozWyf3.js` hash `115007f768b8376d80164d66e8edce7559b3729e3809cf2911785c5193258301`, 517,664-byte hero GLB hash `538be4269927544fa91cfc405742523a60422d77d546ce9c8859f407deb2e6e8`, and 43,872-byte notices hash `f837ed705667d0f3976bbd419f42d8c63844a3eb4b52f76db88ed3a1d6e270c2`. After a probe caught the live 4173 preview serving visual-QA bytes, the normal build was restored and relaunched. The latest scoped live smoke at `/tmp/rrr-live-editor-smoke-992ce65-rerun/result.json` proves runtime marker `992ce65`/`dirty=false`, absent QA/performance APIs, visible Start Lesson 1 and Skip Training controls, authored bike ready, Rider School progress to Lesson 2, campaign menu access, Track Builder entry, visible Save/Test Ride/Fit Route/Route Complete editor controls, and no page/request/HTTP/unexpected-console failures. This is not a format-2 manifest, installed/offline smoke, clean performance/soak, or immutable-source release gate. |
| Dependency audit | Installed dependency tree has no reported vulnerabilities | PASS (WORKING TREE) / RELEASE UNVERIFIED | Current `npm audit --audit-level=high` exits 0 with zero vulnerabilities. A clean frozen-candidate install must still be audited. |
| Release-scope brand/path/secret/tracker audit | Ship-capable files and current `dist` contain no prohibited marks, local paths, token-shaped secrets, or tracker SDK/domain markers | PASS (SCOPED) / RELEASE UNVERIFIED | Current `npm run audit:release-scope` scans tracked `src/`, `public/`, root app/build metadata, and optional current `dist`; it passed over 131 files / 15,208,175 bytes with aggregate SHA-256 `463b7d9e36d6c071efaf3c26711f05552e1aef8170033fa4dcf4d2b62028a240` and zero findings. Full Git-history, legal, local-machine, and final tagged-candidate audits remain separate gates. |
| Comprehensive first-play tutorial | Earned path completes every lesson and drill below three minutes and remains replayable | PASS (SCOPED) / RELEASE UNVERIFIED | The latest QA-mode Chromium tutorial file rerun passed 5/5 executed cases with 1 scoped touch-intro skip on isolated port 4174 while the launched 4173 preview stayed up. It proves the visible “12 lessons + 2 contact drills” intro, 12 riding lessons, two accessible contact-drill progress states, retry behavior, replayability, and below-three-minute in-game recap. Touch tutorial introduction also passes 2/2 in emulation. Physical devices, screen-reader/manual review, storage pressure, frozen-candidate rerun, and release-bound acceptance remain open. |
| Barrier, Turbo, AI-contact, and crash-presentation correction | Visible contact/tuning behavior matches the intended source policy | PASS (SCOPED) / RELEASE UNVERIFIED | Current focused tutorial/gameplay paths pass the raised-front-wheel barrier clear with speed cost and slower heat journey, and the complete working-tree unit suite passes. Production-length tuning, AI fairness, and manual collision/visual review remain open. |
| Production-length target, AI, and Summit Mastery calibration | Changed targets are feasible for players and fair across AI and all Mastery tiers | PASS (CAMPAIGN SCOPE) / RELEASE UNVERIFIED | Campaign modes pass 18/18 in Chromium in 27.7 minutes, including a deterministic Summit Mastery escalation case that uses a QA-only Rookie difficulty hook to clear tier 1 and verify the tier 2 hot-start increase. Focused source tests pass 28/28 across progression, target feasibility, and AI rules, including sequential clearance of every Summit Mastery tier, final-tier replayability, deterministic production-length representative-player completion of every Solo target without crash or overheat, and complete six-rider Rookie/Rider/Ace field classification across all five launch tracks with average AI pace ordered Rookie > Rider > Ace. Current source provisionally sets Solo/par targets to Canyon 190/181 s, Pine 208/198 s, Coastline 224/214 s, Foundry 239/228 s, and Summit 259/247 s. Physical/manual fairness review and exact frozen-candidate repetition remain open. |
| Pre-race loading/countdown | Asset readiness, accessible countdown, and zero-time start are enforced | PASS (SCOPED) / RELEASE UNVERIFIED | Focused reliability/functionality passes authored, hard-failure, invalid, timeout-fallback, late-ready, and countdown/race-gate paths. The latest gate-flow slice adds a readable loading affordance for slow prep: reveal after 150 ms, hold visible for at least 400 ms, then remove before countdown; fast-ready races skip the loading card. `RRR_PLAYWRIGHT_PORT=4174 npx playwright test e2e/core-flow.spec.ts -g "slow race prep" --project=chromium` passed, and the live 4173 delayed-asset probe recorded `loading/loading/true → countdown/countdown/false → racing/null/false` with no console/page/request failures. Hero and rival release the gate to their complete safe fallback after five seconds and may atomically upgrade on a valid late result; Canyon kit and Canyon panorama use 12-second visual-art deadlines so cold non-QA preview can install the authored venue before falling back, with Canyon-kit timeout still terminal if it expires. Exact-product load/performance and complete restart/disposal stress remain open. |
| Persistence and editor-session recovery safety | Concurrent tabs preserve unrelated data; retry cannot overwrite an unread profile or newer mutation; stale track delete/save is non-destructive; session-mode Test Ride remains usable | PASS (SCOPED) / RELEASE UNVERIFIED | Current focused persistence, reliability, and editor-session journeys plus the complete working-tree unit/fixture command pass normal retry/save, unavailable/corrupt/newer storage, session Test Ride, snapshot recovery, library, reload/export/import, and lifecycle paths. True two-tab conflicts, destructive recovery rehearsal, physical/browser storage pressure, and frozen-candidate repetition remain open. |
| Keyboard defaults and remapping | Arrow-cluster lane defaults, targeted migration, and custom remaps work | PASS (SCOPED) / RELEASE UNVERIFIED | Current focused remapping passes, the eight-case migration scope passes fresh arrow defaults plus exact legacy A/D conflict-safe migration, and `InputManager` passes 22/22 with exact-once quick-tap delivery plus suspension clearing. A corrected muted production-preview check advances the public lane lesson with one zero-delay Arrow press. A frozen-candidate rerun and manual OS/browser-system-key review remain open. |
| Modes and progression | Complete campaign/mode matrix remains correct | PASS (CAMPAIGN SCOPE) / RELEASE UNVERIFIED | The current isolated Chromium campaign-modes suite passes 18/18 in 27.7 minutes. Exact frozen-candidate repetition and separate production-length calibration remain open. |
| Gamepad, touch, accessibility, and responsive layouts | All required projects and interaction gates pass | PASS (SCOPED) / RELEASE UNVERIFIED | Current focused gamepad emulation passes; the emulated touch matrix passes 6/6 across mobile Chrome, mobile Safari, and tablet Chrome; touch tutorial intro passes 2/2 across mobile/tablet Chrome; and narrow editor/accessibility paths pass. Physical controllers/touch, installed browsers, assistive technology, owner review, and full frozen-candidate matrix remain unverified. |
| Visual acceptance | Desktop/mobile composition, five-track identity, and Builder/Test Ride presentation meet accepted concept direction | UNVERIFIED | **NOT ACCEPTED.** The canonical current hero v20 diagnostic manifest is 121,832 bytes with SHA-256 `9a6beeb30396e57054d5f5aa4500ec32cd2d9f9ca8746eb48846f2f7cca24223`; all 15 scoped captures report `PASS`, `conceptFidelityClaimed: false`, `visualAcceptance: PENDING_OWNER_REVIEW`, and `baselinePromoted: false`, with material/shadow/dust readiness, arrow-key lean, landing pulse, touch wheelie, Reduced Motion, and forced fallback checks recorded for the current runtime. That dirty, nonbaseline technical result is not owner acceptance. The other four venues, rivals, Builder/Test Ride, high contrast, physical devices, similarity/trade-dress, manual accessibility, and owner/legal review remain open. Retained failures compare against obsolete baselines, curved Canyon still lacks an accepted baseline, and no baseline was created or updated. |
| Canyon tiered-grandstand/watchtower visual and performance acceptance | Raised crowd structures match the concepts without overlap, route occlusion, density leakage, or budget regression | PASS (WORKING-TREE STRUCTURAL/RUNTIME/VISUAL-DIAGNOSTIC SCOPE) / OWNER UNVERIFIED | Deterministic High preserves 22 route pockets plus four cooling watchtowers for 26 pockets / 104 tiers and the current two-lap runtime reports 62 Canyon-kit placements plus twelve lane-open cooling arches. Fresh desktop/portrait frames were inspected; focused serial quality and reliability checks pass. A 21-second hardware-backed High sample holds 60 FPS with 2.54 ms maximum synchronous frame work, 301 maximum draw calls, and zero dropped simulation/page/console errors. This is not clean-candidate performance, production smoke, accessibility acceptance, physical-device proof, or owner acceptance. |
| Canyon edge-wall visual/performance acceptance | Continuous edge treatment is readable, non-misleading, isolated from authored courses, and within render budgets | PASS (CANYON WORKING-TREE VISUAL-DIAGNOSTIC) / OWNER UNVERIFIED | Fresh desktop and portrait frames show the smaller cream/coral treatment outside the four readable lanes, and the headed High slice includes its renderer cost; focused editor/Test Ride coverage retains authored-course exclusion. Clean-candidate counts, high contrast, manual accessibility, physical devices, and owner acceptance remain open. |
| Visual regression | Accepted desktop/mobile/editor/high-contrast baselines pass | FAIL | Corrected production-length harness prerequisites pass. The exact-tip suite ends 5 failed / 5 skipped against 2%: desktop 633,032 pixels (about 69%), curved Canyon absent baseline, editor 196,238 (about 22%), high contrast 315,169 (about 35%), and portrait 131,538 (about 39%). Separate title checks fail at 49,370 (about 6%) in Chromium 3/3, 51,119 (about 6%) in Firefox, and 53,317 (about 6%) in WebKit. No baseline was changed. |
| Five-track visual identity diagnostics | Every launch venue exposes a distinct current-runtime scenery signature | PASS (SCOPED) / OWNER UNVERIFIED | Current Chromium QA runtime diagnostics pass 1/1 on isolated port 4178. Canyon reports route-following sponsors, rail bleachers, cactus, and cut-bank dressing; Pine reports bump-aligned roots and red markers; Coastline reports water, boardwalk, and colorful huts; Foundry reports gantries and furnace panels; Summit reports bilateral finale equipment. This improves current-byte visual evidence but does not replace accepted baselines, owner concept/legal approval, or physical-device review. |
| Focused lifecycle/reliability/offline | Required focused behaviors pass | PASS (SCOPED) / RELEASE UNVERIFIED | Current focused Chromium reliability/lifecycle paths pass, as do current rival pack 2/2, 6/6 WebKit lifecycle cases, and focused hero/rival asset checks. The hero/rival/panorama timeout and late-upgrade policy is implemented as recorded above. Real service-worker install/activation/cache/icon transitions and exact-product offline smoke remain unqualified. |
| Service-worker cache boundary and install metadata | Runtime caching is static-only, final-response-bound, and update-safe | PASS (FIXTURE SCOPE) / RELEASE UNVERIFIED | Source uses `shell-v35`; falls back to the last complete shell after rejected, cross-origin-final, or 5xx navigation without masking same-origin 4xx; validates final URLs against the strict static allowlist; rejects `no-store`/`Vary: *` while allowing ordinary CDN variance only inside that boundary; bounds warming and cache growth; bypasses arbitrary same-origin paths; and remembers controller-change/reconnect preparation requests. The 34 production-smoke/service-worker/release-scope fixtures pass. Real install/update/rollback transitions, installed icons, exact format-2 product identity, and production/offline smoke remain unverified; required inputs remain untracked. |
| Full cross-browser/device matrix | Chromium, Firefox, WebKit, mobile/tablet, persistence, lifecycle, offline, and error gates pass | PASS (SCOPED) / RELEASE UNVERIFIED | Current campaign 18/18, rival 2/2, desktop functional 10/10 across Firefox plus WebKit, WebKit lifecycle 6/6, touch matrix 6/6 across mobile Chrome/mobile Safari/tablet Chrome, and touch tutorial 2/2 across mobile/tablet Chrome pass. The complete frozen-candidate all-project suite, installed non-Chrome applications, physical devices/inputs, and exact-product offline path remain unqualified. |
| Current headed performance diagnostic | Desktop normal/stress mean ≥58 FPS with p95 work ≤16.67 ms; emulated-mobile normal/stress mean ≥30 FPS with p95 work ≤33.33 ms; gzip bytes <12,000,000 | FAIL (NO FORMAT-2 MANIFEST) / DIAGNOSTIC | The current headed Chromium 149/Apple Metal diagnostic is source/runtime-bound to clean commit `0049739` and meets every numeric threshold: desktop 60.00/60.01 FPS with 2.8/3.1 ms p95; emulated mobile 60.00/60.00 FPS with 3.7/4.4 ms p95; gzip 5,460,155 bytes. It records stable served bytes, authored hero ready in both profiles, and zero request/HTTP/console/page errors. The schema-4 artifact remains overall `FAIL` because no format-2 release manifest exists yet, so the release-contract validation records `Production smoke requires a readable format-2 release manifest.` Emulated mobile is not physical-device proof; a clean tagged manifest-bound rerun remains required. |
| Continuous 30-minute Rival soak | Automated 30-minute stability gate passes, with trends retained for human interpretation | UNVERIFIED | The corrected headed four-minute product calibration completes one validated real Rival race with zero overheats, timeouts, dropped simulation, or unexpected errors. It is not a 30-minute pass. Headless SwiftShader was shown unable to complete within the attempt ceiling; Rival qualification now requires headed hardware-backed WebGL. A clean manifest-bound 30-minute run and trend review remain required. |
| Final non-QA artifact and production smoke | Complete notices, checksum manifest, and installed-Chrome boot/race/editor/offline smoke pass | UNVERIFIED | The current 33-file local non-QA preview has complete notices, stable served bytes, no unexpected smoke error, authored bike ready, absent QA/performance APIs, visible Rider School progress, and Track Builder reachability. This is scoped working-tree production-preview smoke only. Format-2 source records deterministic gzip level-9 bytes/SHA beside every raw record; schema-5 production smoke requires installed Google Chrome/channel/version/viewport, exact runtime title/version/commit and `shell-v35`, complete raw/gzip served inventories before and after the offline journey, and four integrity-checked screenshots. That official path did not run. There is no clean sidecar-bound artifact, `shell-v35` offline/icon evidence, or installed-browser smoke. Retained format-1 hash `6a55c00ea36debe543ab946dee06dc5fa73cf8a13d45b047aec399469c9931a3` qualifies only the earlier build. |
| Evidence-only post-tag attestation | Final reports/evidence are pinned without changing the product tag or bytes | UNVERIFIED | Schema-v3 source checks product→evidence ancestry and an evidence-only path allowlist; binds raw/gzip inventories to the exact format-2 manifest; recomputes fixed performance gates and soak summaries/trends from cadence-bounded raw samples; enforces exact desktop/mobile and smoke identity/chronology; and requires ten candidate-bound structured QA records plus their exact command logs before QA approval. QA must hash-bind both final reports, the canonical Canyon approval record, and the promoted 1280×720 PNG. The verifier reapplies the complete guarded-promotion candidate/capture schemas and rollback constraints. The 71 release-attestation fixtures pass, establishing source-level positive and rejection behavior only. No product/evidence tag pair, real complete QA record set, approval, source-bound predecessor archive record, rollback rehearsal/data-safety record, or attestation exists; technical consistency cannot authenticate human review. |
| Support, privacy, accessibility, and About shipping surface | Public information is accurate, accessible, and owner-complete | PASS (SCOPED) / RELEASE UNVERIFIED | Current focused browser coverage exercises the rendered title entry, footer/back keyboard path, responsive surface, headings/navigation, and automated accessibility assertions. Public contact, operator/data-controller identity, host policy, retention/deletion commitments, product license, manual screen-reader review, and legal approval remain owner-blocked or unverified. |
| Deployable predecessor rollback archive | Immutable predecessor artifact can be located, retrieved, hash-verified, and staged | UNVERIFIED | The `v1.0.0-rc.1` source tag and historical reports exist, but no tested archive locator, exact archive byte count/SHA-256, source-bound format-2 manifest, or complete matching pre/post staged raw/gzip inventories are recorded. Rollback readiness cannot be inferred from the source tag. |
| No known unaddressed code-owned P0/P1 | Every mandatory code-owned gate is closed | UNVERIFIED | Earlier gameplay and launch-boundary audits found and corrected code-owned risks; independent final static re-reviews of the settled service-worker, persistence, editor-locking, immutable-attempt, and retry paths reported no remaining P0/P1/P2. Current working-tree unit/fixtures, migration, scoped browser, asset, audit, and build gates pass. This row still cannot pass because exact-candidate artifact qualification, the failing visual regression/acceptance gate, installed-browser/physical-device coverage, manual review, performance/soak, rollback, approvals, and immutable source identity remain open. |

## Historical evidence retained from an earlier `rc.2` snapshot

Every result below is retained for its exact earlier snapshot and scope; none is a pass for the current candidate source snapshot.

- Final post-adjustment whole-project strict typecheck — `PASS`.
- Final post-adjustment full lint and diff validation — `PASS`.
- Final post-adjustment Vitest — `PASS`, 9 files / 78 tests.
- `VITE_QA_MODE=1 npx vite build` — `PASS` for direct compilation only.
- Chromium quality presets + tutorial — historical `PASS`, 2/2 and approximately 1.2 minutes, but the assertions missed the bypasses and explicit lesson-event requirements now recorded above.
- Chromium migrations — `PASS`, 4/4.
- Chromium keyboard remapping — `PASS`, 1/1.
- Chromium lifecycle — `PASS`, 5/5.
- Chromium reliability — `PASS`, 6/6 after the paused fallback HUD emit and explicit Lesson 1 start.
- Chromium offline service worker — `PASS`, 1/1.
- Chromium fresh keyboard race/save/retry — `PASS`, 1/1.
- Chromium gamepad emulation + persistence + release quality — `PASS`, 3/3.
- Chromium accessibility controls — `PASS`, 7 checks; 1 touch-project case intentionally skipped.
- Chromium core fresh race/editor — `PASS`; touch-project case intentionally skipped.
- Original isolated Chromium campaign suite — `PASS`, 3/3 in 6.4 minutes.
- Hardened isolated Chromium campaign suite — `PASS`, 18/18 in 7.3 minutes: 16 independent track/mode cases plus Solo-to-Rival and Summit Mastery.
- Selected Firefox/WebKit/mobile-chrome/mobile-safari/tablet-chrome journeys — 19 initial passes / 74 project-scope skips; the only two failures were the same stale keyboard focus-order assertion. After adding Rider School to the expected tab sequence, the Firefox/WebKit targeted rerun passed 2/2. Covered fresh race, editor, settings, release-quality, WebKit lifecycle stress, and emulated phone/tablet touch/mirroring scopes.
- Controlled Chromium/mobile-chrome visual regression and repeat — `FAIL`, each 1 pass / 3 failures; editor passed, while desktop race differed by 61%, portrait race by 36%, and high contrast by 39% against 2%; baselines untouched pending owner acceptance.
- Headed local performance — `PASS`, `artifacts/performance/final-headed-measurement.json`; Chromium 149.0.7827.55 headed on macOS arm64. Desktop normal/stress mean FPS 59.31/59.31; emulated-mobile normal/stress mean FPS 59.54/59.77. Exact frame-work, draw-call, timing, heap, host, and build-size results are retained in the artifact and `QA_REPORT.md`.
- Continuous 30-minute Rival soak automated gate — `PASS`, `artifacts/performance/final-30m-soak.json`; 1,800,607 ms active workload, 58 completed races, 0 timeouts, and no harness/network/HTTP/unexpected console-page errors. Heap, input, and fixed-step trends remain recorded for manual interpretation; headless FPS and cumulative dropped time are diagnostic.
- `npm run assets:build` — `PASS`; normalized only the Basis license to the already-inventoried 9,197-byte pinned pipeline output, with no GLB, KTX2, or asset-manifest content change.
- Final `npm run assets:verify` — `PASS`.
- Final normal `npm run build` — historical `PASS` after that snapshot's final scenery-density adjustment; asset verification, strict typecheck, Vite production build, and 38,475-byte notices generation completed (SHA-256 `c959caf9bfbb3d9b051921453e8a19132c1b40fec2ef3f3db41676d3492f84a1`).
- `npm audit --audit-level=high` — `PASS`, exit 0 and 0 vulnerabilities.
- Earlier normal-build release manifest — historical `PASS`, `artifacts/history/release-manifest-1.0.0-rc.2-earlier-snapshot-format-1.json`, format 1, 21 files, 4,689,233 bytes, aggregate SHA-256 `6a55c00ea36debe543ab946dee06dc5fa73cf8a13d45b047aec399469c9931a3`; it does not match current `dist/`.
- Earlier installed-Chrome production smoke — historical `PASS`, `artifacts/production-smoke/chrome-smoke.json`; it does not qualify the current source, current `dist/`, or `shell-v35`.
- Earlier production browser inspection — historical `PASS` at desktop 1280×720 and emulated portrait 390×844; it does not qualify the current tutorial/visual candidate.

The retained performance and soak artifacts qualify an earlier QA-enabled build. The retained release manifest and installed-Chrome smoke qualify a separate earlier normal build. These are deliberately distinct historical byte sets and neither qualifies the current candidate. Emulated portrait/mobile viewport results remain responsive/local evidence only, not physical-mobile qualification.

## Required code-owned closure

1. Repeat the passing tutorial, input, gameplay, editor, persistence, responsive, accessibility-automation, campaign, cross-engine, and touch scopes against one frozen candidate, then complete manual screen-reader, physical input/device, installed-browser, true cross-tab, destructive-recovery, subjective audio/camera, and other environment-specific checks that automation has not closed.
2. Run deterministic production-length calibration against the five changed Solo/par target pairs: record clean and representative player completions, manual/physical Rookie/Rider/Ace fairness review, and exact frozen-candidate repetition of the seven Summit Mastery target/starting-heat tiers plus the source full-field classification before describing the source values as fully tuned or balanced.
3. Complete concept-art review using fresh native-size desktop and portrait captures, deliberately accept or revise the visual baselines, and rerun the controlled visual suite.
4. Repeat the passing asset gates after freezing one exact tracked candidate, then capture and review the Blender-authored hero and complete built-in fallback silhouettes/pose contacts, deterministic font rendering, Canyon panorama crop/fallback/authored-course isolation, watchtowers, grandstands, wall, and all five track identities before accepting any replacement baseline.
5. Inventory and track every intended candidate source, fixture, Node pin, provenance record, retained historical artifact, art file, font, and license; remove or explicitly exclude local-only inputs; commit once; and verify a clean tree before creating the annotated candidate tag.
6. Repeat the passing static/unit, Firefox, WebKit, touch, persistence, reliability, asset, audit, and build gates, then complete the installed-browser, physical mobile/tablet, physical gamepad, manual accessibility, audio, offline, performance, and soak matrix against that one exact final source identity.
7. Under pinned Node 26.4.0/npm 11.17.0, rerun the currently passing 45 release-manifest, 71 release-attestation, and 34 production-smoke/service-worker/release-scope fixtures against the frozen candidate; then let the real guard create its isolated clean install/build and format-2 sidecar from annotated `v1.0.0-rc.2`, regenerate the asset/license audit, and run manifest-bound `shell-v35` installed-Chrome/offline smoke against those exact replaced root bytes.
8. Archive the passing manifest and complete named smoke bundle in a separate evidence-only commit, add all ten real structured QA check/log records, real QA/accessibility/legal approvals, and predecessor evidence that binds the exact archive byte count/SHA-256 plus complete pre/post staged raw/gzip inventories and native-60→native-30 data safety; update final reports without changing product files, add their candidate-bound structured READY markers only when truthful, and hash-bind both reports, the canonical Canyon approval record, and the promoted Canyon PNG in the QA approval; create annotated `attestation/v1.0.0-rc.2`; and run the schema-v3 verifier from that clean tag. Publish it with the immutable product tag only after the verifier passes.

## Historical `v1.0.0-rc.1` predecessor

The annotated predecessor source tag remains immutable. Its historical 67-pass Playwright report, 76-test Vitest run, headed-performance capture, post-fix 30-minute soak, checksum manifest, and installed-Chrome production-smoke record remain comparison evidence only for the named predecessor bytes. They may not be relabeled as `rc.2` results or used to skip affected gates. Because no immutable archive locator, exact archive byte count/SHA-256, source-bound format-2 manifest, or complete pre/post staged served-inventory rehearsal is recorded, predecessor rollback remains **UNVERIFIED** despite the source tag and reports.

## Unverified physical and application environments

| Environment | Status | Missing evidence |
|---|---:|---|
| Actual installed Firefox | UNVERIFIED | Separate current installed-application run |
| Actual Edge | UNVERIFIED | Current installed-application run |
| Actual Safari | UNVERIFIED | Current installed Safari run; Playwright WebKit is not equivalent |
| Physical Android Chrome | UNVERIFIED | Real touch, audio, storage, GPU, thermal, and battery session |
| Physical iPhone/iPad Safari | UNVERIFIED | Real touch, audio, storage, GPU, thermal, and battery session |
| Physical gamepad | UNVERIFIED | Controller model, prompts, stuck-input cleanup, and vibration where supported |
| Subjective audio, touch comfort, visual fidelity, and race fairness | UNVERIFIED | Human review on representative physical devices and inputs |

## External owner actions

| Owner action | Status | Required evidence |
|---|---:|---|
| Genuine product-owner authorship and concept-art acceptance for the promoted visual baseline | EXTERNAL BLOCKER | The owner personally reviews the exact Canyon Practice 500 frame, authors the external approval within the 24-hour promotion window, and confirms authorship outside the hash-consistency mechanism; `external-manual-trust-boundary` is not authentication. |
| Final trademark, trade-dress, copyright, and generated-output review | EXTERNAL BLOCKER | Owner/counsel approval and archived provenance decision |
| Top-level product license | EXTERNAL BLOCKER | Owner-selected root `LICENSE` and final notice review |
| Generated-image account/agreement provenance | EXTERNAL BLOCKER | Governing account terms and prompt/session or receipt evidence |
| Neutral public workspace/repository name | PASS | The canonical workspace uses the neutral `Rivet Ridge Rally` directory name; the legacy prohibited-name folder has been retired. Fresh release scans must still reject stale absolute paths. |
| Production hosting, DNS/TLS, security headers, credentials, and deployment target | EXTERNAL BLOCKER | Owner-selected host, verified response headers, and tested immutable deploy/rollback procedure |
| Public support, release, rollback, and incident ownership | EXTERNAL BLOCKER | Named release/support/rollback/incident owners and public support path |
| Physical/application test-matrix access | EXTERNAL BLOCKER | Owner-provided devices, gamepad, installed browsers, and assigned human review |

**Current final status: NOT READY.**
