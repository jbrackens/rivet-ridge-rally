# RIVET RIDGE RALLY — Launch Readiness

# NOT READY

**Decision date:** 2026-07-15

**Candidate:** `1.0.0-rc.2` source snapshot; no annotated release tag or release-bound rebuild

**Code-owned gate status:** UNVERIFIED

**Commercial readiness status:** NOT READY

**Authority:** `QA_REPORT.md`

**Decision owner:** Unassigned

Player feedback reopened default lane controls, first-play training, and visual fidelity after the immutable `v1.0.0-rc.1` predecessor. The changed work is therefore a new `1.0.0-rc.2` candidate; the predecessor's tag, artifact, and evidence remain historical and do not qualify these bytes.

An earlier `rc.2` snapshot produced the retained static, unit, Chromium, campaign, selected cross-engine, performance, soak, manifest, and installed-Chrome results listed below. Those results are historical evidence for their exact recorded scopes and bytes; they do not qualify the current changed tutorial, visual, persistence-recovery, or race-gate work. The automated suite is intentionally paused at the owner's request. Current asset verification, strict typecheck, and normal build/notices pass. Pre-final scripted headless installed-Chrome fixed-viewport captures were manually reviewed for desktop/mobile composition and five-track distinction, but no current lint, unit, Playwright test-runner, performance, soak, audit, manifest, offline-smoke, production-smoke, or physical-device pass is claimed.

The retained format-1 release manifest at `artifacts/history/release-manifest-1.0.0-rc.2-earlier-snapshot-format-1.json` records 21 files, 4,689,233 bytes, and aggregate SHA-256 `6a55c00ea36debe543ab946dee06dc5fa73cf8a13d45b047aec399469c9931a3`; it does not match current `dist/`. On macOS 26.5.2 arm64 with Node v26.4.0 and npm 11.17.0, the permitted `npm run build` ran from 2026-07-15 10:26:16 to 10:26:21 CEST and completed successfully. Exact asset, title, icon, panorama, font, and license verification passed; strict typecheck and the 142-module Vite production compilation passed; and the 43,825-byte notices retained SHA-256 `5fa8f7b4aaf95b4cc28b9b36a10c7f451e0ef83e6ac959d530ed3fdceacb60d0`. Current `dist/` contains 24 files / 6,679,904 raw bytes with `shell-v30`; `dist/index.html` is 1,418 bytes with SHA-256 `4252cb572300d9e2dd838b7e39f8440b8b40b6762556e17cd9f3d7b44690f433`, and the 5,722-byte `dist/sw.js` has SHA-256 `f5cc9b23c2d507b9cede7a11cd72f62d05549c4ae72888853b5191d4cb6b78ca`. The 282,888-byte game chunk is `GameView-Cy_z8oq_.js`, SHA-256 `3517031f108f19e570938c10d31fb6e58af56ecb8859ea25a09355b6ef575bd4`; the 612,073-byte shared route chunk is `CoursePresentationRoute-CMRHtjX1.js`, SHA-256 `87e856adf652bb8a49b45ed3d96256221a360ca7edff9a94341a17fa63af71b8`; and the 56,935-byte editor chunk is `TrackEditorScreen-niBUL8yv.js`, SHA-256 `6c800124e378c8e16dc420210b1e696e3418d4843c537ebab3218e3797b1f5f0`. The build embeds commit `62664b1f8ba814f3ec685d228fa4e600e6e7dc84` with `dirty=true`, so it is development/build evidence rather than immutable release evidence. It includes schema-v2 checkpoint-authored Builder/Test Ride routes, strict full rotated parent-footprint containment, legacy repair access, graded campaign presentation, shared multi-section obstacle rules and distinct silhouettes, the bounded editor continuation and fit corrections, tutorial/input prompt fixes, and numbered/articulated rival presentation. Working source also contains pre-write production-smoke symlink containment, but the normal build does not execute that Node smoke path. No current release manifest, dependency audit, production/offline smoke, automated test, migration journey, browser capture, or performance command was run.

The latest permitted build also compiles active-scoped ordered tutorial clearance, measured coast/mud/grass evidence, an atomic 550 ms handoff that preserves held Ride/Turbo input, the active critical-heat latch, focused lesson retry, interactive-target keyboard guards, repeated-key pause guard, mapped-threshold device arbitration, touch-release keyboard restoration, six original touch pictograms, service-worker-controller invalidation, and `shell-v30` generation. Deferred regression source covers early-evidence rejection, ordered multi-signal lessons, held Ride across the handoff, deliberate lesson failure/retry/success with remapped-Space activation, all 12 lessons, touch-icon semantics, and finish integrity. Earlier scoped read-only reviews found no concrete P0/P1 in those tutorial/input/offline slices, but no paused automated, browser, real worker-transition, touch/accessibility, or physical-input evidence is promoted to `PASS`.

The retained headed Chromium 149 measurement and 30-minute headless Rival soak passed their earlier QA-enabled build in the recorded scopes. The current renderer-only shared `CoursePresentationRoute` maps the campaign road and course-anchored presentation from the existing scalar race state, adds positive rolling grades between protected flat corridors, and replaces the separately mounted distant-course vista. The same source adds distinct multi-part module silhouettes/contacts and expanded per-theme density/signatures. That code is present in the successful normal build, but runtime/browser, visual, automated-regression, performance, and soak evidence were **NOT RUN** while automation remains paused; those gates are **UNVERIFIED** for the current candidate.

The release remains **NOT READY**. Concept fidelity is **NOT ACCEPTED / UNVERIFIED**. Pre-final fixed-viewport desktop/mobile and five-track captures, manually reviewed after generation, show improved full-bike framing, rider articulation/`22` identity, atmosphere, and venue separation, but they predate the shared course-presentation renderer, current focal-bike bytes, new Canyon festival panorama, and staffed elevated cooling-gate watchtowers. Authoritative gameplay remains scalar while the current renderer projects the same progress/lane state onto one curved campaign presentation centerline. Source now also targets the concepts' knobby tread, brake detail, layered plastics, rear `22`, exposed exhaust, shadow depth, steering lean, speed pose, red-rock/festival horizon, and elevated bilateral live venue framing at Canyon's two unique cooling-gate locations. The successful build and source review prove compilation, a static asset contract, and coherent instance math—not the panorama's runtime crop/horizon/separation, watchtower appearance, motion, performance, similarity/legal acceptance, or owner approval. Near-field environment/material quality/detail density and the new route/bike/background/watchtower composition remain unaccepted. The retained controlled run/repeat still differ from obsolete baselines by 61% desktop, 36% portrait, and 39% high contrast. The apparent black wedge seen in one decoded high-contrast view is absent from the clean source PNG and is not a product bug, but the obsolete-baseline/frozen-composition failure remains unresolved. Tutorial integrity, current renderer qualification, final-byte capture and automated qualification, owner visual/legal acceptance, the complete cross-browser/physical-device matrix, physical/manual review, and immutable `rc.2` identity remain open.

The latest permitted build contains a continuous cream/coral/teal safety wall along both Canyon Kickoff and Rider School course edges. Source review verifies one quality-tier instanced mesh batch, centers at x ±7.70, overlapping 8/6/4 m Low/Medium/High spans, and explicit authored-Test-Ride exclusion. That proves source inclusion and compilation only; browser appearance, non-collision readability, renderer draw calls, performance, accessibility, and owner acceptance remain **UNVERIFIED**.

The build also contains Canyon-only outward-rising timber grandstands and staffed elevated cooling-gate watchtowers. The watchtowers raise only existing deck/canopy/post instances, use mirrored teal/coral roofs, and move two/three/four existing spectators onto each deck on Low/Medium/High while retaining the same five venue batches. Source math and independent reviews verify all capacities and vertical clearances; exact QA-fast counts; production-High's 22 route pockets plus four towers for 26 pockets / 104 tiers; Canyon tower staffing totals of 8/12/16; Rider School totals of 4/6/8; and zero custom-course Canyon watchtowers/tier pockets. Separate per-theme density rules remain active. The 5.6 m showcase guard rejects no current production-High pocket because all already clear it. The accepted concepts were inspected directly, but no current desktop/portrait browser frame, motion review, draw-call measurement, performance run, or owner comparison qualifies the result; visual acceptance remains **UNVERIFIED**.

Format-2 release-provenance hardening is present in source and remains **UNVERIFIED**. It captures clean `HEAD`, binds package/lock/Node reads to a detached checkout and revalidates them, preserves the exact annotated tag object/type through the build, rechecks the absolute npm CLI file identity/SHA-256 after every invocation, isolates both npm config scopes with scripts enabled, and rejects unexpected ignored inputs at all three build phases. Only regular isolated output can replace root `dist/`; guarded recursive cleanup removes stale `dist/` and malformed sidecar directories on failure. Hostile-config/fake-CLI fixtures include same-commit retagging and CLI mutation boundaries, but none ran. Historical evidence remains unchanged and the dirty untagged candidate cannot satisfy the guard.

The latest permitted build also compiles, but does not behaviorally qualify, the safe 62 normal-Ride heat ceiling, pairwise AI-vs-AI collision resolution, numbered `1–4` campaign/Rider School start treatment with authored-Test-Ride exclusion, shared portrait-or-sub-680 px camera predicate, 320–780 px / 140% normal-and-mirrored control geometry, and semantic touch/Recovery states. That build also contains source for runtime Git commit/dirty binding, pre/post served-byte verification, explicit performance quality/device/error gates, offline cached-Practice smoke, and fail-closed fresh-reload `shell-v30` installation; the build proves compilation only. Their unit, E2E, smoke-flow/support, install, browser, offline, accessibility, visual, performance, and physical-device checks were **NOT RUN**. These additions do not change **UNVERIFIED**, **NOT ACCEPTED**, or **NOT READY** status.

An earlier source-only review identified and the same build compiled four additional P1 corrections: fixed-step catch-up now advances environment, player, track, AI, and contacts once per due simulation tick; exact legacy A/D lane defaults migrate without destroying actions already assigned to the arrows; authored Test Rides cannot inherit the Canyon panorama; and the built-in bike fallback now retains the focal knobby-wheel, panel, exhaust, brake, and rear-`22` silhouette. The deterministic licensed display font and candidate-specific atomic smoke-evidence layout are also present. No focused regression, fallback/font render, or smoke execution ran during the pause.

The latest three-way static audit found no P0 and identified 16 unique P1 defects. Production source now corrects all 16: truthful pre-countdown HUD values and competitive-only results copy; typed wheelie/crash feedback with consecutive-crash counting; selectable/editable placements, full accepted-route editor navigation, and non-color pointer validity; bounded blocked database opens, future-version-safe strict profile records, atomic corrupt-track recovery/export, and strict bounded import preflight; plus safe-area/scrolling pause geometry, visible toggle focus, 44 px tutorial Skip, in-card narrow captions, and UI-scale-aware narrow telemetry. Focused regression source exists and the latest permitted build compiles these changes. No relevant unit, component, Playwright, browser geometry, persistence failure path, editor interaction, audio/camera, cross-device, or manual accessibility check ran, so these gates remain **UNVERIFIED** and the candidate remains **NOT READY**. The current 1,418-byte `dist/index.html` SHA-256 is `4252cb572300d9e2dd838b7e39f8440b8b40b6762556e17cd9f3d7b44690f433`; this build byte is not a release manifest or served-byte qualification.

A follow-up static audit also found no P0. The latest permitted build compiles held-touch precedence, selective atomic profile recovery, current-snapshot cross-tab track recovery, transactional replay pruning, one 500-module boundary, exact pending Test Ride retry, budget-before-materialization recovery encoding, shared authored-module contacts, distinct runtime module silhouettes, campaign grade, and expanded venue density/signatures. It also compiles schema-v2 checkpoint-only Route turn/rise anchors shared by Builder and Test Ride, exact identity mapping for omitted anchors and migrated v1 routes, and strict full rotated parent-footprint Start/Finish containment. Legacy v1 tracks needing the stricter repair remain readable/editable without quarantine, but save/Test Ride/export stay blocked until repair. All focused migration, unit, browser, visual, accessibility, and performance evidence remains **NOT RUN / UNVERIFIED**.

## Gate summary

| Gate | Required outcome | Status | Current evidence |
|---|---|---:|---|
| Candidate identity | Final bytes tied to a clean source commit and annotated `v1.0.0-rc.2` tag | UNVERIFIED | Format-2 source now enforces exact-tag identity through an isolated detached clean-install/build, but its fixture was not run and no annotated tag, clean release-bound rebuild, or current matching sidecar/smoke exists. Every intended source, fixture, provenance record, retained artifact, art file, font, and Node pin is tracked by the commit containing this record, but post-commit qualification remains required. |
| Strict typecheck | Whole-project strict TypeScript check passes | PASS | Current `npm run build` executed the whole-project strict check successfully on 2026-07-15 under Node v26.4.0. |
| Latest PB/AI/responsive/release corrective slice | Standing PB details, per-step rival contact, narrow/short-phone UI, and exact served-root/toolchain binding pass their focused gates | UNVERIFIED | The latest permitted build compiles the current source, and three independent second-pass static reviews completed; the visual pass found narrow marker, overlay-stacking, vertical-containment, and timeout risks that were source-corrected before handoff. Its unit/component/E2E/release-fixture/smoke-support/worker-install regressions, current browser geometry, persistence round trip, official classification, and offline/served-root paths were **NOT RUN** while the owner's pause remains active. |
| Final static gameplay/editor/persistence/accessibility P1 slice | All source-corrected defects pass focused behavioral and browser gates | UNVERIFIED | Source-only audits found no P0 and corrected the prior 16 P1 defects plus the authored-route, full-parent-footprint, legacy-repair, fallback-range, and rendered-yaw consistency boundaries. Production and focused regression source cover the changes, and the latest permitted build typechecks and compiles them. None of the unit/component/Playwright, migration, browser geometry, persistence failure, editor interaction, audio/camera, cross-device, or manual accessibility checks ran. |
| Lint | Whole-project lint passes | UNVERIFIED | Earlier snapshot passed; current changes have not been checked while the suite is paused. |
| Unit tests | Whole-project deterministic suite passes | UNVERIFIED | Earlier snapshot passed 9 files / 78 tests; current changes have not been tested. |
| Asset pipeline and verification | Every shipped asset matches the committed licensed inventory | UNVERIFIED | Current `assets:verify` passes through the build wrapper: the 29,132-byte GLB hash matches, 22 Meshopt views / 34 nodes / 15 meshes / 7 materials / one texture decoded, the 30-mesh-bearing-node ceiling and merged tread/number composition passed, and the validator reported 0 errors / 2 expected warnings / 3 informational notices. The panorama contract matches its exact 1,801,764 bytes/SHA, 1774×887 opaque-RGB PNG structure, single C2PA chunk, and embedded provenance markers. The bundled Barlow Condensed 700/900 WOFF2 files and complete SIL OFL 1.1 text also match their recorded bytes, signatures, and SHA-256 values. Cryptographic C2PA validation, rendered-font/fallback proof, human similarity/legal review, and the complete current source/license/static audit remain open, so the broader release gate is unverified. |
| Normal release build | Repository build wrapper and notices complete | PASS | Current `npm run build` ran from 2026-07-15 10:26:16 to 10:26:21 CEST and exited 0 after exact 3D manifest/directory, shipped/source-title, app-icon, panorama, font, and license verification, strict typecheck, 142-module Vite production compilation, and notices verification. `dist/` has 24 files / 6,679,904 raw bytes and `shell-v30`; the 1,418-byte `dist/index.html` has SHA-256 `4252cb572300d9e2dd838b7e39f8440b8b40b6762556e17cd9f3d7b44690f433`; the 5,722-byte `dist/sw.js` has SHA-256 `f5cc9b23c2d507b9cede7a11cd72f62d05549c4ae72888853b5191d4cb6b78ca`; and the 43,825-byte notices have SHA-256 `5fa8f7b4aaf95b4cc28b9b36a10c7f451e0ef83e6ac959d530ed3fdceacb60d0`. This does not close manifest, smoke, audit, test, migration, performance, or immutable-source gates. |
| Dependency audit | Installed dependency tree has no reported vulnerabilities | UNVERIFIED | Historical audit passed, but the current lockfile and final installed tree require a fresh candidate audit. |
| Comprehensive first-play tutorial | Earned path completes every lesson and drill below three minutes and remains replayable | UNVERIFIED | Source now uses one tutorial lap, holds simulation/time paused behind the intro, masks any retained editor Test Ride payload, requires actual Ride input at usable grounded speed, and routes an incomplete finish into restart/notice. A separate active-lesson gate ignores evidence recorded before that lesson begins, enforces ordered jump/grass/recovery signals, measures actual coast/mud/grass speed loss, and freezes simulation atomically for the 550 ms presentation handoff without discarding held Ride/Turbo input. Every incomplete riding lesson exposes **Retry this lesson**: it preserves the active lesson, earlier markers, and cumulative recap while resetting the authoritative attempt, active evidence, one-shot obstacle handling, replay/presentation state, and keyboard/touch latches. Interactive-target guards keep native button activation available even when Space is remapped to Pause or Recover. Fresh-intro and paused-training Settings now retain one mounted tutorial engine/canvas, keep started training paused, apply live controls, and restore origin focus without persisting a skip; recovery text adds `Hold` once. Named bump/barrier/surface/final-recovery events, 12+2 earned completion, non-color progress, storage-failure routing, input-adaptive prompts, the six-control recap, and an explicitly labeled skip are implemented. Critical heat latches within its active lesson so immediate Turbo release remains safe without sustained red-zone heat or overheat. Deferred unit/browser source covers early evidence, held Ride beyond the handoff, heat release, deliberate bump failure/retry/success, remapped-Space activation, Settings lifecycle, recovery copy, all 12 lessons, and finish decisions, but it was **NOT RUN**. The preceding capture showed only the earlier intro, arrow map, 12 markers, and Lesson 1; the full timed sequence, focused-retry negative path, editor isolation, early-finish integration, and device paths remain unexecuted while testing is paused. |
| Pre-race loading/countdown | Asset readiness, accessible countdown, and zero-time start are enforced | UNVERIFIED | Source waits for the compressed rider or a five-second built-in fallback deadline, using a dedicated Three.js manager so timeout/disposal aborts the request and terminates the KTX2 loader. Canyon/tutorial readiness also settles its isolated panorama load or generated-sky fallback within five seconds and closes late/disposed bitmaps. The engine/race clock remains paused through `3 → 2 → 1`, paints visible `GO` before `racing`, and exposes a deterministic gate phase; tutorial remains paused behind its explicit start. Focused stalled-bike and failed-panorama test source exists and a normal strict build passes; runtime/browser/lifecycle evidence is paused. |
| Persistence and editor-session recovery safety | Retry cannot overwrite an unread valid profile; session-mode Test Ride and return remain usable; recovery cannot reset a live attempt | UNVERIFIED | Source blocks writes after an initial profile-load failure and reconciles valid stored settings/progress before writing fallbacks. Track Builder launches a validated in-memory Test Ride immediately while a best-effort save runs independently, and separately retains the exact thumbnail-bearing draft, undo/redo stacks, tool selection, and valid placement selection. Custom pause/results return directly to that retained session even without IndexedDB. Profile progress is no longer an engine-effect dependency, so retrying storage from paused Settings cannot reconstruct the live race. Focused store, unavailable-IndexedDB editor-return, results-return, and paused-Settings lifecycle regression source exists, but none was executed. |
| Keyboard defaults and remapping | Arrow-cluster lane defaults, targeted migration, and custom remaps work | UNVERIFIED | Earlier migrations passed 4/4, remapping 1/1, race 1/1, and smoke confirmed `← Left` / `→ Right`. Current source additionally makes native-v3 migration conflict-safe: an action occupying ArrowLeft/ArrowRight moves to the vacated KeyA/KeyD before the exact historical lane pair claims the arrows. The latest source gives each remap button an action-specific accessible name, rejects focus-navigation/browser/system keys with explicit errors, and suppresses browser defaults for every effective accepted gameplay binding outside interactive controls. Its focused unit/browser source was not run while testing is paused. |
| Modes and progression | Complete campaign/mode matrix remains correct | UNVERIFIED | Earlier isolated suites passed 3/3 and 18/18. Current engine changes have not received a regression run. |
| Gamepad, touch, accessibility, and responsive layouts | All required projects and interaction gates pass | UNVERIFIED | Source derives gamepad activity from the same mapped command thresholds it returns, so neutral drift and sub-action trigger values cannot suppress keyboard/touch. A follow-up review found active mapped gamepad input still preceded held touch; source now orders held touch first and resumes keyboard/gamepad only after touch release. Gamepad Start now claims prompt priority through `InputManager` before pausing, and disconnect clears stale gamepad prompts back to the remembered non-gamepad device, Keyboard or Touch. The portrait touch cluster uses six original `currentColor` rally pictograms while preserving the existing named buttons, visible Ride/Turbo text, non-focusable decorative SVGs, hit areas, safe areas, and mirrored meaning. Pre-final scripted headless installed-Chrome sessions captured fixed 1672×941 and 390×844 viewports with no recorded console/page/request/HTTP failures, but they predate these final bytes and did not exercise headed/native-device rendering, accessibility behavior, touch ergonomics, gamepad behavior, or physical hardware. New unit/browser assertions were added but not run; fresh portrait comparison, physical inputs/devices, accessibility, and the full matrix remain unverified. |
| Visual acceptance | Desktop/mobile composition, five-track identity, and Builder/Test Ride presentation meet accepted concept direction | UNVERIFIED | **NOT ACCEPTED.** Pre-final captures predate the graded shared `CoursePresentationRoute`, distinct multi-part module silhouettes, expanded festival/per-theme density and signatures, dirt/accessibility pass, current focal-bike bytes and fallback, deterministic display font, Canyon panorama, staffed cooling-gate watchtowers, and schema-v2 authored Builder/Test Ride centerline. The latest source/build addresses the earlier flat-grade, collapsed-jump, underscaled-density, incomplete-signature, and identity-only authored-route findings, but no current browser frame, motion/performance evidence, or owner comparison shows that those changes meet the concepts. Near-field finish and the complete composition remain unaccepted; final-byte visual evidence, similarity/trade-dress review, and owner acceptance are required. |
| Canyon tiered-grandstand/watchtower visual and performance acceptance | Raised crowd structures match the concepts without overlap, route occlusion, density leakage, or budget regression | UNVERIFIED | The normal build and source reviews cover Canyon-only two/three/four-row tiers; elevated staffed cooling-gate watchtowers with mirrored roofs and 2/3/4 deck occupants; exact QA-fast 8-pocket, 16/24/32-tier, four-tower, and 8/12/16 staffing counts; production-High 26-pocket/104-tier counts; Rider School's two towers and 4/6/8 staffing; yaw/top-surface/vertical-clearance math; the 5.6 m showcase guard; zero custom Canyon towers/tier pockets; and reuse of the existing five venue batches. Separate per-theme density rules remain active. Deferred diagnostics and exact browser assertions exist but were **NOT RUN**. No current desktop/portrait frame, actual draw-call/performance measurement, accessibility review, or owner acceptance exists. |
| Canyon edge-wall visual/performance acceptance | Continuous edge treatment is readable, non-misleading, isolated from authored courses, and within render budgets | UNVERIFIED | The normal build and source review cover the one-batch quality-tier implementation, coverage/count math, x ±7.70 placement, all-tier shadow receiving with High-only shadow casting, and zero-instance authored exclusion. Deferred diagnostics and browser assertions exist but were **NOT RUN**. No current frame, renderer measurement, accessibility review, or owner acceptance exists. |
| Visual regression | Accepted desktop/mobile/editor/high-contrast baselines pass | FAIL | Retained frozen captures fail obsolete baselines by 61% desktop, 36% portrait, and 39% high contrast. A dedicated production-scale Canyon 500 m curved-route snapshot case is now present without changing the existing baselines, but it has not been executed or accepted while automation is paused. The inspected black wedge was a decoder artifact, not a source-PNG/product defect. Revised accepted baselines and a rerun are still required. |
| Focused lifecycle/reliability/offline | Required focused Chromium behaviors pass | UNVERIFIED | Earlier lifecycle 5/5, reliability 6/6, and offline 1/1 remain historical. Current source commits each finished attempt synchronously/idempotently before guarded results presentation, prevents storage recovery from rebuilding the live engine, cancels/disposes rider and panorama work, closes late bitmaps, makes paused content inert, ignores repeated keyboard pause events, and enforces exact `shell-v30` acknowledgement/current-cache preference with the panorama in the core cache. Every later service-worker `controllerchange` immediately clears the page readiness claim until a fresh exact-generation acknowledgement. Synthetic page-side invalidation and repeat-key regressions exist but were not run; real worker install/activation/cache transitions and all other current finish/editor/tutorial/renderer/persistence/race-gate/cache changes remain unqualified. |
| Full cross-browser/device matrix | Chromium, Firefox, WebKit, mobile/tablet, persistence, lifecycle, offline, and error gates pass | UNVERIFIED | Earlier focused Chromium, campaign, installed-Chrome, and selected Firefox/WebKit/mobile/tablet scopes passed. The current candidate, complete all-project suite, installed non-Chrome applications, and physical-device/input projects remain unqualified. |
| Headed local performance | Desktop normal/stress mean ≥58 FPS with p95 work ≤16.67 ms; emulated-mobile normal/stress mean ≥30 FPS with p95 work ≤33.33 ms; gzip bytes <12,000,000 | UNVERIFIED | Source now prevents a headless measurement from reporting release `PASS` and automates these GAME_SPEC-derived floors, but it was not run. Retained measurement passed an earlier build only. Emulated mobile remains a local technical floor; representative physical mobile is still required. |
| Continuous 30-minute Rival soak | Automated 30-minute stability gate passes, with trends retained for human interpretation | UNVERIFIED | Retained soak passed an earlier build. Current simulation/tutorial/rendering changes have not been soaked. |
| Final non-QA artifact and production smoke | Complete notices, checksum manifest, and installed-Chrome boot/race/editor/offline smoke pass | UNVERIFIED | The latest permitted normal build/notices are a PASS only for build-time asset verification, strict typecheck, Vite compilation, and notices verification; they are not an official artifact, manifest, or smoke result. The format-2 guard performs its own isolated clean install/build and rejects foreign root output. Production smoke requires an internally consistent format-2 sidecar, binds the runtime Git commit and every served file/aggregate before Chrome, opens cached Practice to `racing` offline, reverifies served bytes after the journey, waits for both restart gates, and stages each run before atomic promotion under `artifacts/production-smoke/candidates/<manifest-sha>/runs/<run-id>/`. Neither fixture nor real path ran. There is no current official sidecar/isolated `dist/`, `shell-v30` offline evidence, dependency audit, or installed-browser smoke. Retained format-1 hash `6a55c00ea36debe543ab946dee06dc5fa73cf8a13d45b047aec399469c9931a3` qualifies only the earlier build. |
| Evidence-only post-tag attestation | Final reports/evidence are pinned without changing the product tag or bytes | UNVERIFIED | Operations now requires a separate allowlisted evidence commit and annotated `attestation/v1.0.0-rc.2` tag containing the archived format-2 manifest, named smoke bundle, attestation record, and final reports. No product/evidence tag pair or attestation exists. |
| Deployable predecessor rollback archive | Immutable predecessor artifact can be located, retrieved, hash-verified, and staged | UNVERIFIED | The `v1.0.0-rc.1` source tag and historical reports exist, but no tested artifact archive locator plus retrieval/hash evidence is recorded. Rollback readiness cannot be inferred from the source tag. |
| No known product P0/P1 | Every mandatory code-owned gate is closed | UNVERIFIED | The latest source-only follow-up found no P0 and source-addressed the known authored-route and full rotated parent-footprint P1 boundaries, including non-destructive legacy repair access. Campaign grade, module contacts/silhouettes, Bump Row/Offset Barrier behavior, venue density/signatures, operation-specific Test Ride recovery, and bounded recovery/export encoding are also source-addressed. All remain behaviorally and visually **UNVERIFIED** because current unit/migration/browser/artifact qualification, visual acceptance/regression, the full cross-browser/physical-device matrix, manual review, performance, and immutable source identity remain open. |

## Historical evidence retained from an earlier `rc.2` snapshot

Every result below is retained for its exact earlier snapshot and scope; none is a pass for the current candidate source snapshot.

- Final post-adjustment whole-project strict typecheck — `PASS`.
- Final post-adjustment full lint and diff validation — `PASS`.
- Final post-adjustment Vitest — `PASS`, 9 files / 78 tests.
- `VITE_QA_MODE=1 npx vite build` — `PASS` for direct compilation only.
- Chromium quality presets + tutorial — historical `PASS`, 2/2 and approximately 1.2 minutes, but the assertions missed the bypasses and explicit lesson-event requirements now recorded above.
- Chromium migrations — `PASS`, 4/4.
- Chromium keyboard remapping — `PASS`, 1/1.
- Chromium lifecycle — `PASS`, 5/5.
- Chromium reliability — `PASS`, 6/6 after the paused fallback HUD emit and explicit Lesson 1 start.
- Chromium offline service worker — `PASS`, 1/1.
- Chromium fresh keyboard race/save/retry — `PASS`, 1/1.
- Chromium gamepad emulation + persistence + release quality — `PASS`, 3/3.
- Chromium accessibility controls — `PASS`, 7 checks; 1 touch-project case intentionally skipped.
- Chromium core fresh race/editor — `PASS`; touch-project case intentionally skipped.
- Original isolated Chromium campaign suite — `PASS`, 3/3 in 6.4 minutes.
- Hardened isolated Chromium campaign suite — `PASS`, 18/18 in 7.3 minutes: 16 independent track/mode cases plus Solo-to-Rival and Summit Mastery.
- Selected Firefox/WebKit/mobile-chrome/mobile-safari/tablet-chrome journeys — 19 initial passes / 74 project-scope skips; the only two failures were the same stale keyboard focus-order assertion. After adding Rider School to the expected tab sequence, the Firefox/WebKit targeted rerun passed 2/2. Covered fresh race, editor, settings, release-quality, WebKit lifecycle stress, and emulated phone/tablet touch/mirroring scopes.
- Controlled Chromium/mobile-chrome visual regression and repeat — `FAIL`, each 1 pass / 3 failures; editor passed, while desktop race differed by 61%, portrait race by 36%, and high contrast by 39% against 2%; baselines untouched pending owner acceptance.
- Headed local performance — `PASS`, `artifacts/performance/final-headed-measurement.json`; Chromium 149.0.7827.55 headed on macOS arm64. Desktop normal/stress mean FPS 59.31/59.31; emulated-mobile normal/stress mean FPS 59.54/59.77. Exact frame-work, draw-call, timing, heap, host, and build-size results are retained in the artifact and `QA_REPORT.md`.
- Continuous 30-minute Rival soak automated gate — `PASS`, `artifacts/performance/final-30m-soak.json`; 1,800,607 ms active workload, 58 completed races, 0 timeouts, and no harness/network/HTTP/unexpected console-page errors. Heap, input, and fixed-step trends remain recorded for manual interpretation; headless FPS and cumulative dropped time are diagnostic.
- `npm run assets:build` — `PASS`; normalized only the Basis license to the already-inventoried 9,197-byte pinned pipeline output, with no GLB, KTX2, or asset-manifest content change.
- Final `npm run assets:verify` — `PASS`.
- Final normal `npm run build` — historical `PASS` after that snapshot's final scenery-density adjustment; asset verification, strict typecheck, Vite production build, and 38,475-byte notices generation completed (SHA-256 `c959caf9bfbb3d9b051921453e8a19132c1b40fec2ef3f3db41676d3492f84a1`).
- `npm audit --audit-level=high` — `PASS`, exit 0 and 0 vulnerabilities.
- Earlier normal-build release manifest — historical `PASS`, `artifacts/history/release-manifest-1.0.0-rc.2-earlier-snapshot-format-1.json`, format 1, 21 files, 4,689,233 bytes, aggregate SHA-256 `6a55c00ea36debe543ab946dee06dc5fa73cf8a13d45b047aec399469c9931a3`; it does not match current `dist/`.
- Earlier installed-Chrome production smoke — historical `PASS`, `artifacts/production-smoke/chrome-smoke.json`; it does not qualify current `dist/` or `shell-v30`.
- Earlier production browser inspection — historical `PASS` at desktop 1280×720 and emulated portrait 390×844; it does not qualify the current tutorial/visual candidate.

The retained performance and soak artifacts qualify an earlier QA-enabled build. The retained release manifest and installed-Chrome smoke qualify a separate earlier normal build. These are deliberately distinct historical byte sets and neither qualifies the current candidate. Emulated portrait/mobile viewport results remain responsive/local evidence only, not physical-mobile qualification.

## Required code-owned closure

1. Resume the paused automated/manual checks and prove the implemented tutorial integrity changes, negative bypass cases, timing, keyboard/touch/gamepad lesson behavior, standing-PB persistence/comparisons, per-tick world/AI/contact coupling, conflict-safe v3 keyboard migration, authoritative countdown HUD/results copy, typed crash feedback/counting, editor selection/long-route/preview/recovery flows, blocked/future/corrupt persistence and strict import, and 320/621/780 plus 320×568 focus/target/caption/pause/UI-scale geometry.
2. Complete concept-art review using fresh native-size desktop and portrait captures, deliberately accept or revise the visual baselines, and rerun the controlled visual suite.
3. Capture and review the current generated-bike and built-in fallback silhouettes, deterministic font rendering, Canyon panorama crop/fallback/authored-course isolation, watchtowers, grandstands, wall, and all five track identities before accepting any replacement baseline.
4. Inventory and track every intended candidate source, fixture, Node pin, provenance record, retained historical artifact, art file, font, and license; remove or explicitly exclude local-only inputs; commit once; and verify a clean tree before creating the annotated candidate tag.
5. Complete the static/unit, Firefox, WebKit, installed-browser, physical mobile/tablet, physical gamepad, accessibility, touch, audio, persistence, reliability, offline, performance, and soak matrix against that one exact final source identity.
6. Under pinned Node 26.4.0/npm 11.17.0, execute the deferred fake-CLI manifest, production-smoke-support/flow, and service-worker-install fixtures—including child-Node identity, credential rejection, root/index equality, same-origin navigation, runtime source-commit binding, single-index-response caching, race-gate waits, post-journey byte verification, and non-overwriting atomic evidence promotion—then let the real guard create its isolated clean install/build and format-2 sidecar from annotated `v1.0.0-rc.2`; regenerate the asset/license audit and run manifest-bound `shell-v30` installed-Chrome/offline smoke against those exact replaced root bytes.
7. Archive the passing manifest and named smoke bundle in a separate evidence-only commit, update the final reports without changing product files, create annotated `attestation/v1.0.0-rc.2`, and publish it with the immutable product tag. Separately locate, retrieve, hash-verify, and stage-smoke the actual predecessor archive before claiming rollback readiness.

## Historical `v1.0.0-rc.1` predecessor

The annotated predecessor source tag remains immutable. Its historical 67-pass Playwright report, 76-test Vitest run, headed-performance capture, post-fix 30-minute soak, checksum manifest, and installed-Chrome production-smoke record remain comparison evidence only for the named predecessor bytes. They may not be relabeled as `rc.2` results or used to skip affected gates. Because no immutable artifact locator plus successful retrieval/hash rehearsal is recorded, predecessor rollback remains **UNVERIFIED** despite the source tag and reports.

## Unverified physical and application environments

| Environment | Status | Missing evidence |
|---|---:|---|
| Actual installed Firefox | UNVERIFIED | Separate current installed-application run |
| Actual Edge | UNVERIFIED | Current installed-application run |
| Actual Safari | UNVERIFIED | Current installed Safari run; Playwright WebKit is not equivalent |
| Physical Android Chrome | UNVERIFIED | Real touch, audio, storage, GPU, thermal, and battery session |
| Physical iPhone/iPad Safari | UNVERIFIED | Real touch, audio, storage, GPU, thermal, and battery session |
| Physical gamepad | UNVERIFIED | Controller model, prompts, stuck-input cleanup, and vibration where supported |
| Subjective audio, touch comfort, visual fidelity, and race fairness | UNVERIFIED | Human review on representative physical devices and inputs |

## External owner actions

| Owner action | Status | Required evidence |
|---|---:|---|
| Final trademark, trade-dress, copyright, and generated-output review | EXTERNAL BLOCKER | Owner/counsel approval and archived provenance decision |
| Top-level product license | EXTERNAL BLOCKER | Owner-selected root `LICENSE` and final notice review |
| Generated-image account/agreement provenance | EXTERNAL BLOCKER | Governing account terms and prompt/session or receipt evidence |
| Neutral public workspace/repository name | PASS | The canonical workspace uses the neutral `Rivet Ridge Rally` directory name; the legacy prohibited-name folder has been retired. Fresh release scans must still reject stale absolute paths. |
| Production hosting, DNS/TLS, security headers, credentials, and deployment target | EXTERNAL BLOCKER | Owner-selected host, verified response headers, and tested immutable deploy/rollback procedure |
| Public support, release, rollback, and incident ownership | EXTERNAL BLOCKER | Named release/support/rollback/incident owners and public support path |
| Physical/application test-matrix access | EXTERNAL BLOCKER | Owner-provided devices, gamepad, installed browsers, and assigned human review |

**Current final status: NOT READY.**
